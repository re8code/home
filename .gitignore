.DS_Store
# 프로젝트 매니페스트 — public 저장소이므로 커밋하지 않는다(지시서 "마무리" 규칙).
# 다만 그 규칙이 드는 이유(계정 이메일 공개)는 이 저장소에서는 이미 무의미하다 —
# 같은 이메일이 추적되는 문서에 15곳 있다. 여기서 실제로 막는 것은 앞으로 들어갈 값이다:
# billing_ref·recovery_ref·account_ref·실제 금액·갱신일은 사람이 채우는 칸이고,
# 한 번 커밋되면 히스토리에서 지우기 어렵다. 비어 있는 지금 막아둔다.
# 대가 — 이 파일은 장비를 따라오지 않는다. tasks/매니페스트-생성-지시서.md로 다시 생성한다.
.project-meta.json
