# DEVLOG.md — 작업 일지

## 2026-08-21
- Figma MCP 서버를 project scope로 등록.
  - 처음엔 `.mcp.json`에 직접 `figma` 서버(`https://mcp.figma.com/mcp`)를 추가했으나, 이미 `figma@claude-plugins-official` 플러그인(user scope, MCP 서버 + `figma-use` 등 스킬 12개 포함)이 설치되어 있어 중복 확인됨.
  - 논의 후 플러그인을 유지하되 scope만 user → project로 변경하기로 결정: `claude plugin uninstall figma` → `claude plugin install figma@claude-plugins-official --scope project`. `.claude/settings.json`의 `enabledPlugins`에 등록되어 git으로 팀과 동기화됨.
  - 수동으로 추가했던 `.mcp.json`의 `figma` 항목은 중복이라 제거(빈 파일도 삭제).
  - `claude mcp login plugin:figma:figma`로 OAuth 인증 완료, `claude mcp list` 기준 `✔ Connected` 확인.
- "작업 완료 후 PRD.md/DEV_PLAN.md/CLAUDE.md/DEVLOG.md 4개 문서 점검" 규칙을 `CLAUDE.md`의 "저장소 관례" 섹션에 명문화.
- `tasks/Philosophy.md` 핵심 지향점에 "최종 목적: 대학생 코딩 컨설팅(코딩 수업뿐 아니라 대학 생활 전반을 아우름)" 보완.
- `tasks/needs.md` 검토 후 `PRD.md`/`DEV_PLAN.md` Scope 정리: oj/lms/business/studio 4개 사이트 모두 별도 서브도메인으로 직접 제작·추후 링크 연결 방침 확정.
  - `PRD.md` §3: "프로젝트 의뢰"/"오프라인 수업 문의" 페이지를 In Scope(이 저장소 내 폼 제작)에서 제외, Out of Scope(외부 서브도메인)로 이동 — business/studio 항목 추가.
  - `PRD.md` §6/§8: 외부 링크·준비중 배지 대상을 OJ/LMS → OJ/LMS/business/studio 4개로 확장, 폼 처리 방식 관련 미결 사항 제거(해당 페이지를 더 이상 이 저장소에서 만들지 않으므로).
  - `DEV_PLAN.md`: Phase 2("프로젝트 의뢰/오프라인 문의 페이지 제작")를 삭제하고, Phase 3(외부 연동 정리)에서 4개 서브도메인 링크 연결을 함께 다루도록 재구성.
- `index.html`을 직접 건드리는 리뉴얼 작업 특성상, 일단락될 때까지 `main` 병합 보류 방침 확정 → `DEV_PLAN.md` §2(브랜치 전략)에 반영. `dev`에만 커밋/푸시.
- `tasks/needs.md` 스펙대로 `index.html` 상단 내비게이션 1차 구현: 로고만 좌측, 나머지(원장님의 낙서/wonoj/LMS/business/studio + 1:1 상담 신청)는 우측 하나의 그룹으로 배치. oj/lms/business/studio 4개는 서브도메인이 아직 없어 "준비중" 배지가 붙은 비활성(`preventDefault`) 링크로 임시 처리, `원장님의 낙서`만 `graffiti.html`로 실제 연결. 데스크톱/모바일 메뉴 모두 클로드 인 크롬으로 로컬 서버(`localhost:8765`) 확인 완료 — 콘솔 에러 없음, 준비중 항목 클릭 시 이동 안 됨, 낙서 링크 정상 이동 확인. `PRD.md` §8 미결 사항에 임시 처리 방식 기록.
- 헤더의 "1:1 상담 신청" 버튼(데스크톱/모바일 모두)을 `#cta` 앵커 대신 구글 폼(`docs.google.com/forms/...`)으로 바로 연결, `target="_blank"`로 새 탭 오픈. 클로드 인 크롬으로 새 탭 오픈·원본 탭 유지 확인.
- 헤더 "wonoj" 항목을 "준비중" 비활성 상태에서 실제 링크(`https://oj.recode.ai.kr`, 새 탭)로 전환(데스크톱/모바일 모두). LMS/business/studio는 아직 "준비중" 상태 유지. `PRD.md` §8에 OJ 연결 완료 반영.
- `tasks/needs.md`가 "랜딩 2번째 레이어(히어로 + 우측 카테고리 게이지 버튼)" 요청으로 갱신됨을 확인. 검토 결과 `mockup.html` 히어로 섹션이 이미 거의 동일 구조(타이머 게이지 사이드 리스트)로 구현되어 있음을 발견 — 카테고리만 4개(Language/Algorithm/Web & WebApp/Unity & Unreal) → 5개(+ Agent AI, Unity & Unreal→Unity)로 조정 필요.
- Figma MCP로 신규 파일 "Recode Coding — Hero Wireframe" 생성(`https://www.figma.com/design/PBS0zISWDoNU3k87kJDvhs`) 후 해당 히어로 레이어(메인 배너 + 5개 카테고리 게이지 사이드 리스트)를 와이어프레임으로 제작, `mockup.html` 톤(인디고 브랜드, 다크 배경) 참고. 디자인 시스템 라이브러리가 없는 신규 파일이라 수동 구성, 폰트는 Pretendard 미설치로 Noto Sans KR 대체 사용. 스크린샷으로 사용자에게 전달, 실제 코드 반영은 아직 안 함(디자인 검토 단계).
- 같은 Figma 파일에 카테고리 게이지 표현 방식 3안(A. 버튼 하단 바 / B. 전체 배경 게이지 / C. 배지 원형 링)을 나란히 그려 비교본 제작, 사용자에게 전달.
- B(전체 배경 게이지)로 확정, `mockup.html`에 반영:
  - 히어로 사이드 리스트 4개 항목의 하단 바 게이지(`h-[3px]` 트랙)를 제거하고, 버튼 전체를 덮는 반투명 배경 채움(`data-gauge`, 카테고리별 accent 컬러 30% 투명도)으로 교체. `.side-item.is-active` 정적 하이라이트는 제거(게이지 채움 자체가 활성 표시 역할). JS(`activateSlide`)는 `[data-gauge]` 셀렉터를 그대로 써서 수정 없이 재사용됨.
  - 헤더 상단 내비게이션을 `index.html`과 동일하게 동기화: 로고를 "R/ Recode Coding"으로, 메뉴를 원장님의 낙서/wonoj(`oj.recode.ai.kr` 연결)/LMS·business·studio(준비중 배지)로, CTA를 구글 폼 직결로 교체. 기존엔 없던 모바일 메뉴(햄버거 버튼 + `assets/js/mobile-menu.js`)도 추가해 index.html과 동일 패턴으로 맞춤. Agent AI 5번째 카테고리 추가는 이번 범위에 포함하지 않음(미정, 필요시 별도 진행).
  - 클로드 인 크롬으로 로컬 서버 확인: 게이지가 왼쪽부터 점진적으로 채워지는 애니메이션과 6초 주기 자동 전환 정상 동작, 콘솔 에러 없음.
- `mockup.html` 색 톤을 `graffiti.html`(=index.html)과 맞추는 작업, 다크/라이트 두 옵션 중 "라이트 배경까지 전체 전환"으로 사용자 선택(일단 보고 최종 판단 예정) → 전체 파일 재작성:
  - Tailwind `brand` 팔레트를 인디고에서 `graffiti.html`/`index.html`과 동일한 에메랄드 그린으로 교체. 페이지 배경을 `#0c0c0f`(다크) → `#FBF7EE`(라이트 크림)로 전환, 본문 텍스트를 슬레이트 계열 밝은 톤으로 재조정.
  - 헤더/모바일 메뉴는 다크 유지(graffiti.html과 동일 패턴), 서브바·코스 카드·사이드 리스트·철학 컴팩트 리스트·푸터는 라이트 카드로 전환. 히어로 배너·철학 프로모 타일·CTA 대각선 배너는 의도적으로 다크 액센트 패널로 유지(라이트 페이지 위 스포트라이트 역할).
  - 5개 카테고리 사이드 리스트에 요청하신 대로 "Agent AI"를 5번째로 추가(보라 계열), 기존 4개는 각 색상 패밀리를 브랜드와 어울리게 재배정(Language=브랜드그린/Algorithm=틸/Web & WebApp=스카이블루/Unity=앰버). 04번 카테고리명도 "Unity & Unreal" → "Unity"로 단순화(needs.md 표기에 맞춤).
  - 새 "05 · Agent AI" 섹션(코스 카드 4개, 히어로 슬라이드, 사이드 게이지, 푸터 링크)을 다른 카테고리와 동일 패턴으로 추가.
  - 클로드 인 크롬으로 전체 스크롤 검증(히어로/4개 코스 섹션/신규 Agent AI 섹션/철학/CTA/푸터) + Agent AI 항목 클릭 전환 확인, 콘솔 에러 없음.
  - `PRD.md` §8 디자인 톤 미결 사항을 진행 상태로 갱신(최종 확정 아님, 사용자 검토 대기).
- `tasks/task-layer-01.md` 3개 항목 반영해 `mockup.html` 히어로 레이어 개선 (상세: `report/2026-08-21-mockup-hero-layer-01.md`):
  1. 우측 5개 카테고리 버튼을 `flex-1`로 균등 분배해 높이를 동일하게 맞추고, 그리드 stretch로 좌측 배너 높이와 자동 일치.
  2. 게이지 완성 → 다음 슬라이드 전환 시 배너 텍스트가 좌측으로 슬라이드 아웃 후 우측에서 슬라이드 인하는 애니메이션 추가(Epic Games Store 벤치마크 참고).
  3. 게이지/자동 전환 주기를 6초 → 3초로 단축.
  - 클로드 인 크롬으로 로컬 서버 확인: 버튼 높이 균등화, 클릭 시 전환 애니메이션 동작, 콘솔 에러 없음 확인.
- 히어로 슬라이드 전환 후속 반영: 텍스트만 슬라이딩되던 것을 배경 블롭(`hero-blob-1/2`)까지 포함해 하나의 장면(`#hero-scene`)으로 묶어 함께 슬라이딩/페이드되도록 확장. `hero-banner`(둥근 사각 틀)는 고정 창 역할만 유지. 클로드 인 크롬으로 전환 중간 프레임 확인, 콘솔 에러 없음.

## 2026-08-22
- origin 전체 브랜치(`main`, `dev`)를 fetch하고, 로컬에 없던 `dev` 브랜치를 `origin/dev` 추적 브랜치로 생성해 동기화. 이후 `dev`로 체크아웃.
- `tasks/task-layer-01.md` 체크박스를 실제 반영 상태(v0.15 커밋에서 이미 완료)에 맞춰 `[x]`로 갱신.
- `tasks/task-layer-02.md` 반영해 `mockup.html` Language(01) 섹션을 Swiper 캐러셀에서 정적 그리드로 교체(상세: `report/2026-08-22-mockup-language-layer-02.md`):
  - 카드 구성을 C/C++, Java, Python, JavaScript, Dart 5개로 확정(Go/Rust 카드 제거, Dart/Flutter 카드 신규 작성).
  - 좌우 이동 화살표·Swiper 마크업 제거, `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5`로 5개 카드가 동일 크기·전체 너비로 배치되도록 변경. 다른 카테고리 섹션은 Swiper 유지.
  - 클로드 인 크롬으로 로컬 서버 확인: 카드 순서/동일 크기 배치, 화살표 미노출, 콘솔 에러 없음.
- 사용자가 Language 5개 카드를 직접 수정(뱃지 제거, 라벨 전체 표기)한 것을 클로드 인 크롬으로 확인 — 마크업/콘솔 정상.
- Language 5개 카드의 커버아트 텍스트 라벨을 언어별 공식 로고 스타일 인라인 SVG 아이콘(C++/Java/Python/JavaScript/Dart)으로 교체, 흰색 아이콘 플레이트 위에 배치(상세: `report/2026-08-22-mockup-language-layer-02.md`). Dart 아이콘은 1차 시안이 너무 얇아 facet 좌표를 재조정. 클로드 인 크롬으로 확대 검수, 콘솔 에러 없음.
- 손그림 SVG 대신 실제 공식 로고 리소스를 웹에서 찾아 `assets/image/`(신규 폴더)에 PNG로 다운로드: C++/Java/Python/JavaScript는 `abranhe/programming-languages-logos`(MIT), Dart는 Wikimedia Commons(Google 공식, CC BY 4.0). 이후 사용자 요청으로 Java/Python/Dart는 "언어명이 함께 구성된" 워드마크 버전(devicon MIT, `*-original-wordmark.svg`)을 wsrv.nl로 PNG 변환해 재다운로드, Pillow로 투명 배경 검증 후 교체.
- `mockup.html` Language 5개 카드의 인라인 SVG 아이콘을 `assets/image/*.png` 실사 로고 이미지로 최종 교체. 흰색 플레이트를 고정 정사각형 대신 이미지 비율에 맞는 패딩 기반으로 변경. 클로드 인 크롬으로 로컬 서버 재확인, 콘솔 에러 없음.
- 로고 이미지의 흰색 플레이트를 제거하고 그린 그라디언트 배경 위에 직접 배치(`drop-shadow-lg`만 유지), 이미지 크기도 전반적으로 확대(cpp/js h-14→h-24, java/python h-20→h-28, dart h-9→h-14). 5개 파일 모두 투명 배경이라 별도 편집 없이 바로 적용, 클로드 인 크롬으로 재확인·콘솔 에러 없음.
- Language 카드 커버아트 그라디언트가 너무 어두워(brand-300→800) 로고 일부가 묻힌다는 피드백 → 브랜드 팔레트 내 밝은 조합(brand-100→400, `#d5f9e4`→`#3ecf8e`)으로 재설정(상세: `report/2026-08-22-mockup-language-layer-02.md`). 다른 카테고리 섹션 색상은 그대로. 클로드 인 크롬으로 재확인, 콘솔 에러 없음.
- Language 5개 카드 소개 문구를 언어별 고유 특징·학습적 의미 중심으로 재작성(C/C++ 메모리 직접 관리, Java 강타입·대규모 구조, Python AI 시대 표준어, JavaScript 브라우저 유일성·비동기, Dart 하나의 코드로 여러 플랫폼). 클로드 인 크롬으로 렌더링 확인, 콘솔 에러 없음.
- `v0.16` 커밋(mockup.html Language 레이어 + 실사 로고) `dev` 브랜치에 push 완료.
- 신규 `ARCHITECTURE.md` 작성: 메인 사이트(정적 GitHub Pages + Firebase 낙서장 백엔드) + 서브도메인 4개(oj/lms/business/studio, 별도 프로젝트) 구조를 mermaid 다이어그램과 함께 정리. 앞으로 아키텍처가 바뀔 때마다 계속 갱신하기로 함 — `CLAUDE.md`의 "작업 완료 후 문서 점검" 대상에 이 문서를 추가(4개→5개).
- `v0.17` 커밋(ARCHITECTURE.md 신설) `dev` 브랜치에 push 완료.
- Language 카드 클릭 시 이동할 언어별 코스 상세 페이지 신설 착수. 첫 번째로 `lang-cp.html`(C/C++)을 제작 — 벤치마크 대상은 korea-pass.kr 공지사항 목록 페이지(`/notice/noticeList.do`, 클로드 인 크롬으로 실제 접속해 레이아웃 확인: 브레드크럼+타이틀, 검색창, 분류 탭, 뱃지+메타+제목으로 구성된 카드 리스트, "더보기" 버튼).
  - 헤더/푸터는 `mockup.html` 라이트 테마를 그대로 재사용하고, 벤치마크 레이아웃은 "커리큘럼 목록" 섹션에 적용: 검색창(제목 부분일치 필터) + 분류 탭(전체/기초/실습/프로젝트) + 카드 리스트(뱃지+주차+제목) + "더보기"(6개 → 전체 12개, 클릭 시 펼치고 버튼 숨김) 형태로 순수 JS(프레임워크 없이) 구현.
  - C/C++ 12주차 분량의 커리큘럼 항목을 신규 작성(포인터·동적 메모리·구조체·STL·스마트 포인터·미니 프로젝트 등).
  - `mockup.html`의 C/C++ 카드(`<div class="course-card">`)를 `<a href="lang-cp.html">`로 교체해 실제 연결(다른 4개 언어 카드는 아직 페이지가 없어 그대로 둠 — 후속 작업 예정).
  - 클로드 인 크롬으로 로컬 서버 확인: mockup.html에서 C/C++ 카드 클릭 → lang-cp.html 정상 이동, 분류 탭 필터링·검색·더보기 모두 정상 동작, 콘솔 에러 없음.
- 사용자 요청으로 "모든 html 페이지는 src 폴더로 이동" 진행. GitHub Pages branch 배포가 `/ (root)` 또는 `/docs`만 지원해(임의 폴더 불가) 그대로 옮기면 배포가 깨진다는 점을 먼저 안내 → 사용자가 "index.html만 루트에 남기고 나머지는 이동"으로 절충안 확정.
  - `graffiti.html`/`graffiti-detail.html`/`graffiti-new.html`/`mockup.html`을 `git mv`로, 아직 커밋 전이던 `lang-cp.html`은 `mv`로 `src/`로 이동. `index.html`만 루트 유지.
  - 이동한 5개 파일의 상대 경로 전수 수정: `assets/...` → `../assets/...`(이미지·`mobile-menu.js`), `href="index.html"`(앵커 포함) → `href="../index.html"`, `graffiti*.html`/`firebase-client.js`/`admin-auth.js` ES module import(`from './assets/js/...'`) → `from '../assets/js/...'`. `src/` 안 페이지끼리(graffiti↔graffiti-new↔graffiti-detail, mockup↔lang-cp)의 상호 링크는 같은 디렉터리라 그대로 둠. `assets/js/*.js` 파일끼리의 상호 import는 위치가 안 바뀌어 손대지 않음.
  - `index.html`의 `graffiti.html` 링크 2곳만 `src/graffiti.html`로 수정.
  - 클로드 인 크롬으로 전수 검증: index.html→src/graffiti.html(원장님의 낙서 nav 링크)→글 상세(`graffiti-detail.html?id=1`, Firestore 정상 로드)→로고 클릭으로 `../index.html` 복귀, 그리고 index.html→src/mockup.html(로고 이미지 `../assets/image/*.png` 정상 표시)→C/C++ 카드 클릭→src/lang-cp.html까지 전 구간 이동 확인, 매 단계 콘솔 에러 없음.
  - `CLAUDE.md`(개요/개발 명령/배포/페이지 구조), `ARCHITECTURE.md`(mermaid 다이어그램에 `src/lang-cp.html` 노드 추가, §2/§3 경로 갱신), `DEV_PLAN.md`(§1 기술 스택에 파일 구조 결정 기록, Phase 2.5 경로 갱신)에 반영.
- 사용자 지시로 `src/mockup.html` 내용을 `index.html`로 전면 이전하고 `mockup.html`은 삭제(`PRD.md` §8의 "디자인 톤 확정" 미결 사항에 대한 최종 결정, 상세: `report/2026-08-22-index-adopt-mockup.md`).
  - 기존 `index.html`(다크 톤, 문제의식/훈련프로세스(`#process`)/대상(`#who`)/CTA 섹션)을 `mockup.html`의 라이트 톤 코스 카탈로그(히어로 자동전환 배너 + Language/Algorithm/Web & WebApp/Unity/Agent AI 5섹션 + 핵심철학 + CTA)로 완전 교체 — 두 파일 모두 자체 철학/CTA 섹션이 있어 병합 대신 전면 교체를 택함. 기존 문제의식/훈련프로세스/대상 콘텐츠는 이 교체로 사라짐(git 이력에서 복구 가능).
  - mockup 특유의 "검토용" 표식 제거: MOCKUP 안내 배너, `<meta name="robots" content="noindex,nofollow">`, 하단 "도구 검토 노트" 섹션 삭제. `<title>`/푸터 최하단 문구는 기존 index.html의 실서비스용 문구로 복원.
  - `src/`→루트로 위치가 바뀌며 상대경로 반전: `../assets/...` → `assets/...`, 로고 자기참조 링크 `../index.html` → `#`, `graffiti.html`/`lang-cp.html` 링크 → `src/graffiti.html`/`src/lang-cp.html`. 헤더 `sticky top-[26px]`→`top-0`, 서브바 `top-[90px]`→`top-16`(배너 제거로 오프셋 불필요).
  - 연쇄 링크 수정: `src/lang-cp.html`의 `mockup.html#lang` 등 8곳 → `../index.html#lang` 등으로 치환. `src/graffiti*.html` 3개 파일 헤더 nav가 옛 `index.html`의 `#process`/`#who`(새 index.html엔 없음)를 가리키던 걸 발견 — "훈련 과정"(`#process`)은 "훈련 코스"(`#lang`)로 교체, "대상"(`#who`)은 대응 섹션이 없어 삭제.
  - 클로드 인 크롬으로 전수 검증: index.html(배너 없음, 로고 정상)→C/C++ 카드→lang-cp.html→"훈련 코스" 링크로 index.html#lang 복귀(히어로 Language 탭 활성화 확인)→graffiti.html 직접 접속(nav "철학"/"훈련 코스" 정상, Firestore 로드 정상), 매 단계 콘솔 에러 없음.
  - `PRD.md`(§1/§3/§8 — 디자인 톤 확정을 "완료"로), `DEV_PLAN.md`(§1 GSAP 미도입 확정, Phase 1 완료 표시, Phase 2.5/브랜치전략 문구 갱신), `CLAUDE.md`(페이지 구조 — index.html/graffiti 설명 갱신), `ARCHITECTURE.md`(mermaid에서 Mockup 노드 제거, §2/§3 갱신)에 반영.
- 사용자 지시로 "CLAUDE.md를 제외한 major 4개 문서"(`PRD.md`/`DEV_PLAN.md`/`DEVLOG.md`/`ARCHITECTURE.md`)를 신규 `doc/` 폴더로 이동(`git mv`). `CLAUDE.md`는 Claude Code가 저장소 루트에서 자동으로 읽는 파일이라 루트 유지.
  - `CLAUDE.md`의 "저장소 관례"(문서 점검 규칙 등)에서 4개 문서 경로를 `doc/PRD.md`처럼 `doc/` 접두사로 갱신, `doc/` 폴더 자체를 설명하는 문장 추가.
  - `doc/ARCHITECTURE.md`·`doc/DEV_PLAN.md`(현재 상태를 설명하는 "살아있는" 문서)의 `report/*.md`·`tasks/*.md`·`CLAUDE.md` 상대경로를 `doc/`가 한 단계 들어간 만큼 `../report/...`/`../tasks/...`/`../CLAUDE.md`로 갱신. `doc/DEVLOG.md`(날짜별 이력 기록)와 `report/*.md`(작성 시점에 고정된 보고서) 안의 과거 항목들은 저장소 관례대로 손대지 않음(기존에도 `report/2026-07-29-deployment-guide.md`의 dothome 도메인 언급처럼 과거 기록은 재작성하지 않는 패턴).
  - 이동 후 `doc/`·루트·`report/`·`tasks/`·`*.html` 전체를 grep해 4개 문서명에 대한 잔여 참조 없음 확인.
- `v0.18` 커밋(페이지 구조 재편 src/·doc/, lang-cp.html 신설, index.html 최종 확정) `dev` 브랜치에 push 완료.
- 사용자 지시로 상단 헤더를 페이지 이동과 무관하게 항상 동일한 GNB(전역 내비게이션)로 통일. 그동안 `index.html`/`src/lang-cp.html`/`src/graffiti*.html` 3개 헤더가 서로 다른 구조였음을 확인:
  - `index.html`: 전체 GNB(원장님의 낙서/wonoj/LMS·business·studio 준비중/1:1 상담 신청), `sticky top-0`, `max-w-[1440px]`.
  - `src/lang-cp.html`: "훈련 코스" 페이지 전용 링크가 섞여 있고 business/studio 항목이 빠진 축소판, `sticky top-0`.
  - `src/graffiti*.html` 3개: 아예 다른 옛 헤더(`fixed top-0`, `max-w-7xl`, nav는 "철학"/"훈련 코스" 2개뿐, "원장님의 낙서"·CTA는 보이지 않는 spacer `<span>`으로만 존재).
  - `index.html`의 헤더를 기준(canonical GNB)으로 삼아 나머지 4개 페이지(`src/graffiti.html`/`graffiti-detail.html`/`graffiti-new.html`/`lang-cp.html`) 헤더를 전부 동일한 마크업으로 교체 — 페이지별 커스텀 nav 링크는 제거. 경로만 위치에 맞게 조정(`src/` 페이지는 로고 `../index.html`, "원장님의 낙서" `graffiti.html`).
  - `graffiti*.html` 3개는 헤더가 `fixed`→`sticky`로 바뀌며 더 이상 필요 없어진 `<main class="pt-16">`의 `pt-16`도 함께 제거(안 지우면 sticky 헤더 아래 빈 여백이 이중으로 생김).
  - normalize한 헤더 텍스트를 diff해 5개 페이지 헤더가 (로고 링크·"원장님의 낙서" 링크의 상대경로 차이를 빼면) 완전히 동일한 마크업임을 스크립트로 확인.
  - 클로드 인 크롬으로 확인: `graffiti.html` 접속 시 통일된 GNB 정상 렌더(이전엔 없던 wonoj/LMS/business/studio 노출), `lang-cp.html`에서 "원장님의 낙서" 클릭 → `graffiti.html` 정상 이동, 콘솔 에러 없음. `pt-16` 제거로 헤더-본문 사이 이중 여백 없음도 확인.
  - `CLAUDE.md`(페이지 구조에 "GNB" 항목 신설), `doc/ARCHITECTURE.md`(§3 표·비고에 GNB 통일 반영), `doc/DEV_PLAN.md`(Phase 2에 헤더 선반영 완료 기록)에 반영.
- 사용자 지시로 `index.html` GNB 바로 아래 있던 서브바("코스 검색" 입력 + 탐색/커리큘럼/후기 탭, `sticky top-16`)를 완전히 제거. JS에서 참조하는 id/셀렉터가 없어 마크업만 삭제하면 되는 구조였음. 클로드 인 크롬으로 확인: GNB 바로 아래 히어로가 이어지고 여백 이상 없음, 콘솔 에러 없음.
- 사용자 지시로 `src/lang-cp.html`을 2가지 정리:
  - 브레드크럼 "홈 / 훈련 코스 / C/C++"에서 "훈련 코스"(`../index.html#lang`) 항목 제거 → "홈 / C/C++"로 축약.
  - "커리큘럼 검색어 입력" 검색창(및 svg 아이콘) 컴포넌트 삭제. JS(`applyView`)에서 `searchInput`/`keyword` 관련 로직 전부 제거하고 분류 탭(전체/기초/실습/프로젝트) 필터링만 남김 — 검색이 유일하게 "0건" 결과를 만들 수 있던 경로였으므로, 더 이상 도달 불가능해진 "검색 조건에 맞는 커리큘럼이 없습니다" 빈 상태 문구(`#curriculum-empty`)도 마크업·JS 양쪽에서 함께 제거.
  - 클로드 인 크롬으로 확인: 브레드크럼 축약 정상, 검색창 사라짐, 분류 탭 클릭(예: "프로젝트" → 2건) 정상 동작, 콘솔 에러 없음.
  - `CLAUDE.md`의 `lang-cp.html` 설명 문구 갱신(검색창 제거, 브레드크럼 축약 반영).
- 사용자 지시로 `src/lang-cp.html`을 추가로 2가지 정리:
  - 타이틀 위 라벨을 "01 · LANGUAGE" → "LANGUAGE"로 변경(번호 제거).
  - 분류 탭(전체/기초/실습/프로젝트, 카드를 카테고리별로 필터링하던 버튼)을 언어 바로가기 탭(C/C++/Java/Python/JavaScript/Dart, 각각 `lang-cp.html`/`lang-jv.html`/`lang-py.html`/`lang-js.html`/`lang-dart.html`로 이동하는 `<a>` 링크)으로 교체 — `<button data-filter-tab>` → `<nav><a href="...">`, 현재 페이지(C/C++)는 `aria-current="page"` + `is-active` 스타일 유지. `role="tablist"`는 실제 탭이 아니라 페이지 이동 링크가 됐으므로 제거.
  - JS도 함께 정리: `tabs`/`activeCategory` 관련 로직 전부 삭제, `applyView()`는 "처음 6개만 보이고 더보기로 전체 노출"만 담당하도록 단순화. `data-category` 속성은 카드에 남아 있지만(정보성 뱃지 텍스트와 별개 메타데이터) 더 이상 JS에서 참조하지 않음.
  - `lang-jv.html`/`lang-py.html`/`lang-js.html`/`lang-dart.html` 4개 파일은 아직 존재하지 않아 해당 탭 클릭 시 404 — Java/Python/JavaScript/Dart 페이지를 이 순서·파일명대로 제작하는 게 다음 자연스러운 후속 작업.
  - 클로드 인 크롬으로 확인: "LANGUAGE" 라벨 정상, 5개 언어 탭 렌더(C/C++만 active), "더보기" 클릭 시 7~12주차 정상 노출, 콘솔 에러 없음.
  - `CLAUDE.md`(lang-cp.html 설명에 언어 바로가기 탭·파일명 규칙 추가), `doc/PRD.md`(§8에 확정된 파일명 4개 기록)에 반영.
- 사용자 지시로 (1) `src/lang-cp.html`의 "더보기" 버튼 완전 제거(12개 카드 처음부터 전부 노출, `hidden` 클래스 제거 + 관련 JS 삭제) (2) `lang-cp.html`을 템플릿으로 `src/lang-jv.html`(Java)/`src/lang-py.html`(Python)/`src/lang-js.html`(JavaScript)/`src/lang-dt.html`(Dart) 4개 신규 제작 (3) 관련 링크 전부 연결.
  - 언어 바로가기 탭의 Dart href를 이전 세션에서 잘못 썼던 `lang-dart.html`에서 사용자가 이번에 지정한 실제 파일명 `lang-dt.html`로 5개 페이지 전부 수정.
  - 각 언어 페이지는 헤더/푸터/레이아웃 구조를 `lang-cp.html`과 동일하게 유지하고: 로고·타이틀·인트로 문단(각 언어 특징 반영, index.html 카드 설명 확장), 언어 바로가기 탭(현재 페이지만 `is-active`+`aria-current`), 12주차 커리큘럼(기초 5·실습 5·프로젝트 2, 언어별 실제 기술 스택 반영 — Java는 Spring/컬렉션/멀티스레딩, Python은 NumPy/Pandas/AI API, JavaScript는 비동기/DOM/React, Dart는 Widget/State/Provider), CTA 문구만 언어별로 교체.
  - 작은 로고 배지(cover-art h-16 w-16 박스) 이미지 크기를 언어별로 조정: java/python은 `h-10 w-auto`(정사각형 1000×1000 원본이라 cpp.png와 동일 처리), javascript는 `h-10 w-auto rounded-lg`(기존 index.html 카드와 동일하게 라운드 처리), dart는 `w-12 h-auto`(원본이 1000×266 와이드 비율이라 height 고정 대신 width를 고정하고 height를 자동 계산해 박스 밖으로 넘치지 않게 함).
  - `index.html`의 Language 섹션에서 지금까지 링크가 없던 Java/Python/JavaScript/Dart 4개 카드(`<div class="course-card">`)를 `<a href="src/lang-jv.html">` 등으로 교체 — C/C++ 카드와 동일한 패턴.
  - 클로드 인 크롬으로 전수 검증: `lang-cp.html`(더보기 버튼 없이 12개 전부 노출)→"Java" 탭 클릭→`lang-jv.html` 정상 이동, Dart 로고 박스 오버플로우 없음(zoom으로 확인), `index.html`→Dart 카드 클릭→`lang-dt.html` 정상 이동, `lang-js.html` 12주차까지 스크롤 확인 — 매 단계 콘솔 에러 없음.
  - grep으로 5개 페이지의 언어 바로가기 탭 `href`가 서로 완전히 일치하는지 확인(`lang-dart.html` 잔여 참조 없음).
  - `CLAUDE.md`(5개 페이지 전부 존재·연결됨으로 갱신, "더보기" 제거 기록 추가), `doc/PRD.md`(§3/§8 — Language 코스 상세 페이지 완료로 갱신), `doc/DEV_PLAN.md`(Phase 2.5 완료 표시), `doc/ARCHITECTURE.md`(mermaid `LangCp` 노드를 5개 페이지로, §3 표·GNB 설명을 9개 페이지 기준으로 갱신)에 반영.
- 사용자 지시로 GNB를 "스크롤을 당길 때 움직이지 않도록" `fixed`로 전환. 헤더가 `sticky top-0`이면 일반적인 스크롤 중에는 화면에 고정돼 보이지만, 트랙패드/마우스 오버스크롤(러버밴드 바운스) 시 살짝 흔들리는 것처럼 보일 수 있어 완전히 뷰포트에 고정되는 `fixed`를 요청하신 것으로 판단.
  - 9개 페이지(`index.html`, `src/graffiti*.html` 3개, `src/lang-*.html` 5개) 전부의 헤더를 `class="sticky top-0 z-[60] ..."` → `class="fixed top-0 inset-x-0 z-[60] ..."`로 일괄 변경(sed).
  - `fixed`는 문서 흐름에서 완전히 빠지므로, 9개 페이지 전부의 `<main>`에 `pt-16`(헤더 높이 64px과 동일)을 추가해 본문이 헤더 뒤로 가려지지 않게 보정 — 이전에 이미 한 번(그래피티 페이지 fixed→sticky 전환 때) 걷어냈던 `pt-16`을 이번엔 반대 방향(sticky→fixed)으로 다시 전부 추가.
  - 클로드 인 크롬으로 확인: `index.html`/`lang-cp.html` 모두 헤더-본문 사이 이중 여백 없이 정상 렌더, 큰 폭으로 스크롤해도 헤더가 뷰포트 상단에 그대로 고정, 콘솔 에러 없음.
  - `CLAUDE.md`(GNB 항목에 `fixed`+`pt-16` 짝 규칙 명시), `doc/DEV_PLAN.md`(헤더 포지셔닝이 이날 fixed→sticky→fixed로 두 번 바뀐 이력 기록)에 반영.
- 사용자가 "페이지 이동할 때 GNB가 잠시 흔들린다"고 재보고 — `fixed`로 바꿔도 남아있던 문제로, 원인을 재진단.
  - Tailwind를 CDN 런타임 스크립트로 쓰는 구조라, 그 스크립트가 로드·실행되기 전까지는 헤더의 Tailwind 유틸리티 클래스(`fixed`, `bg-[#0c0c0f]/95`, `h-16` 등)가 전혀 적용되지 않는다는 게 근본 원인으로 파악됨 — 매 페이지 이동(전체 문서 리로드)마다 헤더가 잠깐 언스타일드 상태(콘텐츠가 세로로 쌓여 늘어남)로 보였다가 스크립트 실행 시점에 64px 높이 고정 바로 갑자기 접히는 것.
  - 9개 페이지 전부의 `<head>`에서 Tailwind CDN `<script>` 태그보다 **앞**에 순수 CSS `<style>` 블록을 추가(sed로 일괄 삽입): `header { position: fixed; top:0; left:0; right:0; z-index:60; height:4rem; overflow:hidden; background-color:rgba(12,12,15,.95); border-bottom:1px solid rgba(255,255,255,.1); }`와 `main { padding-top:4rem; }`. `<style>` 태그는 파싱되는 즉시 적용되고 네트워크 요청(스크립트 다운로드)을 기다리지 않으므로, Tailwind 스크립트가 아직 로드되지 않은 순간에도 헤더가 처음부터 올바른 위치·크기로 렌더링된다.
  - 클로드 인 크롬으로 9개 페이지 모두 정상 렌더·콘솔 에러 없음 확인(전환 순간의 FOUC 자체는 스크린샷 도구로는 캡처하기 어려운 서브 300ms 현상이라, 정적 렌더 결과와 삽입된 CSS의 타이밍 근거로 판단).
  - `CLAUDE.md`(GNB 항목에 Tailwind CDN FOUC 원인·critical CSS 대응 기록, 새 페이지 만들 때 헤더/`pt-16`/critical CSS 3종 세트를 함께 복사하라고 명시), `doc/DEV_PLAN.md`(§1 기술 스택에 Tailwind CDN의 FOUC 트레이드오프와 완화 방법 기록, 근본 해결엔 빌드 도구가 필요함을 명시).
- 사용자가 "여전히 흔들린다, 새로고침해도 동일하다"고 재보고 — 1차 critical CSS(헤더 박스 크기/위치만 고정)로는 부족했던 것으로 판단, 실제로 눈으로 확인 가능한 재현 방법을 마련해 재진단.
  - `index.html`을 스크래치 폴더에 복사하고 Tailwind CDN `<script>` 태그를 `setTimeout(..., 2~3초)`로 인위적으로 지연 로드하도록 패치한 테스트용 사본을 만들어(원본 저장소는 건드리지 않음, `assets`/`src`는 심볼릭 링크로 연결) 별도 포트(8766)에서 서빙 — 지연 구간 동안 헤더를 직접 스크린샷/zoom으로 관찰.
  - 발견: 헤더 박스 자체(위치/높이)는 1차 조치로 이미 안정적이었지만, 그 안의 nav 링크가 파란 밑줄(브라우저 기본 링크 스타일)로 보였다가 Tailwind 로드 후 회색·밑줄 없음으로 바뀌고, CTA 버튼이 맨텍스트 → 초록 알약 버튼으로, 로고 옆 사각 배지가 무늬 없음 → 초록 톤 사각형으로, 데스크톱 폭에서도 모바일 햄버거 아이콘이 잠깐 노출되는 4가지 잔여 깜빡임을 확인 — 이게 사용자가 계속 느낀 "흔들림"의 정체.
  - critical CSS `<style>` 블록에 4가지를 전부 추가해 Tailwind가 나중에 적용할 값과 동일하게 맞춤: `header a{color:#fff;text-decoration:none}` + `header nav a{color:#94a3b8}`(nav 링크 밑줄 제거·회색), `header nav + a{...}`(CTA를 처음부터 초록 알약 버튼으로), `header span.rounded-lg{...}`(로고 배지를 처음부터 초록 톤 사각형으로), `@media (min-width:768px){header #mobile-menu-btn{display:none}}`(Tailwind의 `md:hidden`과 동일한 768px 기준으로 데스크톱에서 모바일 버튼 숨김). 9개 페이지 전부에 동일 적용.
  - 같은 지연 테스트 사본으로 재검증: 지연 구간 동안 캡처한 헤더와 Tailwind 로드 완료 후 캡처한 헤더가 zoom 스크린샷 상 구분이 안 갈 정도로 동일함을 확인(멱등) — 테스트용 사본·심볼릭 링크·임시 서버는 검증 후 전부 삭제.
  - `CLAUDE.md`(GNB 항목에 1차/2차 대응 구분, 잔여 깜빡임 4종과 대응 규칙 상세 기록)에 반영.
- 사용자가 "GNB가 fixed인데 왜 아직도 흔들리냐"고 정확한 반문 — 지금까지의 수정이 실제로 콘텐츠 레벨 시프트를 없앴는지 스크린샷 비교가 아니라 브라우저의 객관적 측정 API로 재검증.
  - `PerformanceObserver({type:'layout-shift', buffered:true})`를 `<head>` 최상단(Tailwind 스크립트보다도 앞)에 심어 `sessionStorage`에 로그를 남기는 스크래치 사본을 만들고, `location.reload()`로 실제 새로고침을 실행 → 로그를 확인한 결과 **레이아웃 시프트 0건**(Cumulative Layout Shift 관점에서 요소가 실제로 움직인 적이 없음)을 확인. 리로드 직후 즉시 스크린샷을 찍어도 이미 완성된 상태만 캡처될 뿐, 전환 중간 프레임을 잡을 수 없었음(로컬 환경이 매우 빨라 전환 자체가 도구의 캡처 속도보다 빠름).
  - 결론: 지금까지 고친 "콘텐츠가 스타일 적용 전후로 다르게 보이는" 종류의 흔들림은 측정상 더 이상 없음. 남아있다면 레이아웃 시프트가 아니라, 정적 다중 페이지 사이트 구조상 새로고침/페이지 이동마다 브라우저가 이전 문서를 완전히 버리고 새 문서를 처음부터 그리는 **일반적인 MPA 전환 자체의 끊김**일 가능성이 높음 — 이건 CSS 포지셔닝으로 고칠 수 있는 대상이 아님.
  - 대응: CSS Cross-Document View Transitions(`@view-transition { navigation: auto; }`)를 9개 페이지 전부의 critical `<style>` 블록 맨 앞에 추가. 같은 오리진 내 페이지 전환/새로고침 시 이전 문서 → 새 문서를 브라우저가 자동으로 크로스페이드 처리해주는 표준 CSS 기능(빌드 도구 불필요, Chrome/Edge 126+ 지원, 미지원 브라우저에서는 그냥 무시되고 기존 동작 유지 — progressive enhancement). 테스트 브라우저(Chrome 151)에서 `'startViewTransition' in document`로 지원 확인.
  - 클로드 인 크롬으로 프로덕션 파일 재확인: 정상 렌더, 콘솔 에러 없음. 테스트에 썼던 `PerformanceObserver` 스크래치 사본·심볼릭 링크·임시 서버는 검증 후 삭제.
  - **사용자가 하드 리프레시로 재확인 후 "수정되었음" 최종 확인**(2026-08-22) — GNB 흔들림 이슈 해결 완료. 원인은 두 겹이었음: ① Tailwind CDN 런타임 로드 전 헤더 내부 요소(nav 링크 색상·밑줄, CTA 버튼 모양, 로고 배지, 모바일 버튼)가 언스타일드로 보이다 스냅되는 FOUC → critical CSS로 최종값과 동일하게 선반영해 해결. ② 그 외 남아있던 체감은 정적 다중 페이지 사이트의 새로고침/페이지 전환 자체가 갖는 "이전 문서를 통째로 버리고 새로 그리는" MPA 특유의 끊김 → `@view-transition { navigation: auto; }`로 브라우저 네이티브 크로스페이드를 적용해 해결.
- 사용자 지시로 `v0.20` 커밋(index.html 서브바 정리, Language 4개 언어 페이지 신설, GNB 흔들림 해결 등 이번 세션 전체) 생성·`dev`에 push 완료(커밋 `2623db0`).
- 사용자 지시로 `dev`(v0.20)를 `main`에 병합 — `index.html` 리뉴얼 기간 동안 걸어뒀던 "main 병합 보류" 방침(2026-08-21 확정)을 사용자의 명시적 지시로 해제하고 fast-forward 병합·push. `main` push로 GitHub Pages가 자동 재배포되어 `recode.ai.kr`에 v0.10~v0.20의 전체 리뉴얼(index.html 전면 개편, src//doc/ 구조 재편, lang-*.html 5개, GNB 통일 등)이 실제로 반영됨. 병합 후 로컬은 다시 `dev` 브랜치로 전환해 작업 재개 상태 유지.
  - `doc/DEV_PLAN.md`(§2 브랜치 전략 — 병합 보류 해제 기록), `doc/ARCHITECTURE.md`(§2 브랜치 전략 갱신, 최종 갱신일 메모를 병합 완료로 갱신)에 반영.
- 사용자가 `doc/` 폴더명을 `docs/`로 직접 변경(`git mv` 없이 파일시스템에서 이름만 변경 — git에는 4개 파일 삭제+미추적 신규 폴더로 인식됨). `CLAUDE.md`와 "살아있는" 문서(`ARCHITECTURE.md`)의 `doc/` 접두사 참조를 `docs/`로 갱신 — `DEVLOG.md`(이 문서) 안의 과거 항목들은 저장소 관례(과거 기록 재작성하지 않음)에 따라 `doc/`로 그대로 유지.
  - `CLAUDE.md`: "저장소 관례" 절의 `doc/` 접두사 설명·5개 문서 경로 전부 `docs/`로 치환.
  - `docs/ARCHITECTURE.md`: 1절 인트로 문장·§3 표의 `doc/DEV_PLAN.md` 참조 2곳을 `docs/`로 수정.
  - `docs/DEV_PLAN.md`/`docs/PRD.md`는 원래 상대경로(`../CLAUDE.md`, 파일명만)로 서로를 참조하고 있어 수정 불필요함을 grep으로 확인.
- 사용자 지시로 `lang-cp.html`(다른 4개 언어 페이지는 제외, 이 파일에만 적용)의 커리큘럼 카드 12개를 2단계로 개편:
  1. 뱃지(기초/실습/프로젝트)+N주차 표시 제거 → 제목 앞에 `1.`~`12.` 번호 부여(제목 폰트 크기는 유지), 제목 아래 작은 non-bold 설명(brief) 한 줄 신규 작성.
  2. 카드를 클릭하면 brief 아래로 더 자세한 detail 문단이 펼쳐지도록 아코디언 구현 — CSS Grid `grid-template-rows: 0fr`→`1fr` 트랜지션(`.curriculum-detail`, `<style>` 블록에 추가)으로 JS 없이 부드러운 높이 애니메이션을 구현하고, 셰브런 아이콘 회전도 같은 `data-expanded` 속성으로 연동. 카드는 `role="button" tabindex="0" aria-expanded`를 갖춰 클릭뿐 아니라 키보드(Enter/Space)로도 토글 가능, 파일 하단 인라인 `<script>`에서 `[data-curriculum-card]` 전체에 리스너를 붙임.
  - 12개 항목 전부에 brief(기존 설명 재사용)와 신규 detail 문장(2~3문장, 실습 내용을 구체적으로 서술)을 작성.
  - 클로드 인 크롬으로 로컬 서버(8765) 확인: 카드 클릭 시 부드럽게 확장되며 detail 텍스트 노출, 재클릭 시 정상 축소, 콘솔 에러 없음.
  - `CLAUDE.md`(`lang-cp.html` 항목에 새 카드 형식 설명 추가, 나머지 4개 언어 페이지는 아직 이전 뱃지+주차 형식이라 형식이 공존 중임을 명시)에 반영.
- 사용자 지시로 "항목에 focus가 풀리면 다시 원래 크기로" — 위 아코디언 카드가 클릭으로 펼쳐진 뒤 계속 열려있지 않고, 포커스를 잃으면(다른 카드 클릭·빈 영역 클릭·Tab 이동 등 `blur` 이벤트) 자동으로 접히도록 동작 추가. 카드마다 `blur` 리스너를 붙여 `data-expanded`/`aria-expanded`를 무조건 `false`로 리셋 — 결과적으로 한 번에 하나의 카드만 펼쳐진 상태를 유지.
  - 클로드 인 크롬으로 확인: 카드 A를 펼친 뒤 카드 B를 클릭하면 A가 자동으로 접히고 B만 펼쳐짐, 빈 영역 클릭 시에도 펼쳐진 카드가 접힘. 콘솔 에러 없음.
  - `CLAUDE.md`(커리큘럼 카드 형식 설명에 blur 시 자동 축소 동작 추가)에 반영.
- 사용자 지시로 `lang-cp.html`의 아코디언 커리큘럼 카드 형식(번호+brief+클릭 시 펼쳐지는 detail, blur 시 자동 축소)을 나머지 4개 언어 페이지(`lang-jv.html`/`lang-py.html`/`lang-js.html`/`lang-dt.html`)에도 동일 적용 — 이제 5개 언어 페이지 전부 동일한 카드 형식을 쓴다.
  - 반복 작업량이 커서(4개 파일 × 12개 카드) 스크래치 폴더에 Python 스크립트(`apply_accordion.py`)를 작성해 일괄 처리: 정규식으로 기존 뱃지+주차+제목 카드 블록을 파싱하고(제목까지 추출해 미리 작성해둔 brief/detail 텍스트와 매칭 검증), 새 카드 마크업(번호·brief·detail·셰브런·`curriculum-detail`)으로 치환. `<style>`에 `.curriculum-detail`/`.curriculum-chevron` 규칙을, 파일 하단에 클릭/키보드/blur 토글 스크립트를 삽입.
  - Java/Python/JavaScript/Dart 각 12개 항목의 brief(기존 한 줄 설명 재사용)와 detail(2~3문장, 언어별 실제 문법·개념을 구체적으로 서술 — 예: Java는 JVM·강타입·Spring 계층 구조, Python은 동적 타이핑·덕 타이핑·Pandas, JavaScript는 이벤트 루프·클로저·React 상태 흐름, Dart는 null 안정성·Widget 트리·Provider) 전부 신규 작성.
  - 스크립트 실행 전 정규식 매칭 12건, 제목 일치 여부를 파일마다 검증(불일치 시 즉시 중단하도록 assert)해 오치환 위험을 없앰 — 4개 파일 전부 12/12 매칭·치환 성공.
  - 클로드 인 크롬으로 4개 페이지 전수 확인: Dart 페이지에서 카드 클릭 시 부드럽게 확장·detail 노출, JavaScript·Python 페이지 스크롤로 12번 카드까지 정상 렌더(CTA 섹션 포함), 콘솔 에러 없음.
  - `CLAUDE.md`(커리큘럼 카드 형식 설명을 "`lang-cp.html`에만 적용"에서 "5개 페이지 전부 동일 형식"으로 갱신, 새 언어 페이지 추가 시 이 5개 파일 중 하나를 템플릿으로 삼으라고 명시)에 반영.
- 사용자 지시로 `index.html`의 "01 · LANGUAGE" 같은 번호 접두사 라벨을 전부 제거 — 히어로 eyebrow(`#hero-eyebrow`)와 5개 섹션 헤더 `<p>` 6곳, 히어로 자동 전환에 쓰이는 JS `SLIDES[].eyebrow` 배열 5곳까지 총 11곳을 sed로 일괄 치환("LANGUAGE"/"ALGORITHM"/"WEB & WEBAPP"/"UNITY"/"AGENT AI"만 남김).
  - 사용자에게 범위를 먼저 확인: 사이드 게이지 리스트(`#hero-side-list`)의 컬러 사각 아이콘 안 숫자(`01`~`05`)도 함께 지울지 물었고, "텍스트 라벨만 제거"로 확정 — 아이콘 배지 숫자는 손대지 않음.
  - 클로드 인 크롬으로 확인: 히어로 라벨 "LANGUAGE"로, 사이드 리스트 아이콘은 `01`~`05` 그대로, 스크롤해 "UNITY" 섹션 헤더도 번호 없이 정상 렌더. 콘솔 에러 없음.
  - `CLAUDE.md`(`index.html` 항목에 라벨 제거 범위와, 사이드 리스트 아이콘 숫자는 별개로 유지된다는 점 명시)에 반영.
- 사용자 지시로 GNB 로고 바로 오른쪽(nav 첫 항목)에 "회사 소개" 메뉴를 "준비중"으로 신규 추가 — 별도 소개 페이지를 제작할 예정이라 우선 LMS/business/studio와 동일한 "준비중" 뱃지 + `aria-disabled="true"` 비활성 링크 패턴으로 노출.
  - 9개 페이지(`index.html`, `src/graffiti*.html` 3개, `src/lang-*.html` 5개) 전부의 데스크톱 nav·모바일 메뉴 nav(총 18곳)에 동일하게 반영 — 스크립트로 "원장님의 낙서" 링크 바로 앞에 일괄 삽입 후, 9개 파일의 `<header>...</header>` 블록이 서로 완전히 동일한지(로고·"원장님의 낙서" 링크의 상대경로 차이만 존재) diff로 재검증.
  - 클로드 인 크롬으로 `index.html` 확인: 로고 오른쪽에 "회사 소개 준비중"이 정상 렌더, 클릭 무반응(비활성 링크), 콘솔 에러 없음.
  - `CLAUDE.md`(GNB 항목에 "회사 소개" 추가 및 데스크톱/모바일 양쪽 다 챙겨야 한다는 점 명시), `docs/PRD.md`(§8 미결 사항에 소개 페이지 제작 예정 항목 추가)에 반영.
- 사용자가 "아니.. '회사 소개' 메뉴는 좌측에 배치"라고 재지시 — 직전에 nav 첫 항목(우측 그룹)으로 넣었던 걸, 로고와 시각적으로 붙어 보이도록 좌측으로 재배치.
  - 헤더 최상단 flex 컨테이너가 `justify-between`이라 로고와 우측 nav+CTA 그룹 사이에 큰 간격이 있어, nav 첫 항목으로 넣어도 로고와 붙어 보이지 않았던 게 원인. 로고 `<a>`와 "회사 소개" `<a>`를 새 `<div class="flex items-center gap-6">`로 함께 묶어 헤더 좌측 그룹으로 이동, 데스크톱 전용(`hidden md:inline-flex`)으로 처리 — 모바일에서는 숨기고 기존처럼 모바일 메뉴 nav의 첫 항목으로만 노출(변경 없음). 우측 데스크톱 nav에서는 "회사 소개" 제거.
  - 부수 이슈: "회사 소개"가 더 이상 `<nav>` 안에 있지 않아 critical CSS `header nav a{color:#94a3b8}`가 적용되지 않게 됨 — Tailwind 로드 전 잠깐 흰색으로 보이는 FOUC 재발을 막기 위해 `header a[aria-disabled="true"]{color:#64748b}` 규칙을 9개 페이지 critical CSS에 추가(LMS/business/studio 뱃지에도 부수적으로 적용되어 사전 로드 색상이 실제 Tailwind 클래스와 더 정확히 일치하게 됨).
  - 9개 페이지 전부 정규식 스크립트로 일괄 처리 후, `<header>...</header>` 블록이 서로 완전히 동일한지(로고·"원장님의 낙서" 상대경로 차이만) diff로 재검증.
  - 클로드 인 크롬으로 `index.html`·`lang-cp.html` 확인: "회사 소개 준비중"이 로고 바로 옆에 붙어 렌더링, 우측 nav는 그대로, 콘솔 에러 없음.
  - `CLAUDE.md`(GNB 항목을 좌측 배치·critical CSS 보완 내용으로 갱신)에 반영.
- 사용자가 Algorithm 코스를 Language처럼 다뤄보고 싶다고 요청 — 대화 중 12주 커리큘럼(기초5·실습5·프로젝트2) 초안을 먼저 제안하고, "visualgo.net 형태로 시각화 가능한가?" 질문에 기술적으로는 가능(빌드 도구 없이 SVG/Canvas+Vanilla JS)하나 작업량이 크므로 파일럿부터 권장한다고 답함. 사용자 동의 후 파일럿 후보 선정을 위한 전체 알고리즘 리스트업과 보고서를 요청받아 작성.
  - `https://visualgo.net`을 WebFetch로 직접 확인해 벤치마크 근거로 삼음(총 24개 모듈, 4단계 난이도) — Recode Coding 목적(코딩테스트 대비+구조적 사고)에 맞게 선별한 리스트를 12주 커리큘럼에 대응시켜 학습 포인트·시각화 아이디어·구현 난이도까지 정리.
  - 파일럿 우선순위 제안: 정렬 5종(막대그래프 엔진 공유)·BFS/DFS(노드-엣지)·이진탐색(범위 하이라이트) 3개 엔진으로 8개 알고리즘 커버 — DP는 학습 임팩트는 크지만 문제마다 테이블 형태가 달라 파일럿 이후로 별도 검토 권장.
  - 코드 변경 없는 기획 문서라 `report/2026-08-22-algorithm-visualization-listup.md`에만 작성, 아직 구현 착수 전이라 `CLAUDE.md`/`docs/PRD.md`/`docs/DEV_PLAN.md`는 갱신하지 않음(사용자가 방향을 확정하면 그때 반영).
- 사용자 지시로 "정렬 5종에 대해서만, Language 때와 같은 방식으로 우선 구성" — 위 보고서의 파일럿 1순위(정렬)를 실제 페이지로 제작. 시각화 엔진은 아직 없고, `lang-cp.html`과 동일한 아코디언 커리큘럼 카드 형식으로 텍스트 콘텐츠만 먼저 구성하는 단계.
  - `src/algo-sort.html` 신규 생성 — `lang-cp.html`을 템플릿으로 GNB/critical CSS/아코디언 카드 스크립트를 그대로 복사하되, 12주가 아니라 버블/선택/삽입/병합/퀵 정렬 5개 카드만 구성(brief+detail 전부 신규 작성, 시간복잡도와 학습 포인트 중심).
  - 브레드크럼은 Language 페이지(2단: 홈/C/C++)와 달리 `홈 / Algorithm / 정렬` 3단으로 구성 — Algorithm은 정렬 외에도 하위 카테고리(그래프탐색/이진탐색/DP 등)가 이어질 예정이라 상위 구분을 남겨두는 게 낫다고 판단. "Algorithm" 구간은 `index.html#algo`로 연결.
  - 아이콘은 언어별 로고 PNG가 없어 막대그래프 형태의 인라인 SVG를 새로 그려 `cover-art`(teal 그라디언트, index.html Algorithm 섹션과 동일 톤)에 배치.
  - 언어 바로가기 탭에 해당하는 카테고리 전환 탭은 아직 만들지 않음(정렬 외 카테고리가 아직 없어 탭이 무의미) — `index.html` Algorithm 섹션 기존 4개 카드도 "정렬"과 1:1 대응하지 않아 아직 연결하지 않음.
  - 클로드 인 크롬으로 확인: 브레드크럼·아이콘·5개 카드 정상 렌더, 카드 클릭 시 부드럽게 확장, CTA·푸터 정상, 콘솔 에러 없음.
  - `CLAUDE.md`(`src/algo-sort.html` 항목 신설, GNB 페이지 수를 9→10개로 갱신), `docs/ARCHITECTURE.md`(페이지 표·GNB 설명 10개 페이지로 갱신, 최종 갱신일 메모 교체), `docs/PRD.md`(§8에 Algorithm 코스 진행 상황과 남은 과제 기록)에 반영.
- 사용자가 "이런 이런.." 하며 방향을 정정 — 실제 의도는 `src/algo-sort.html` 같은 12주형 커리큘럼 상세 페이지가 아니라, **랜딩 페이지(`index.html`) Algorithm 섹션의 카드를 Language 섹션과 같은 방식(정적 그리드)으로 구성**하는 것이었음. 세부 상세 페이지는 나중에 visualgo.net처럼 시각화 형태로 별도 제작할 예정이라고 확인.
  - `index.html` Algorithm 섹션(`#algo`)을 기존 Swiper 캐러셀 4카드(자료구조 기초/알고리즘 문제 해결/코딩테스트 대비/시스템 설계 기초, 입문/중급/심화 뱃지)에서 Language 섹션과 동일한 정적 그리드(`grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-4`, 화살표 네비게이션 없이 5개 카드 전체 노출)로 교체, 내용도 버블/선택/삽입/병합/퀵 정렬 5개 알고리즘으로 대체.
  - 언어 로고 이미지가 없어 3글자 모노스페이스 약어(Bub/Sel/Ins/Mrg/Qck)를 cover-art에 배치. 상세 페이지가 아직 없어 카드는 Language의 `<a>`가 아니라 클릭 불가능한 `<div>`로 유지(과거 Java/Python 카드가 페이지 완성 전 `<div>`였던 것과 동일 패턴) — 완성되면 `<a>`로 전환 예정.
  - Swiper JS는 `.course-swiper` 셀렉터로 나머지 3개 섹션(Web & WebApp/Unity/Agent AI)에 계속 쓰여 라이브러리 자체는 그대로 두고, Algorithm 섹션 마크업만 이 패턴에서 제외.
  - `src/algo-sort.html`은 삭제하지 않고 남겨뒀지만 현재 어디서도 링크되지 않는 미사용 파일 — 재사용(시각화 페이지로 개조) 여부는 사용자와 추후 확인 필요.
  - 클로드 인 크롬으로 확인: Language와 동일한 정적 5카드 그리드 정상 렌더(AOS 페이드인 애니메이션 완료 후 확인), 나머지 3개 섹션(Web & WebApp 등)의 Swiper 캐러셀은 영향 없이 정상 동작, 콘솔 에러 없음.
  - `CLAUDE.md`(`index.html` Algorithm 섹션 개편 내용 추가, `algo-sort.html` 항목을 미사용 파일로 정정), `docs/ARCHITECTURE.md`(페이지 표·최종 갱신일 정정), `docs/PRD.md`(§8 Algorithm 항목을 랜딩 구성 완료로 정정), `docs/DEV_PLAN.md`(Phase 2.6 제목·내용을 "랜딩 구성"으로 정정, 시행착오 기록 추가)에 반영.
- 사용자 지시로 "버블 정렬에 대해서만, 최대한 visualgo.net의 시각적 시뮬레이션을 반영한 형태로" 상세 페이지 제작 — Algorithm 코스의 첫 실제 시각화 파일럿.
  - `src/algo-bubble-sort.html` 신규 생성. 배열을 막대그래프(높이=값)로 그리고, 정렬 과정을 미리 전부 계산해 "step" 배열(스냅샷·비교 인덱스·정렬 완료 경계·의사코드 라인·설명 문구·누적 비교/교환 횟수)로 기록한 뒤 재생/일시정지/이전·다음 단계/처음·끝 컨트롤이 이 배열의 인덱스만 이동시키며 다시 그리는 구조로 구현(visualgo.net과 동일한 "사전 계산 후 재생" 패턴) — 알고리즘 로직과 재생 UI가 분리되어 있어 다른 정렬 알고리즘에도 그대로 재사용 가능.
  - 색상 코딩(대기/비교 중/방금 교환됨/정렬 완료 4단계)과 범례, 의사코드 4줄 패널(현재 실행 줄을 브랜드 그린으로 하이라이트), 배열 프리셋 4종(무작위/정렬됨(최선)/역순(최악)/거의정렬) + 새로 섞기, 속도 3단(느리게/보통/빠르게) 구현.
  - 조기 종료 최적화(한 순회에서 교환이 0회면 즉시 중단)를 실제로 구현해 "정렬됨" 프리셋에서 O(n)(비교 9회·교환 0회, n=10)이 실제로 나오는 것을 증명 — 복잡도 표에서 주장하는 최선/평균/최악의 차이를 텍스트가 아니라 조작 가능한 시뮬레이션으로 직접 확인 가능.
  - `index.html` Algorithm 섹션의 "버블 정렬" 카드를 `<div>`에서 `<a href="src/algo-bubble-sort.html">`로 전환해 연결.
  - 클로드 인 크롬으로 전수 검증: 무작위 프리셋 재생 시 비교(amber)·교환(rose)·정렬완료(brand green) 색상 전환과 의사코드 하이라이트 정상 동작, "마지막으로" 버튼으로 즉시 완료 상태 이동(전체 오름차순 정렬 확인), "정렬됨" 프리셋에서 비교 9회·교환 0회로 조기 종료 확인, 랜딩 페이지 카드 클릭 시 정상 이동, 콘솔 에러 없음.
  - `CLAUDE.md`(`algo-bubble-sort.html` 항목 신설 — step 배열 구조·색상 코딩·프리셋·속도 조절 상세 기록, GNB 페이지 수 10→11개 갱신), `docs/ARCHITECTURE.md`(페이지 표·GNB·최종 갱신일 갱신), `docs/PRD.md`(§8에 시각화 1호 완료 기록), `docs/DEV_PLAN.md`(Phase 2.6에 시각화 1호 완료·다음 알고리즘 계획 추가)에 반영.
- 사용자 피드백 2건을 한 번에 반영 — "브레드크럼을 홈 > 버블 정렬로" + "같은 형태로 선택 정렬도 작성".
  - `algo-bubble-sort.html` 브레드크럼을 `홈 / Algorithm / 버블 정렬` 3단에서 `홈 / 버블 정렬` 2단으로 정정(Language 상세 페이지의 `홈 / C/C++`와 동일 패턴).
  - `src/algo-selection-sort.html` 신규 생성 — `algo-bubble-sort.html`을 템플릿으로 GNB/critical CSS/시각화 UI 골격을 그대로 재사용하되, 선택 정렬 알고리즘에 맞게 buildSteps·색상 코딩·의사코드를 새로 작성.
    - 선택 정렬은 버블 정렬과 달리 정렬된 구간이 **앞**에서부터 채워지므로(`sortedFrom` 대신 `sortedUntil` 개념 사용), 매 outer loop마다 "최솟값 후보(minIdx)"를 추적하는 5번째 색상(sky-400)을 추가하고, 의사코드도 6줄(`minIdx ← i`, `if array[j] < array[minIdx]`, `minIdx ← j`, `swap(array[i], array[minIdx])`)로 구성.
    - 선택 정렬은 버블 정렬의 조기 종료 최적화가 없다는 알고리즘적 사실을 그대로 구현(항상 남은 구간 전체를 스캔) — "정렬됨" 프리셋에서도 비교 횟수(45회, n=10)는 동일하고 교환 횟수만 0이 되는 것을 실제로 보여줘, "왜 선택 정렬은 최선의 경우도 O(n²)인가"를 버블 정렬과 대비해 체감하게 함.
    - `index.html` Algorithm 섹션의 "선택 정렬" 카드를 `<div>`에서 `<a href="src/algo-selection-sort.html">`로 전환.
  - 클로드 인 크롬으로 검증: 버블 정렬 페이지 브레드크럼이 `홈 / 버블 정렬`로 정상 축약 확인. 선택 정렬 페이지는 무작위 프리셋 재생 시 최솟값 후보(sky)·비교 중(amber)·정렬 완료(green) 색상 정상 전환, "마지막으로" 이동 시 비교 45회·교환 8회로 완전 정렬 확인, "정렬됨" 프리셋에서 비교 45회·교환 0회로 조기 종료 없음을 확인(버블 정렬과의 차이가 수치로 정확히 드러남), 랜딩 카드 연결·콘솔 에러 없음까지 확인.
  - `CLAUDE.md`(버블+선택 정렬 항목을 하나로 통합해 색상 코딩·프리셋 차이·브레드크럼 정정 이력 기록, GNB 페이지 수 11→12개 갱신), `docs/ARCHITECTURE.md`(페이지 표·GNB·최종 갱신일 갱신), `docs/PRD.md`(§8에 선택 정렬 완료·브레드크럼 통일 기록), `docs/DEV_PLAN.md`(Phase 2.6에 2호 완료 기록)에 반영.
- 사용자 지시로 "남은 3개의 정렬 페이지도 모두 작성해 줘" — 삽입·병합·퀵 정렬 시각화 상세 페이지를 마저 제작해 정렬 5종 전부 완료.
  - `src/algo-insertion-sort.html`: 버블·선택 정렬과 동일한 골격에, 인접 교환 방식(swap-based)으로 구현 — `while j>0 and array[j-1]>array[j]`의 자연스러운 즉시 실패 덕분에 별도 조기 종료 로직 없이도 "정렬됨" 프리셋에서 O(n)(비교 9회·교환 0회)이 재현됨. "삽입 대상"(sky-400) 색상으로 이번 순회에서 삽입할 값을 표시.
  - `src/algo-merge-sort.html`: 다른 정렬과 달리 정렬 진행 상태가 단일 경계값이 아니라 재귀적 `[lo,hi)` "병합 구간"(indigo-200 배경)이라, `range` 필드를 step에 추가하고 최상위(전체) 병합이 끝나야만 `sortedUntil=n`으로 전체 완료 처리되도록 구현. 통계 라벨도 "교환" 대신 "기록"(merge는 스왑이 아니라 덮어쓰기이므로)으로 변경. 의사코드는 재귀 분할(mergeSort)이 아니라 실제 스텝이 발생하는 merge 단계만 8줄로 구성.
  - `src/algo-quick-sort.html`: Lomuto partition으로 구현, 정렬 완료 상태를 단일 경계가 아니라 pivot이 확정될 때마다 하나씩 늘어나는 `fixed` 인덱스 배열(조각날 수 있음)로 관리. **의도적으로 마지막 원소를 pivot으로 고정**해, "정렬됨"·"역순" 두 프리셋 모두 O(n²) 최악의 경우가 되도록 설계하고 프리셋 라벨도 "정렬됨 (최악)"/"역순 (최악)"으로 명시 — pivot 선택 전략의 중요성을 프리셋 결과로 직접 증명. i===j·i+1===hi인 자기 자신과의 swap은 카운트에서 제외하도록 가드 처리.
  - `index.html` Algorithm 섹션의 "삽입 정렬"/"병합 정렬"/"퀵 정렬" 카드를 각각 `<div>`에서 `<a href="...">`로 전환 — Algorithm 섹션 5개 카드 전부 실제 페이지로 연결 완료.
  - 클로드 인 크롬으로 3개 페이지 전수 검증: 삽입 정렬 "정렬됨" 프리셋에서 비교 9회·교환 0회 확인, 병합 정렬은 재생 중 병합 구간(indigo)·비교(amber)·기록(rose) 색상 전환과 완료 시 전체 초록 정렬 확인(비교 22회·기록 34회), 퀵 정렬 "정렬됨(최악)" 프리셋에서 비교 45회·교환 0회로 O(n²) 저하 재현 확인. 5개 정렬 페이지 콘솔 에러 전부 없음, 랜딩 페이지 5개 카드 링크 전부 정상 연결 확인.
  - `CLAUDE.md`(정렬 5종 항목으로 통합 재작성 — 알고리즘별 정렬 진행 상태 표현 차이, 색상 코딩 5종 요약, 프리셋별 시간복잡도 증명 방식, GNB 페이지 수 12→15개 갱신), `docs/ARCHITECTURE.md`(페이지 표를 5종 통합 행으로, GNB·최종 갱신일 갱신), `docs/PRD.md`(§8을 정렬 5종 완료로 갱신), `docs/DEV_PLAN.md`(Phase 2.6을 정렬 5종 완료 + 다음 단계로 갱신)에 반영.
- 사용자 지시로 "랜딩 페이지 정렬 카드의 상징적인 이미지를 SVG로, 카드 상단 영역을 다 채우는 형태로" 제작 — 기존 3글자 모노스페이스 약어(Bub/Sel/Ins/Mrg/Qck) 텍스트 마크를 알고리즘별 인라인 SVG 일러스트로 교체.
  - `viewBox="0 0 400 300"`(cover-art의 `aspect-[4/3]`와 정확히 일치)에 막대그래프 형태로 그리고 `class="absolute inset-0 h-full w-full"`로 cover-art 영역 전체를 채우도록 구현(요청하신 "상단 영역을 다 채우는 형태").
  - 5개 아이콘 전부 상세 페이지에서 이미 쓰고 있는 의미 색상(rose=교환, sky=선택/삽입 대상, violet=pivot, brand green `#aef1cd`=완료/병합결과)을 재사용해 랜딩 카드와 상세 페이지 사이 시각 언어를 통일: 버블(인접 두 막대 rose 강조 + 이중 화살표 swap 아이콘), 선택(최솟값 막대 sky + 맨 앞 점선 목표 자리로 향하는 곡선 점선 화살표), 삽입(정렬된 행 위에 뜬 sky 막대가 점선 빈 자리로 내려꽂히는 화살표), 병합(위쪽 작은 두 그룹이 곡선으로 수렴해 아래쪽 합쳐진 브랜드그린 막대 행으로), 퀵(violet pivot 막대를 사이에 둔 두 점선 세로선으로 좌우 분할).
  - 클로드 인 크롬으로 5개 카드 전부 확대 검수 — 각 아이콘이 의도한 개념(교환/최솟값 이동/삽입/병합/분할)을 명확히 전달하는지, cover-art 영역을 빈틈없이 채우는지 확인. 콘솔 에러 없음.
  - `CLAUDE.md`(Algorithm 섹션 항목에 아이콘 교체 내용과 색상 의미 매핑 기록, "카드는 `<div>`로 유지" 등 이미 5종 페이지 완료로 낡아진 서술 정정)에 반영. 시각적 폴리시 수준의 변경이라 `docs/ARCHITECTURE.md`/`PRD.md`/`DEV_PLAN.md`는 갱신하지 않음.
- v0.24를 `dev`에 커밋·푸시(`a3a1bcc`) — 정렬 5종 시각화 페이지 + 랜딩 카드 SVG 아이콘. `main` 병합은 보류.

## 2026-08-23
- 사용자 질문("Algorithm 파트는 8개 카드로 구성하기로 했나? 남은 3개는?")에 `report/2026-08-22-algorithm-visualization-listup.md` §4를 근거로 답변 — 파일럿 범위는 정렬 5종 + 그래프탐색(BFS/DFS) 2종 + 이진 탐색 1종 총 8개이며, 남은 3개는 BFS·DFS·이진 탐색임을 확인.
- 사용자 지시로 "이진 탐색부터 먼저 만들어봐" — `src/algo-binary-search.html` 신규 제작.
  - `algo-bubble-sort.html`을 템플릿으로 GNB/critical CSS/시각화 골격을 재사용하되, 이진 탐색의 특성에 맞게 다음을 새로 설계:
    - 배열은 정렬 5종과 동일한 `BASE_VALUES`를 고정 사용(이진 탐색의 전제 조건)하고, 대신 "목표값"을 바꾸는 프리셋 4종(중앙값 적중=최선/일반/존재하지 않음=최악/첫 값=경계)을 만들어 O(1)~O(log n) 차이를 체감시킴. 프리셋 외에 숫자를 직접 입력해 즉석 탐색하는 입력창+버튼도 추가(정렬 5종에 없던 기능).
    - 정렬 진행 상태(`sortedFrom` 등) 대신 탐색 범위 `[lo,hi]`와 `mid`를 step에 기록 — 범위 밖은 흐리게(제외됨), mid는 amber(비교 중), 발견 시 green 4색 체계로 단순화(교환 개념이 없어 정렬 5종보다 색상이 적음).
    - 막대 인덱스 행 아래에 L/M/R 포인터 행을 추가해 현재 `lo`/`mid`/`hi` 위치를 문자로도 표시(visualgo류 포인터 표현 강화).
    - 의사코드 7줄, cover-art는 인디고 그라디언트+"Bin" 텍스트(정렬 5종의 teal 계열과 구분), 카테고리 라벨 "SEARCHING", 브레드크럼 `홈 / 이진 탐색` 2단(정렬 5종과 동일 패턴 유지).
  - 클로드 인 크롬으로 검증: "중앙값 (최선)" 프리셋에서 비교 1회 즉시 발견(O(1) 실증), "존재하지 않음 (최악)" 프리셋에서 비교 4회 후 not-found(전체 배열 회색 처리, L/R 포인터 교차 표시) 확인, 직접 값 입력(78) 정상 동작 확인. 콘솔 에러 없음.
  - 아직 `index.html`에는 연결하지 않음 — 그래프탐색(BFS/DFS)까지 만든 뒤 Algorithm 랜딩 섹션에 탐색 3종을 어떻게 반영할지(그리드 확장 vs 별도 카테고리) 함께 결정하기로 함.
  - `CLAUDE.md`(정렬 5종 항목 뒤에 이진 탐색 항목 추가, GNB 페이지 수 15→16개 갱신), `docs/ARCHITECTURE.md`(페이지 표에 이진 탐색 행 추가, GNB·최종 갱신일 갱신), `docs/PRD.md`(§8에 이진 탐색 착수 기록), `docs/DEV_PLAN.md`(Phase 2.6에 이진 탐색 착수 + 남은 작업 갱신)에 반영.
- 사용자 피드백: "이진 탐색을 시각화하는 건 좀 어색해 보이긴 하는데, 일단 랜딩 페이지에서 알고리즘 파트의 카드가 5개 이상으로 늘어나야 하니 swiper처럼 슬라이딩 가능한 형태로, 한 번에 5개 카드만 보이게 구성해줘" — `index.html` Algorithm 섹션을 정적 그리드(5칸 고정)에서 다시 Swiper 캐러셀로 전환하고, 이진 탐색을 6번째 카드로 연결.
  - Web & WebApp/Unity/Agent AI와 동일한 `.course-swiper` 마크업(각 카드를 `swiper-slide`로 감싸고, 섹션 헤더 우측에 이전/다음 화살표 + 하단 `swiper-pagination`)으로 되돌렸다. 다만 그 3개 섹션은 카드 수가 4개로 고정이라 `slidesPerView:'auto'` + 카드 고정폭(`!w-[240px] sm:!w-[280px]`)으로 자연스럽게 개수가 차는 방식인 반면, Algorithm은 카드가 계속 늘어날 예정(현재 6개, 그래프탐색까지 더하면 8개)이라 "한 번에 5개만" 요구사항을 만족시키려면 폭이 아니라 개수를 직접 고정해야 했음 — JS Swiper 초기화 루프에서 `el.id === 'algo-swiper'`를 분기해 `slidesPerView`를 화면 폭별 2(기본)/3(640px~)/5(1024px~)로 지정(기존 그리드의 `grid-cols-2 sm:grid-cols-3 lg:grid-cols-5`와 동일한 반응형 단계를 그대로 재사용), `spaceBetween`도 그리드 시절 `gap-4`와 같은 16px로 맞춤.
  - 이진 탐색 카드를 마지막(6번째) 슬라이드로 추가 — 랜딩 카드 전용 아이콘을 새로 제작(상세 페이지 헤더의 인디고 톤과 달리 Algorithm 섹션 통일성을 위해 정렬 5종과 동일한 teal 그라디언트 유지): 정렬된 막대그래프에 좌우 점선 괄호로 현재 탐색 범위를 표시하고 중앙 막대를 amber(mid 비교 중)로 강조, 위에서 아래로 꽂히는 화살표를 얹었다. "마지막 카드 하이라이트(테두리+링)" 관례를 퀵 정렬에서 이진 탐색으로 이동.
  - Algorithm 섹션 부제도 "같은 문제를 푸는 다섯 가지 정렬로…"에서 "정렬과 탐색, 같은 문제를 푸는 여러 알고리즘으로…"로 정정(더 이상 "다섯 가지"로 고정되지 않으므로).
  - 클로드 인 크롬으로 검증: 데스크톱 폭에서 정확히 5개 카드 노출 + 우측에 pagination 점 2개(6장이므로) 확인, "다음" 화살표 클릭 시 한 칸씩 슬라이드되며 이진 탐색 카드(amber 아이콘 + 하이라이트 링)가 들어오는 것 확인, 카드 클릭 시 `algo-binary-search.html`로 정상 이동 확인. 콘솔 에러 없음.
  - `CLAUDE.md`(Algorithm 섹션 항목에 Swiper 재전환 내역 추가, 이진 탐색 항목에 랜딩 카드 연결 완료 반영), `docs/ARCHITECTURE.md`(index.html 행·이진 탐색 행·최종 갱신일 갱신), `docs/PRD.md`/`docs/DEV_PLAN.md`(§8·Phase 2.6을 "완료" 상태로 갱신, Swiper 전환 내용 추가)에 반영.
- 사용자 지시로 "BFS/DFS도 이어서 만들어줘" — Algorithm 코스 파일럿 8종의 마지막 카테고리인 그래프 탐색 2종을 완성해 파일럿 전체를 마무리.
  - `src/algo-bfs.html`/`src/algo-dfs.html` 신규 생성 — 정렬·이진 탐색 페이지의 "step 배열 사전 계산 → 재생 컨트롤이 인덱스만 이동" 골격은 재사용하되, 시각화 대상이 막대그래프가 아니라 **노드-엣지 그래프**라 렌더링 엔진을 SVG(`<circle>`/`<line>`/`<text>`, `document.createElementNS`로 동적 생성)로 새로 작성.
    - 두 파일이 완전히 동일한 그래프(9노드 A~I, 12개 무방향 간선, `ADJ`/`POS` 상수를 각 파일에 중복 정의)를 공유하도록 설계 — "같은 그래프를 다른 순서로 방문한다"는 대비 자체가 핵심 학습 포인트이기 때문. 그래프는 BFS가 레벨별로 고르게 퍼지고 DFS는 한 갈래로 깊게 파고드는 차이가 잘 드러나도록 사이클을 포함해 손으로 설계(트리형 레이아웃 + 일부 교차 간선).
    - BFS: 큐(FIFO)를 `visited-at-enqueue` 방식으로 구현(재큐잉 방지, 표준 구현) — A에서 시작 시 방문 순서 A→B→C→D→E→F→G→H→I(정확히 레벨 단위)를 손으로 먼저 시뮬레이션해 예측한 뒤 코드로 재현, 클로드 인 크롬으로 최종 단계(28/28)까지 재생해 방문 순서·배지 번호 일치 확인.
    - DFS: 스택(LIFO)을 `visited-at-pop` 방식으로 구현하되, **이웃을 알파벳 역순으로 push**해야 pop 순서가 실제 재귀 DFS(이웃을 알파벳 순으로 순회)와 정확히 일치한다는 점을 손 시뮬레이션으로 먼저 검증(재귀 방식과 반복 방식이 방문 순서까지 동일함을 확인한 뒤 구현) — A에서 시작 시 A→B→E→I→G→C→F→D→H, 총 60스텝(BFS의 28보다 훨씬 많음 — 이미 방문한 노드가 스택에 중복으로 쌓였다가 나중에 pop되며 건너뛰는 스텝들이 포함되기 때문). 이 "중복 pop 건너뜀"을 노드 테두리 점선(rose dashed ring)으로 별도 표시하고, 클로드 인 크롬으로 실제 건너뜀 메시지("A는 이미 방문했으므로 건너뜁니다")가 뜨는 스텝을 확인.
    - 두 페이지 모두 정렬 배열 프리셋 대신 "시작 노드" 프리셋(A 기본/E/G/I 버튼 4개) + `<select>`로 나머지 노드(B/C/D/F/H) 중 골라 시작하는 커스텀 입력을 제공 — 같은 그래프에서 시작점만 바꿔도 레벨 구조·방문 순서가 달라지는 것을 비교하게 함(이진 탐색 페이지의 "직접 값 입력" 패턴을 계승).
    - 노드 상태 4색(미방문/대기·발견됨/처리 중/방문 완료) + 활성 간선 강조(rose) + 노드 우상단 "방문 순서" 숫자 배지 + 큐/스택 pill 패널(다음에 처리될 항목에 링 하이라이트)로 구성. 의사코드 BFS 5줄/DFS 6줄.
  - `index.html` Algorithm 섹션에 7·8번째 카드로 연결 — 랜딩 카드 전용 아이콘도 새로 제작(작은 트리형 그래프 SVG): BFS는 층별 색상 분포(완료=초록/처리중=앰버/대기=스카이)로 "넓게 퍼짐"을, DFS는 뿌리에서 한 갈래로 이어지는 rose 경로 + 나머지 미방문(반투명 회색)으로 "깊게 파고듦"을 대비시켰다. "마지막 카드 하이라이트(테두리+링)" 관례가 이진 탐색 → DFS로 이동.
  - 클로드 인 크롬으로 전수 검증: BFS/DFS 두 페이지 모두 그래프·큐/스택 패널·범례 렌더링 확인, 최종 단계에서 방문 순서 문자열이 손으로 미리 계산한 값과 정확히 일치함을 확인, 랜딩 페이지에서 8개 카드 전부(다음 화살표로 페이지 넘김) 노출과 BFS 카드 클릭 시 정상 이동 확인. 콘솔 에러 없음.
  - **Algorithm 코스 파일럿 8종(정렬 5 + 이진탐색 1 + 그래프탐색 2) 전부 완료** — `report/2026-08-22-algorithm-visualization-listup.md` §4에서 제안한 범위를 모두 구현.
  - `CLAUDE.md`(BFS/DFS 항목 신설, GNB 페이지 수 16→18개 갱신), `docs/ARCHITECTURE.md`(페이지 표에 BFS/DFS 행 추가, GNB·index.html 행·최종 갱신일 갱신), `docs/PRD.md`(§8을 파일럿 8종 전부 완료로 갱신), `docs/DEV_PLAN.md`(Phase 2.6을 파일럿 완료 + 남은 작업으로 갱신)에 반영.
- 사용자 지시로 "카드 swiper 처리를 하나씩 스와이프 하던거를 5개 카드를 스와이프 하는 걸로 수정" — `index.html`의 Algorithm Swiper(`#algo-swiper`) 초기화 옵션에 `slidesPerGroup`이 빠져 있어(기본값 1) `slidesPerView`를 2/3/5로 반응형 지정해도 화살표 클릭 시 한 칸씩만 넘어가던 문제를 수정. `slidesPerView`와 동일한 값(2/3/5)으로 `slidesPerGroup`을 breakpoints에 함께 지정해 화면에 보이는 카드 묶음 단위로 한 번에 넘어가도록 변경. 클로드 인 크롬으로 데스크톱 폭에서 "다음" 클릭 시 5개 카드가 한 번에 교체됨(마지막 그룹은 남은 카드 수에 맞춰 자동 보정) 확인, 콘솔 에러 없음.
- 사용자와 논의 후 Algorithm 파트에 자료구조(스택/큐/힙 등) 콘텐츠를 추가하기로 결정 — 대화로 범위를 좁혀감: 스택/큐/연결 리스트/해시 테이블/힙/이진 탐색 트리 6종으로 확정, 정렬·탐색 8종과 합치면 14장이라 배지 구분보다 섹션 분리가 낫겠다고 제안했고 사용자가 "그 방향으로 진행해줘"로 승인.
  - `index.html` Algorithm 섹션(`#algo`)을 "정렬·탐색"(`#algo-swiper`, 기존 8장)과 "자료구조"(`#ds-swiper`, 신규 6장) 두 서브그룹으로 분리 — 각각 `.swiper-block`으로 감싸 자기 소제목·nav 화살표·pagination을 독립적으로 가짐. 히어로 5개 카테고리 구조는 건드리지 않고 Algorithm 섹션 내부만 재구성.
  - nav 화살표 스코프를 `el.closest('section')`에서 `el.closest('.swiper-block') || el.closest('section')`로 좁혀, 같은 섹션 안 두 캐러셀의 nav 화살표가 서로 간섭하지 않도록 JS 수정. 카드 수 고정(2/3/5 고정 슬라이드) 대상 목록을 `algo-swiper` 단일값에서 `['algo-swiper', 'ds-swiper']` 배열로 일반화.
  - 자료구조 6개 카드(스택/큐/연결 리스트/해시 테이블/힙/이진 탐색 트리)를 전용 SVG 아이콘(보라 계열 `--from:#ddd6fe;--to:#4338ca`, 정렬·탐색의 teal과 구분) + "준비중" 배지와 함께 `#ds-swiper`에 추가 — 상세 시각화 페이지는 아직 없어서 `<a>`가 아닌 비클릭 `<div>`로 처리(OJ/LMS 미오픈 배지 관례를 카드 단위로 재사용).
  - 클로드 인 크롬으로 검증: 정렬·탐색/자료구조 두 캐러셀이 각자 5개/6개 카드로 정상 노출, "다음" 화살표가 서로 다른 캐러셀만 독립적으로 움직임(상대 캐러셀에 영향 없음) 확인, 콘솔 에러 없음.
  - `CLAUDE.md`(Algorithm 섹션 항목에 slidesPerGroup 수정 이력 + 서브그룹 분리 내역 추가), `docs/ARCHITECTURE.md`(index.html 행에 서브그룹 구조 반영, 최종 갱신일 갱신), `docs/PRD.md`(§8에 자료구조 파일럿 착수 기록), `docs/DEV_PLAN.md`(Phase 2.7 신설)에 반영.
- 사용자 피드백: "알고리즘과 자료구조 섹션에서 카드 focus 효과가 조금 이상하네, Language 섹션과 같은 방식으로 맞춰줘" — 원인은 AOS `[data-aos^=fade].aos-animate{transform:...}` 규칙(명시도 0,3,0)이 `.course-card:hover{transform:translateY(-6px)}`(명시도 0,2,0)보다 높아, `data-aos`가 걸린 요소 자신의 hover `transform`만 무효화되는 CSS 캐스케이딩 특성 때문. Language 섹션은 원래 `data-aos`를 `course-card` 자체(`<a>`)에 붙여왔던 반면 Algorithm/자료구조 두 섹션은 부모 `swiper-slide` div에 붙여서, `course-card`의 hover `transform`이 살아남아 카드가 위로 6px 튀는 효과가 났던 것.
  - `index.html`의 `#algo-swiper`(8장)·`#ds-swiper`(6장) 전체 14개 슬라이드에서 `data-aos="fade-up"`을 `swiper-slide` wrapper에서 각 `course-card`(`<a>` 또는 준비중 `<div>`) 요소로 이동 — Language 섹션과 완전히 동일한 마크업 패턴으로 통일.
  - 클로드 인 크롬으로 검증: 이동 전 hover 시 `getComputedStyle(card).transform`이 `translateY(-6px)`에 해당하는 값으로 바뀌던 것이, 이동 후에는 Language 카드와 동일하게 `matrix(1,0,0,1,0,0)`(identity, 즉 움직임 없음)으로 유지되고 테두리·그림자만 바뀜을 확인. 정렬·탐색/자료구조 두 캐러셀 모두 동일하게 수정 확인, 콘솔 에러 없음.
  - Web & WebApp/Unity/Agent AI 3개 섹션도 같은 `swiper-slide` wrapper에 `data-aos`를 붙이는 동일 패턴이라 이론상 같은 현상이 있지만, 이번 요청 범위(알고리즘·자료구조)에는 포함되지 않아 손대지 않음 — 필요시 별도 요청으로 처리.
  - `CLAUDE.md`에 "`data-aos`는 `swiper-slide`가 아니라 `course-card` 요소 자체에 붙일 것" 관례를 신설 항목으로 추가(새 캐러셀 카드를 만들 때 재발 방지).
- 사용자 지시로 "스택 상세 페이지부터 제작해줘" — 자료구조 6종(스택/큐/연결 리스트/해시 테이블/힙/이진 탐색 트리) 중 첫 페이지로 스택을 선택해 제작.
  - `src/algo-stack.html` 신규 생성 — 정렬·탐색 페이지의 "step 배열 사전 계산 → 재생 컨트롤이 인덱스만 이동" 골격을 재사용하되, 스택은 비교·탐색 대상이 아니라 **push/pop 연산 시퀀스**이므로 각 시나리오를 `{op:'push'|'pop', value, label}` 객체 배열로 미리 스크립트로 작성하고 공통 `buildSteps()`가 이를 순회하며 스텝을 기록하는 방식으로 설계.
  - 동작 시나리오 4종(다른 페이지의 "배열/목표값/시작 노드" 프리셋에 대응): 기본 Push/Pop(마지막에 빈 스택에서 pop을 시도해 언더플로우까지 시연), 함수 호출 스택(중첩 함수 호출·반환을 push/pop으로 재현), 괄호 검사(`{[()]}` 문자열을 스캔하며 여는 괄호 push·닫는 괄호 pop으로 짝 맞춤 확인), 실행 취소(Undo, 타이핑 스냅샷 push 후 Ctrl+Z를 pop으로 반복해 되돌리다 결국 언더플로우까지 도달).
  - 다른 상세 페이지들의 "직접 값 입력"(이진 탐색)·"이 노드에서 시작"(BFS/DFS)이 단발성 재계산이었던 것과 달리, 스택은 값 입력창 + Push/Pop 버튼을 누를 때마다 현재 커스텀 연산 목록에 한 건씩 누적하며 즉시 마지막 단계로 이동하는 방식으로 구현(`state.mode`를 `'preset'`↔`'custom'`으로 전환, 커스텀 진입 시 프리셋 버튼 `is-active` 전부 해제) — 반복 조작이 자연스러운 스택의 성격에 맞춘 설계.
  - 렌더링 엔진은 막대그래프·SVG 그래프와 별개로 새로 작성 — 세로 박스 리스트를 `flex flex-col-reverse`로 감싸 배열의 앞(바닥)~뒤(꼭대기)가 자연스럽게 아래→위로 쌓이도록 구현. push/pop마다 박스 개수가 바뀌므로 막대그래프처럼 스켈레톤을 고정해두는 최적화 없이 매 스텝 전체를 다시 그리는 단순한 방식을 택함. 색상 4종(내부 값/최상단 TOP/방금 push/언더플로우 시도) 범례 포함.
  - `node --check`로 인라인 스크립트 문법 검증, `buildSteps` 로직을 Node로 별도 시뮬레이션해 4개 시나리오 모두 의도한 최종 상태(기본·Undo는 언더플로우로 종료, 함수 호출 스택·괄호 검사는 빈 스택으로 정상 종료)와 일치함을 확인. 이번 세션은 claude-in-chrome MCP 연결이 끊겨 실제 브라우저 조작 검증은 수행하지 못함 — 코드 검증(정적 분석 + 로직 시뮬레이션)만 완료된 상태이므로, 다음에 브라우저로 직접 재생/커스텀 조작을 확인할 필요가 있음.
  - `index.html`의 `#ds-swiper` 스택 카드를 비클릭 `<div>`+"준비중" 배지에서 `<a href="src/algo-stack.html">`로 전환. 나머지 5장(큐/연결 리스트/해시 테이블/힙/이진 탐색 트리)은 그대로 둠.
  - `CLAUDE.md`(자료구조 항목 신설, GNB 페이지 수 18→19개 갱신)에 반영.
- 사용자와 "자료구조도 visualgo 방식이 어울리는지" 논의 후 "큐도 구성해줘" 지시 — 자료구조 6종 중 두 번째로 큐를 스택 페이지 그대로 템플릿 삼아 제작.
  - `src/algo-queue.html` 신규 생성 — 엔진(공통 `buildSteps()`, 값 입력창+버튼 2개 커스텀 조작, `state.mode` 전환)은 스택과 완전히 동일하게 재사용하고, FIFO/LIFO 차이만 반영. `enqueue`는 `array.push`, `dequeue`는 `array.shift`로 구현(스택의 `push`/`pop`이 같은 쪽 끝을 쓰는 것과 달리 큐는 반대쪽 끝을 쓴다는 점이 핵심 차이 — 설명 섹션에 명시).
  - 동작 시나리오 4종(스택의 함수 호출/괄호 검사/Undo에 대응): 기본 Enqueue/Dequeue(언더플로우 포함), 프린터 작업열, 대기열(줄서기), BFS 탐색열(A 방문→B,C 발견→B 방문→D 발견→C→D 순서로 "가까운 노드부터 레벨 순서로 처리"라는 FIFO의 실제 쓰임을 보여줌).
  - 렌더링은 스택의 세로 박스 리스트(`flex flex-col-reverse`)와 달리 **가로 박스 리스트**(`overflow-x-auto`)로 변경 — FIFO는 "왼쪽 FRONT에서 빠지고 오른쪽 REAR에 쌓인다"는 좌우 은유가 스택의 위/아래 은유보다 직관적이라 판단.
  - `node --check`로 문법 검증, `buildSteps` 로직을 Node로 시뮬레이션해 4개 시나리오 모두 의도한 최종 상태(기본은 언더플로우 종료, 나머지 3개는 빈 큐로 정상 종료, BFS 시나리오는 A→B→C→D 레벨 순서로 정확히 dequeue됨)와 일치함을 확인. 이번에도 claude-in-chrome MCP 연결이 끊긴 상태라 실제 브라우저 조작 검증은 수행하지 못함 — 정적 분석 + 로직 시뮬레이션만 완료.
  - `index.html`의 `#ds-swiper` 큐 카드를 `<a href="src/algo-queue.html">`로 전환. 나머지 4장(연결 리스트/해시 테이블/힙/이진 탐색 트리)은 그대로 둠.
  - `CLAUDE.md`(큐 항목 신설, GNB 페이지 수 19→20개 갱신)에 반영.
- 사용자 지시로 "나머지 4개 카드 상세 페이지 작업해줘" — 자료구조 6종의 마지막 4종(연결 리스트/해시 테이블/힙/이진 탐색 트리)을 한 번에 제작해 파일럿을 완료.
  - 작업 전, 각 구조의 연산 로직(buildSteps)을 Node로 먼저 시뮬레이션·검증한 뒤 HTML/JS로 옮기는 방식을 4개 전부에 적용(이전 스택/큐와 동일한 방법론) — 최종적으로 파일에서 그대로 추출한 코드를 다시 Node로 실행해 실제 배포될 코드가 맞는지 재검증.
  - `src/algo-linked-list.html`: 스택/큐와 달리 값에 인덱스로 바로 접근할 수 없다는 것을 보여주기 위해, 대상을 찾아야 하는 연산(`insertBack`/`insertAfter`/`deleteValue`)마다 노드를 하나씩 지나가는 "순회" 스텝을 명시적으로 기록하도록 `buildSteps()`를 설계. 시나리오 4종(기본/재생목록/즐겨찾기/검색 후 삽입·삭제) 전부 Node 시뮬레이션으로 최종 리스트 상태 일치 확인. 큐의 가로 박스 리스트를 재사용하되 화살표(→)와 `NULL` 종단 박스, "순회 중"(호박색) 상태를 추가.
  - `src/algo-hash-table.html`: `hash(key) = key % 7` 단순 함수로 버킷 배열(7개)과 체이닝을 시뮬레이션하는 새 엔진. 시나리오 4종 설계 시 충돌이 정확히 일어나도록 키를 직접 계산해서 골랐다 — 기본(3,10이 충돌) / 학번→사물함 배정(15,22가 충돌, "다른 학번인데 같은 사물함"이라는 체감형 비유) / 전화번호부(11,23,35가 서로 다른 버킷이라 충돌 없이 O(1) 검색) / 최악의 해시 함수(7,14,21이 전부 버킷 0에 몰리고 없는 키 28을 검색하면 3개를 전부 순회한 뒤 실패 — 나쁜 해시 함수가 O(n)으로 퇴화하는 것을 수치로 증명). Node로 각 시나리오의 최종 버킷 구성을 검증.
  - `src/algo-heap.html`: Algorithm 코스 최초로 **동적으로 좌표를 계산하는 SVG 트리 엔진**을 도입(BFS/DFS는 고정 그래프라 좌표를 상수로 미리 정의했지만, 힙은 크기가 계속 바뀌므로 인덱스로부터 `heapPos(i,n)`이 매번 레벨과 위치를 계산). 최소 힙으로 insert(sift-up)/extractMin(sift-down) 구현, 트리 바로 아래에 같은 힙을 배열로도 나란히 보여줘 "배열 하나로 트리를 표현한다"는 힙의 핵심을 시각적으로 연결. 시나리오 4종(기본 삽입 / 마감일 스케줄러 / 힙 정렬 체감 — extractMin 반복으로 오름차순 출력 증명 / 최솟값이 떠오르는 순간) 전부 Node로 `verifyMinHeap()`을 돌려 매 시나리오 종료 시점에 힙 속성이 실제로 유지되는지 확인.
  - `src/algo-bst.html`: 힙과 마찬가지로 새 SVG 트리 엔진을 쓰되, 힙과 달리 실제 노드 객체(`{value,left,right}`)를 스텝마다 깊은 복사해 히스토리로 남기고, 좌표는 `layoutTree()`가 **중위 순회 순서**로 x좌표를 매겨 계산(중위 순회 결과가 항상 오름차순이라는 BST 성질을 그대로 레이아웃에 활용). 삭제는 자식 수(0/1/2)에 따라 처리가 복잡해져 의도적으로 범위에서 제외하고 insert+search만 구현 — 설명 섹션에 이 트레이드오프를 명시. 시나리오 4종(기본 삽입 / 정렬된 순서 삽입=편향 트리, 높이5 / 균형 잡힌 삽입=높이3 / 검색 시연=존재값 2회·미존재값 3회 비교) 전부 Node로 `isBST()` 유효성과 트리 높이·모양을 검증, 처음 초안에서 "기본 삽입" 프리셋에 검색 스텝이 잘못 섞여 들어간 것을 발견해 분리 수정.
  - `index.html`의 `#ds-swiper` 나머지 4개 카드(연결 리스트/해시 테이블/힙/이진 탐색 트리)를 전부 `<a>` 링크로 전환. **자료구조 6종 전부 완료**를 기념해 마지막 카드(이진 탐색 트리)에 "마지막 카드 하이라이트" 관례(테두리+링, `text-slate-600`)를 처음 적용 — 지금까지는 완성되지 않은 자료구조 서브그룹이라 어떤 카드에도 적용하지 않고 있었음.
  - 이번에도 claude-in-chrome MCP 연결이 끊긴 상태라 실제 브라우저 조작 검증은 수행하지 못함 — HTML 태그 밸런스 검사, JS 문법 검사(`node --check`), 그리고 각 파일에서 실제로 추출한 `buildSteps`/`SCENARIOS` 코드를 Node로 재실행하는 방식으로 6개 파일 모두의 로직을 검증했다. 브라우저 연결이 복구되면 6개 페이지 전부 시각적으로 재생/커스텀 조작을 확인할 필요가 있음.
  - `CLAUDE.md`(4개 항목 신설, GNB 페이지 수 20→24개 갱신)에 반영.
- 사용자와 다음 코스인 Web & WebApp 섹션 구성을 대화로 논의 — 카드 흐름을 프론트/백엔드/DevOps/CI-CD 4단계로 정리했으나, 사용자가 기존 "Full-stack 프로젝트" 캡스톤 카드를 유지하고 싶어해 5장으로 확정. 각 상세 페이지에 들어갈 커리큘럼 주제도 함께 스케치(코드로 남기지 않고 대화로만 정리).
  - 사용자가 "카드 상단 썸네일을 더 직관적인 이미지로 만들 수 있는지, unsplash 같은 무료 이미지 사이트를 참고해도 되는지" 질문 — Unsplash 사진은 사이트의 "그라디언트+크리스프한 심볼" 톤과 안 맞을 것 같다고 판단해, Language 섹션처럼 실제 기술 로고를 쓰는 방향을 역제안했고 사용자가 승인.
  - `index.html`의 `#web` 섹션 카드를 4장에서 5장으로 재구성: Frontend(React) / Backend(Node·Spring) / DevOps(Docker) / CI/CD(GitHub Actions) / Full-stack 통합 프로젝트(캡스톤, 마지막 카드 하이라이트를 기존 "배포/인프라 기초" 카드에서 이관). 기존 한 장에 뭉쳐 있던 DevOps·CI/CD 설명을 분리해 각각 새 문구 작성.
  - React/Node.js/Docker/GitHub Actions 로고는 devicon(MIT 라이선스) 저장소의 `*-original.svg`(컬러 아이콘 단독, 128×128)를 jsdelivr CDN에서 다운로드해 `assets/image/react.svg`/`nodejs.svg`/`docker.svg`/`github-actions.svg`로 저장 — Java/Python/Dart 워드마크를 가져올 때(2026-08-22)와 동일한 저장소·라이선스이지만, 이번엔 SVG를 그대로 써서 당시 필요했던 PNG 래스터화(wsrv.nl 프록시) 단계가 필요 없었음.
  - Full-stack 통합 프로젝트는 특정 브랜드가 없어 커스텀 인라인 SVG(가로 막대 3개가 화살표로 위→아래 이어지는 형태, 불투명도로 계층 구분)를 새로 그려 "프론트·백엔드·인프라가 하나로 엮인다"는 개념을 표현.
  - 검증: 4개 SVG 파일 다운로드 후 `content-type: image/svg+xml`·HTTP 200 확인, `index.html` HTML 태그 밸런스 검사 통과, 로컬 서버로 이미지·페이지 전부 200 확인. claude-in-chrome MCP 연결이 이번에도 끊긴 상태라 실제 브라우저 렌더링(카드 배치·로고 시각 확인)은 수행하지 못함.
  - 5개 카드 모두 아직 상세 페이지가 없어 비클릭 `<div>`로 유지(Unity/Agent AI 섹션처럼 "준비중" 배지 없이 그대로 둠 — 섹션 전체가 미착수 상태라 개별 배지가 불필요).
  - `report/2026-08-23-web-webapp-cards.md` 신규 작성(로고 소스·라이선스·검증 내역), `CLAUDE.md`(Web & WebApp 섹션 항목 신설, `assets/image/` 설명에 신규 로고 4종 추가)에 반영.
- 사용자 피드백 3건 — "① 컬러톤을 JS 대표 색깔인 노란색으로, ② 카드 상단을 가득 채우는 이미지가 가능한지, ③ 카드가 5개뿐이니 Swiper 없이 Language처럼 정적 그리드로" — 방금 만든 Web & WebApp 카드를 즉시 수정.
  - 커버아트를 하늘색(`--from:#7dd3fc;--to:#075985`)에서 노란색(`--from:#fde047;--to:#854d0e`)으로 5개 카드 전부 변경.
  - "가득 채우는 이미지" 질문에는, 방금 넣은 `h-20 w-20` 실제 로고를 그대로 확대하면 로고 여백 때문에 어색하다고 판단해, Algorithm/자료구조 섹션에서 이미 쓰던 "커버아트 전체를 채우는 커스텀 인라인 SVG"(`absolute inset-0 h-full w-full`) 패턴을 역제안하고 실행 — Frontend는 브라우저 창+React 원자 심볼, Backend는 서버 랙↔DB 원통 화살표, DevOps는 컨테이너 3개+톱니바퀴, CI/CD는 4단계 파이프라인 노드로 각각 새로 그렸고, 각 기술의 실제 브랜드 색상(React/Node/Docker/GitHub Actions)을 강조색으로 재사용해 직관성을 유지. 직전에 다운로드한 로고 SVG 4개는 삭제하지 않고 `assets/image/`에 그대로 남겨둠(현재 미사용, 추후 상세 페이지에서 재사용 가능).
  - Swiper 캐러셀(`.course-swiper`/`swiper-slide`/`swiper-pagination`/nav 화살표)을 완전히 제거하고 Language 섹션과 동일한 `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-4` 정적 그리드로 교체 — Unity/Agent AI는 여전히 4카드 Swiper라 손대지 않음.
  - 검증: HTML 태그 밸런스 재검사 통과, 섹션 내 "swiper" 문자열 0건(grep) 확인, 로컬 서버로 200 확인. claude-in-chrome 연결이 계속 끊긴 상태라 실제 렌더링은 미확인.
  - `report/2026-08-23-web-webapp-cards.md`·`CLAUDE.md`에 후속 수정 내역 반영.
- 사용자 지시로 미세 조정 2건: Frontend 카드 React 원자 심볼을 17px 위로 이동(`translate(200,172)`→`translate(200,155)`, 아래 UI 블록 때문에 무게중심이 낮아 보인다는 지적 반영), 5개 카드 전부에서 난이도 배지(입문/중급/심화)를 완전히 제거. HTML 검증·로컬 서버 200 확인 완료.
- 사용자와 "상세 페이지는 어떻게 구성할까 — Language처럼 커리큘럼 중심? Algorithm처럼 시각화 중심?" 논의. Algorithm 방식은 어려울 것 같다는 사용자 의견에 동의하면서도, CI/CD만은 파이프라인 자체가 순차적 상태를 가져 작은 시각화가 가능할 것 같다고 역제안 — 사용자가 "네가 제안해준 방식으로 가자"고 승인해, 5장 전부 Language 커리큘럼 형식 + CI/CD만 보너스 파이프라인 시각화로 제작.
  - `src/web-frontend.html`/`web-backend.html`/`web-devops.html`/`web-cicd.html`/`web-fullstack.html` 5개 파일 신규 생성 — `lang-cp.html`을 템플릿으로 GNB/브레드크럼/커리큘럼 아코디언/CTA/Footer/토글 스크립트를 그대로 재사용하고 콘텐츠만 교체. 각 페이지 12개 커리큘럼 항목(기초 5+실습 5+프로젝트 2)을 사이트의 "데이터 흐름" 철학에 맞춰 새로 작성 — Frontend(브라우저 렌더링→React 컴포넌트/상태/이펙트→대시보드 프로젝트), Backend(요청-응답 사이클→REST/DB/ORM→인증/캐싱→API 서버 프로젝트), DevOps(개발-배포 환경 차이→Docker 이미지/Compose→클라우드/Nginx→컨테이너화 프로젝트), CI/CD(자동화 필요성→GitHub Actions 문법→자동 배포/롤백→파이프라인 프로젝트).
  - Full-stack 통합 프로젝트 페이지만 예외적으로 12개 항목이 새 개념이 아니라 **기획→설계→구현→배포→회고**로 이어지는 프로젝트 진행 단계이며, 각 항목 설명에서 앞선 4개 코스를 명시적으로 되짚어("Backend 코스에서 다룬 ~을 이번엔 실제로" 식) 통합 코스라는 성격을 드러냄.
  - Web & WebApp 코스 바로가기 pill 탭을 5개 파일 모두에 넣고(언어 페이지의 "언어 바로가기 탭"과 동일 패턴) `aria-current="page"`로 현재 페이지 표시.
  - CI/CD 페이지 전용 보너스로 Push→Build→Test→Deploy→Live 5단계 파이프라인 재생 위젯을 커리큘럼 목록 위에 추가 — Algorithm 코스의 "step 배열 사전 계산 → 재생 컨트롤이 인덱스만 이동" 패턴을 아주 단순화해 재사용(새 엔진을 통째로 만들 필요 없이 원형 노드+연결 화살표만으로 구성). 시나리오 2종(성공 흐름/테스트 실패 흐름)의 로직을 Node로 사전 검증(성공 7스텝 전부 done, 실패 5스텝에서 Test만 failed 나머지 pending)한 뒤 코드로 옮기고, 실제 파일에서 추출한 코드로 재검증까지 완료.
  - `index.html` `#web` 섹션 5개 카드를 전부 `<a>` 링크로 전환(각 상세 페이지로 연결).
  - 검증: 5개 파일 전부 HTML 태그 밸런스 검사 통과, CI/CD 페이지 인라인 스크립트 3개 전부 `node --check` 통과, 로컬 서버로 `index.html`+5개 페이지 전부 200 확인. claude-in-chrome 연결이 계속 끊긴 상태라 실제 브라우저 렌더링(아코디언 펼침, 파이프라인 재생 애니메이션)은 미확인 — 연결 복구 시 확인 필요.
  - `CLAUDE.md`(Web & WebApp 상세 페이지 항목 신설, GNB 페이지 수 24→29개 갱신)에 반영.
- 사용자가 이미지+텍스트 레이어 4개를 순서대로 실제 콘텐츠로 확정(위 "임시" 항목 이어서 진행):
  - 사용자가 "이미지 자리에 Gemini 이미지를 구성할 예정인데 워터마크를 가릴 방법?"으로 문의 → 배너처럼 CTA로 덮는 방식은 이 4개 레이어엔 CTA가 없어 어울리지 않는다고 답하고, 이미지를 4:3보다 여유 있게 생성해 워터마크가 있는 가장자리를 크롭하는 방식을 제안. 이어 "이미지 사이즈는 동일한가?" 질문에 4개 전부 표시 648×486px(4:3), 권장 소스 1300×975px(2x)로 통일되어 있음을 답변.
  - 레이어 1: 사용자가 `consult-01.png`(Gemini 생성, 2400×1792 — 도서관에서 홀로그램 지구본을 가리키는 학생) 제공, 주제 "AI-Agent와 함께하는 거시적 사고의 확장 연습"으로 타이틀·서브 텍스트 작성. 이미지 우하단의 반투명 스파클 워터마크를 `PIL`로 크롭 제거해 `assets/image/about-consult-01.jpg`(1300×975)로 저장·적용. 이어진 사용자 지시로 ① 섹션을 `pt-24 pb-14`로 아래로 내리고 ② 배지 텍스트(녹색 eyebrow) 제거 ③ 타이틀 한 줄 강제(`whitespace-nowrap` + 반응형 폰트 축소)로 정리 — 이 스타일(배지 없음·한 줄 타이틀)이 이후 4개 레이어 전체의 공통 규칙이 됨. 이후 사용자가 타이틀을 "AI Agent 시대, 지식의 습득을 넘어 거시적 통찰로"로 교체 요청해 서브 텍스트도 새 타이틀 논리(지식 습득 가치 하락 → 질문·판단력이 경쟁력 → Recode Coding 훈련이 그 능력을 기름)로 재작성.
  - 레이어 2: `consult-02.png`(코딩테스트 준비하며 야근하는 개발자, 좌하단 워터마크) → `about-consult-02.jpg`. 주제 "지식 위에서 훈련을 통해서 비로소 지혜가 쌓인다" — 암기가 아닌 도제식 훈련이라는 맥락으로 서브 텍스트 구성.
  - 레이어 3: `consult-03.png`(사수가 옆에서 코드를 봐주는 멘토링 장면, 워터마크가 머그컵 위에 인쇄된 형태로 등장 — 다른 레이어와 달리 오른쪽 인물 옷 대신 소품에 합성됨) → `about-consult-03.jpg`. 처음 타이틀 "실력은 혼자 자라지 않는다, 사수 곁에서 자란다" → 이후 두 차례 수정: ① "左멘토 右AI-Agent"로 교체(레이어 1의 AI-Agent 주제와 재결합, "속도는 AI-Agent, 판단은 사수"라는 역할 분담 논리로 서브 텍스트 재작성) ② "좌/우"를 한자 표기로 바꿔달라는 지시로 최종 "左멘토 右AI-Agent" 확정 — 사이트 전체에서 유일하게 타이틀에 한자를 쓰는 사례.
  - 레이어 4: `consult-04.png`(팀원들이 화이트보드 설계도 앞에서 토론하는 장면, 워터마크가 오른쪽 인물의 옷에 등장) → `about-consult-04.jpg`. 타이틀 "실전은 경험하는 곳이 아니라 증명하는 무대"로 서브 텍스트까지 작성했는데, 이후 사용자가 파일을 직접 열어 "실전은 경험이 아니라 증명하는 무대"로 축약 수정 — 확인 후 그대로 유지(이번 세션에서 사용자가 about.html에 직접 손댄 두 번째 사례, 첫 번째는 위 수동 `<br/>` 추가).
  - 4개 레이어 전부 검증: HTML 태그 밸런스 통과, 5개 이미지(배너+레이어4) `naturalWidth`/`complete` 정상 로드, `scrollWidth > clientWidth` 비교로 4개 타이틀·서브 텍스트 전부 오버플로우 없음을 브라우저에서 직접 확인.
  - `CLAUDE.md`(`src/about.html` 항목의 "임시" 레이어 설명을 실제 콘텐츠·이미지 소싱 워크플로우 설명으로 전면 교체)에 반영.
- v0.32를 dev에 커밋·푸시, main 병합 — 이미지+텍스트 레이어 4개 최종 확정(consult-01~04 워터마크 제거·크롭, 타이틀/서브 텍스트 확정, 左/右 한자 표기).
- 사용자 지적 — "web 섹션 마지막 카드 테두리가 혼자 연두색이네" — Full-stack 통합 프로젝트 카드의 "마지막 카드 하이라이트"가 다른 섹션과 동일하게 브랜드 그린(`border-brand-400/60 ring-brand-400/40`)이었는데, Web & WebApp만 노란 톤 카드라 유독 튀어 보였음. `border-yellow-500/60 ring-1 ring-yellow-500/40`로 교체해 섹션 톤에 맞춤(다른 섹션의 브랜드 그린 하이라이트는 그대로 유지 — Web & WebApp만의 예외 처리).
- 히어로 배너(`#hero-banner`) 레이아웃·콘텐츠를 사용자 요청으로 단계적으로 개편:
  - 상단 라인 정렬: `#hero-content`를 `justify-end`(전체 하단 정렬)에서 자연스러운 상단 흐름(eyebrow → title → desc → CTA)으로 바꿔, eyebrow 라벨이 우측 `#hero-side-list` 첫 항목과 같은 높이에서 시작하도록 수정.
  - 타이틀 상단 라인 고정: desc 줄 수(1~2줄)가 슬라이드마다 달라도 title의 시작 y좌표(177px)가 5개 슬라이드 전부 동일하게 유지되도록, title을 desc/CTA 높이 변화와 무관한 위치에 배치.
  - CTA 버튼을 `mt-auto pt-10`으로 배너 하단 쪽으로 이동(desc 바로 아래 붙지 않고 여백을 두고 하단에 배치).
  - **랜덤 카드 노출**: 히어로 버튼(게이지)이 포커싱될 때마다, 해당 섹션의 실제 `.course-card`(Language 5·Algorithm 14·Web & WebApp 5·Unity 4·Agent AI 4 = 총 32개) 중 하나를 랜덤으로 뽑아 title/desc를 히어로 타이틀·설명 자리에 노출하도록 `hero-content` 스크립트에 `SLIDES[i].cards` 수집·`pickRandomCard()` 로직 추가. eyebrow·CTA·배경색은 섹션 고정값 유지.
  - **3줄 설명 확장**: 카드 그리드용 1줄 요약(`<p>`)은 그대로 두고, 히어로 전용 3줄 분량 설명을 32개 카드 전부에 `data-hero-desc` 속성으로 추가 작성(JS는 `data-hero-desc` 우선, 없으면 `<p>` 텍스트로 폴백). 실제 렌더 너비(576px)·줄바꿈 지점은 텍스트마다 달라 char count만으로는 예측이 부정확해, 브라우저에서 `Range` 대신 오프스크린 `<p>` 프로브 엘리먼트의 `getBoundingClientRect().height / line-height`로 실제 줄 수를 측정하며 32개 전부가 정확히 3줄이 될 때까지 반복 보정.
  - 검증: 로컬 서버 + claude-in-chrome으로 히어로 버튼 5개를 반복 클릭해 실제 카드 콘텐츠가 무작위로 노출되는지, 32개 설명이 전부 3줄로 렌더되는지, HTML 태그 밸런스까지 직접 확인 완료.
- `report/task & bug/task-01.md` 확인 — "회사 소개 페이지 제작, liveklass.com/service/consulting 참고, 상단 배너 이미지 크기 문의" 3개 항목. liveklass 페이지를 직접 방문해 구조(배너→서비스소개→고객사례→후기→컨설턴트 프로필→CTA→Footer) 파악하고 배너 이미지 크기(표시 1400×600px, 소스 2800×1200px 권장)를 답변 — 단, 실제 회사 정보(연혁·대표자·강사진 등)는 지어낼 수 없어 페이지 제작 자체는 사용자 확인 후 진행하기로 하고 우선 체크리스트에 반영.
- 사용자가 "배너+소개+CTA로 단순하게, CTA는 우측하단(워터마크 덮는 위치), 소개 문구는 신입 시니어 강조(시니어 강조)"로 범위 확정 → `src/about.html` 신규 제작.
  - 상단 배너는 처음엔 `index.html` `#hero-banner`와 동일한 다크 그라디언트+블롭 패턴(실제 사진 없어 placeholder, `aspect-[7/3]`=1400:600 고정)으로 제작, 카피 "신입이 아니라, 시니어를 키웁니다"에서 "시니어"만 `text-4xl sm:text-6xl` + 브랜드 그린으로 확대 강조. CTA(1:1 상담 신청)는 배너 우측 하단에 `absolute`로 겹쳐 배치 — 추후 실제 사진 삽입 시 우측 하단 워터마크를 덮도록 미리 고정한 위치.
  - 소개 섹션은 기존에 이미 확립된 브랜드 카피(메타 설명·`#philosophy` 섹션의 "신입 시니어로 성장")를 재사용해 새 사실을 지어내지 않았고, "시니어"가 등장하는 두 곳 모두 `<strong>`으로 강조.
  - GNB "회사 소개" 링크를 index.html 포함 전체 30개 페이지에서 "준비중" 배지 제거 후 실제 링크(`about.html`/`src/about.html`)로 일괄 활성화(Python 스크립트로 데스크톱/모바일 메뉴 두 군데씩 총 60곳 치환, 치환 실패 0건).
  - 검증: 신규/기존 30개 파일 전부 HTML 태그 밸런스 통과(기존에 있던 `lang-jv.html`의 `List<T>` 텍스트로 인한 파서 오탐 1건은 이번 변경과 무관한 사전 이슈로 확인, 미수정), 로컬 서버로 `about.html`/`index.html` 200 확인, claude-in-chrome으로 배너·CTA·소개 문단·Footer 렌더링과 index.html→about.html GNB 링크 이동을 직접 확인.
  - **직후 실사진 반영**: 페이지 제작 도중 사용자가 저장소에 `assets/image/bg-consult.png`(3168×1344, 8.9MB — 숲 전망 통유리 코워킹 공간에서 노트북 작업하는 인물 사진)를 직접 추가 — 원본 비율(2.36:1)이 미리 권장했던 7:3(2.33:1)과 거의 일치함을 확인. `sips`로 2400×1018 JPEG(quality 72, ~550KB)로 리사이즈·압축해 `assets/image/about-banner.jpg`로 저장하고 그라디언트 placeholder를 실제 사진으로 교체, 텍스트 가독성을 위해 좌측이 진한 `bg-gradient-to-r from-slate-950/85...` 스크림을 사진 위에 겹침. 사진을 실제로 열어보니 우측 하단에 반투명 스파클 워터마크가 있어 Python `PIL`로 크롭해 정확한 좌표(우측에서 약 7~9%, 하단에서 약 17~22%)를 확인한 뒤 CTA 버튼 위치(`right-6 bottom-14 sm:right-10 sm:bottom-20`)를 그에 맞춰 재조정 — claude-in-chrome으로 CTA가 워터마크를 실제로 덮는지 확대 스크린샷까지 확인 완료. 원본 PNG는 `assets/image/`에 그대로 보존(삭제하지 않음).
  - `report/task & bug/task-01.md`(체크리스트 갱신), `CLAUDE.md`(`src/about.html` 항목 신설 + 실사진 반영 내역 + GNB 활성화 내역), `docs/PRD.md`(§8 "회사 소개" 항목을 완료로 갱신), `docs/ARCHITECTURE.md`(페이지 표·GNB 페이지 수 29→30 갱신)에 반영.
- 사용자 지시 — "상단 '회사 소개' 메뉴로 연결해 줘": Footer "회사" 컬럼의 "소개" 링크가 26개 페이지에서 `../index.html`(또는 index.html은 `#`)로 남아있던 것을 발견해, 상단 GNB "회사 소개"와 같은 목적지(`about.html`/`src/about.html`)로 일괄 교체(about.html 자기 자신과 Footer 구조가 다른 graffiti*.html 3개는 제외).
- 사용자 지적 — "'회사 소개' 메뉴가 처음부터 focus 되어 있네?": GNB 활성화 당시 "회사 소개" 링크를 `text-white`(hover 조건 없이 항상 흰색)로 지정한 게 원인 — `<nav>` 밖(로고 옆)에 배치돼 있어 다른 nav 링크가 상속받는 회색 규칙을 못 받았는데, 회색 기본색을 따로 챙기지 않고 항상 흰색으로만 둬서 페이지 로드 직후부터 마치 포커스/hover 상태처럼 보였음. 30개 페이지 전부에서 데스크톱은 `text-slate-400 hover:text-white`, 모바일은 `hover:text-white`(부모 nav에서 회색 상속)로 정정해 다른 메뉴와 동일하게 평소엔 회색, hover 시에만 흰색이 되도록 수정.
- v0.30을 dev에 커밋·푸시(`bd3aa00`, main 병합은 별도 지시로 진행) — 회사 소개 페이지 신설 + GNB 활성화 전체.

## 2026-08-24
- 사용자가 about.html 소개 문구를 직접 두 차례 수정 지시: ① "신입인데 시니어로 키운다는 것은 실무에 준하는 경험과 경력을 훈련시킨다는 의미" — 배너 서브카피·소개 문단을 추상적 "시야" 표현에서 "실무 경험·경력 훈련"으로 정정. ② "키운다"보다 "성장시킨다"가 어울린다 — 헤드라인 "시니어를 키웁니다" → "시니어로 성장시킵니다".
- 사용자가 배너 CTA 문구를 "1:1 상담 신청"에서 "대학생 컨설팅 신청"으로 교체 지시(이 버튼만 예외, 링크는 동일한 상담 신청 폼 유지) — 이때부터 페이지 타깃이 "대학생 대상 코딩 컨설팅"으로 좁혀짐이 문구로 드러남.
- 사용자 지시 — "서브 텍스트는 제거해 줘": 배너 안의 서브 텍스트 단락 삭제, 헤드라인만 남김.
- v0.31을 dev에 커밋·푸시(`76da59e`) — 위 about.html 카피 정리 4건(실무 경험 강조/성장시킨다/대학생 컨설팅 신청/서브 텍스트 제거) 일괄 반영.
- 사용자가 about.html을 직접 미세 조정(서브 텍스트에 수동 `<br/>` 추가해 줄바꿈 위치 고정) — 확인 후 그대로 유지.
- 사용자 지시 — "배너 다음 레이어들은 수평 2분할(이미지 한쪽 + 메인/서브 텍스트 한쪽)로 4개 정도, liveklass 참고 페이지처럼, 일단 이미지·문구는 임시로 구성해서 레이어 구성된 모습부터 보자": liveklass.com/service/consulting을 다시 방문해 "데이터 기반 분석"(이미지 좌/텍스트 우) ↔ "회원 관리 CRM"(텍스트 좌/이미지 우) 교대 배치 패턴을 확인한 뒤, about.html에 동일 패턴의 임시 섹션 4개를 추가(`cover-art` 그라디언트 placeholder + "(임시)" 표기 가짜 카피 — 실전 프로젝트 커리큘럼/1:1 코드 리뷰/현직 개발자 멘토링/포트폴리오 완성). `.cover-art` CSS 규칙을 about.html에 복원(커리큘럼 템플릿에서 파생될 때 제거됐던 것). claude-in-chrome 세션의 실제 브라우저 뷰포트가 1024px(lg 브레이크포인트) 미만으로 고정되어 있어 `resize_window`로도 데스크톱 2단 배치를 직접 캡처하지 못해, JS로 `order` 값을 lg 브레이크포인트와 동일하게 임시 적용한 뒤 스크린샷으로 4개 행 전부 좌우 교대가 올바른지 확인.
- 사용자 버그 제보 — "모바일에서 GNB가 안 보이고, 우측 상단 메뉴 아이콘을 눌러도 아무것도 안 뜬다. '회사 소개'도 안 보인다": 원인은 FOUC 방지용 critical CSS `header #mobile-menu { display: none; }`(ID 선택자)가 `mobile-menu.js`의 `menu.classList.toggle('hidden', ...)` 토글과 무관하게 항상 우선 적용돼, 모바일 메뉴 버튼을 눌러 `hidden` 클래스를 제거해도 메뉴가 절대 열리지 않던 것 — 30개 페이지 전부(`index.html` 포함) 동일 코드가 중복 정의되어 있어 사이트 전체에 영향을 미친 버그. "회사 소개"는 모바일 화면에서 이 `#mobile-menu` 안(nav 첫 항목)에만 존재해 메뉴가 안 열리니 함께 안 보였던 것으로 확인. `header #mobile-menu.hidden { display: none; }`로 선택자를 수정(class가 있을 때만 숨김 → JS가 class를 제거하면 규칙이 더 이상 매칭되지 않아 정상적으로 펼쳐짐, 데스크톱은 `md:hidden` 유틸리티가 그대로 항상 숨김 처리)해 30개 파일 전부 일괄 반영. claude-in-chrome 확장이 연결되지 않아 실제 브라우저 재현·확인은 못 했고, HTML 태그 밸런스 검사(30개 파일 전부 통과)와 CSS 명세도 검토로 대체.
- v0.33을 dev에 커밋·푸시, main 병합 — 모바일 GNB(메뉴 버튼 클릭 시 메뉴가 열리지 않던 사이트 전역 버그) 수정.
- 사용자 버그 제보(아이폰 스크린샷 첨부) — "상단 배너를 보면 글씨랑 CTA 버튼이 겹쳐 보임": 원인은 배너 CTA가 `absolute right-6 bottom-14` 같은 고정 좌표로 배치돼 있는데, `aspect-[7/3]` 배너는 화면 폭이 좁아지면 높이도 함께 줄어드는 반면 타이틀은 세로 중앙 정렬된 고정 크기 2줄 텍스트라 좁은 화면에서 배너 높이 대비 텍스트 비중이 커지면서 CTA와 겹쳤던 것. `#about-banner-scene` 내부를 `flex flex-col`(텍스트 + CTA) 하나로 합치고 CTA에 `mt-auto self-end`를 적용해 항상 텍스트 아래 여백에 배치되도록 구조 변경(좌표 기반 → flow 기반, index.html 히어로 배너의 `mt-auto pt-10` 패턴과 동일한 해법) — 화면 폭에 관계없이 구조적으로 겹칠 수 없게 됨. claude-in-chrome 연결이 계속 끊긴 상태라, 로컬 서버 + 헤드리스 Chrome 스크린샷(`--window-size`가 500px 미만으로는 내려가지 않는 Chrome 자체 제약 확인 후 500px 폭에서)으로 겹침 해소를 확인.
- `CLAUDE.md`(`src/about.html`의 "CTA 버튼 위치" 항목에 모바일 겹침 버그·수정 내역 추가)에 반영.
- v0.34를 dev에 커밋·푸시, main 병합 — 배너 CTA/타이틀 모바일 겹침 버그 수정.
- `report/task & bug/bug-report-01.md` 확인 — Language·Web & WebApp 섹션(둘 다 5카드 정적 그리드)에 모바일용 swiper 버튼이 없다는 지적, 그리고 카드 5개가 모두 보일 때는 nav 버튼을 비활성화해야 한다는 지적 2건.
  - Language(`#lang`)·Web & WebApp(`#web`) 두 섹션을 `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5` 정적 그리드에서 Algorithm의 "고정 개수 Swiper" 패턴(화면 폭별 slidesPerView 2/3/5, slidesPerGroup 동일, nav 화살표 + pagination)으로 전환하고 `FIXED_COUNT_SWIPER_IDS`에 `lang-swiper`/`web-swiper` 추가.
  - Swiper 공통 옵션에 `watchOverflow: true` 추가 — 슬라이드 수가 화면에 보이는 개수 이하면 Swiper가 자동으로 nextEl/prevEl에 `swiper-button-disabled` 클래스를 부여, `.nav-arrow.swiper-button-disabled { opacity:.35; cursor:not-allowed; pointer-events:none }` CSS로 시각적으로도 비활성화. 전역 옵션이라 Unity/Agent AI(카드 4개 고정)도 화면이 넓어 4장이 모두 보이면 nav가 함께 비활성화됨(의도된 부수 효과).
  - 검증: HTML 태그 밸런스 통과, claude-in-chrome 연결이 여전히 안 돼 헤드리스 Chrome(`--headless=new --screenshot`)으로 대체 확인 — 1440px에서 Language·Web & WebApp 5장 전부 노출 시 nav 회색 비활성화, 500px(Chrome headless 최소 창 폭 제약으로 이보다 좁게는 안 됨)에서 2장만 보이고 nav 활성화 + pagination 3점 정상 확인.
  - `report/task & bug/bug-report-01.md`(체크리스트 완료 표시), `CLAUDE.md`(Algorithm/Web & WebApp 섹션 항목에 반영 내역 추가)에 반영.
- v0.35를 dev에 커밋·푸시, main 병합(`612af75`) — Language/Web & WebApp 섹션 swiper 버튼 및 nav 비활성화 버그 수정.
- 사용자 재제보(두 번째 아이폰 스크린샷) — "여전히 문제가 있어 보인다"(배너 CTA가 하단에서 잘려 보임) + "첫번째 레이어 타이틀은 2줄로 삐져나오고 있다"(배너 다음 h2 "AI 시대, 진짜 대학생활을 위한 코딩 컨설팅"이 "컨설"+"팅"으로 단어 중간이 끊겨 2번째 줄에 "팅" 한 글자만 남음).
  - 배너 CTA: v0.34에서 겹침은 없앴지만 배너가 여전히 `aspect-[7/3]`(모바일 높이 ~163px)라 텍스트+CTA+패딩을 다 담기엔 공간 자체가 부족해 CTA가 배너 바닥 아래로 밀려나 `overflow-hidden`에 잘려 있었음 — `aspect-[7/3]` → `aspect-[4/3] sm:aspect-[7/3]`로 640px 미만에서만 배너를 더 세로로 길게 만들어 여유 공간 확보(`sm:` 이상은 기존 그대로 유지, 데스크톱 렌더링 영향 없음).
  - 타이틀 줄바꿈: h2에 `break-keep`(`word-break: keep-all`) 추가 — 전체를 한 줄로 강제하는 대신, wrap이 일어나도 항상 단어 경계에서만 끊기도록 수정(2줄 자체는 허용).
  - 검증: HTML 태그 밸런스 통과, claude-in-chrome 연결 안 됨 → 헤드리스 Chrome(`--window-size=500,1100`/`700,500`/`1440,900`)으로 모바일·sm 경계·데스크톱 3구간 전부 스크린샷 확인 — 500px에서 CTA 전체 노출(잘림 없음)·타이틀 1줄, 700px·1440px 데스크톱은 기존과 동일하게 렌더링됨을 확인.
  - `CLAUDE.md`(배너 CTA 위치 항목에 후속 버그 기록, 배너 다음 레이어 항목에 줄바꿈 버그 기록)에 반영.
- v0.36을 dev에 커밋·푸시, main 병합 — 배너 CTA 하단 잘림 + 타이틀 단어 중간 줄바꿈 버그 수정.
- 사용자 재지적 — "첫번째 레이어 타이틀이 2줄로 아직 보임. 2,3,4번째 레이어에서는 폰트 크기를 줄여서 한 줄로 잘 구성되고 있음, 같은 방식으로 처리하면 될 듯": v0.36의 `break-keep`은 단어 중간 끊김만 막았을 뿐 2줄 wrap 자체는 여전히 허용했던 것이 문제로 재확인됨 — 이미지+텍스트 레이어 4종(`whitespace-nowrap` + 반응형 폰트 축소로 항상 1줄 유지)과 동일한 방식으로 통일해달라는 요청.
  - 격리된 헤드리스 Chrome 테스트로 실제 필요 폭을 정밀 측정: `text-xl`(20px) 자연 폭 320px, `text-2xl`(24px) 자연 폭 384px. 모바일 컨테이너 폭(iPhone 428pt→380px, 390pt→342px, 375pt→327px, 전부 `px-6` 패딩 제외 기준)이 384px보다는 좁고 320px보다는 넓다는 걸 확인 — 기존 `text-2xl sm:text-4xl`이 딱 이 구간(384px 필요, 327~380px 제공)에서 항상 부족해 wrap이 강제되고 있었던 게 근본 원인.
  - `text-2xl sm:text-4xl` → `text-xl sm:text-4xl` + `whitespace-nowrap`으로 교체(`break-keep`은 nowrap과 중복이라 제거). 로컬 서버 + 헤드리스 Chrome 500px 스크린샷으로 한 줄 렌더링 확인.
  - `CLAUDE.md`(배너 다음 레이어 항목의 줄바꿈 버그 기록을 이번 최종 수정 내용으로 갱신)에 반영.
- v0.37을 dev에 커밋·푸시, main 병합 — 배너 다음 타이틀 2줄 wrap 버그 최종 수정(nowrap + 반응형 폰트 축소로 통일).
- 사용자 지시 — "배포된 페이지 확인해줘": 지금까지 모바일 검증에 쓰던 claude-in-chrome `resize_window`/헤드리스 Chrome `--window-size`가 이 환경에서 폭 500px 밑으로 안 내려가는 하드 플로어가 있다는 걸 재확인(둘 다 시도 → 요청값 무관하게 500px 고정) — 처음으로 Chrome을 `--remote-debugging-port`로 직접 띄우고 CDP `Emulation.setDeviceMetricsOverride`로 진짜 390px 모바일 뷰포트를 강제해 검증하는 방법을 도입.
  - 이 방법으로 배포된 페이지를 확인하다가, 500px 테스트에서는 가려져 있던 새 버그 2건을 발견: 배너 타이틀 h1("신입이 아니라, 시니어로 성장시킵니다.")이 390px에서 의도한 2줄을 넘어 3번째 줄로 흘러넘치며 "성장시킵니"+"다."로 단어 중간이 끊기고, 배너 다음 서브 텍스트 p도 "컨설"+"팅합니다"로 같은 방식으로 끊김.
  - h1은 두 가지 폰트 크기(기본 + "시니어" 강조 span)가 섞여 있어 h2처럼 폰트 축소로 한 줄에 다 넣기엔 무리라 판단, h1·p 둘 다 `break-keep`만 추가해 줄바꿈은 허용하되 단어 경계에서만 끊기도록 수정(h1은 결과적으로 3줄이지만 깨끗하게 끊김). CTA는 v0.36에서 확보한 `aspect-[4/3]` 여유 공간 덕분에 h1이 3줄이 되어도 겹침·잘림 없음을 CDP로 재확인.
  - `CLAUDE.md`에 CDP 검증 방법(개발 명령 섹션 신설)과 이번 버그 내역(about.html 항목)을 반영.
- v0.38을 dev에 커밋·푸시, main 병합 — 배너 타이틀/서브 텍스트 진짜 모바일 폭(390px)에서의 단어 중간 줄바꿈 버그 수정.

## 2026-08-25
- 사용자 질문 — "GNB에서 studio에 URL 연결할 수 있지?": 가능함을 확인(단순 `href="#"` + `aria-disabled` + "준비중" 뱃지 구조라 다른 활성 링크와 동일한 패턴으로 교체하면 됨).
- 사용자 지시 — "studio는 https://recodemate-mjrkgnh2vq-du.a.run.app/ 로 연결해줘": 30개 페이지(GNB를 공유하는 전체 페이지) 전부의 데스크톱·모바일 nav에서 studio 링크를 "준비중" 비활성 링크에서 `target="_blank" rel="noopener"` 외부 활성 링크로 전환(정규식 기반 일괄 치환, 60곳=2×30). LMS/business는 여전히 "준비중" 상태로 유지.
- `CLAUDE.md`(GNB 항목)에 studio 활성화 내역 반영.
- 사용자 지시 — "GNB 우측 상단에 'studio' 메뉴 링크를 'mate.recode.ai.kr'로 변경해 줘": 커스텀 도메인 연결 완료로 studio 링크를 Cloud Run 기본 URL(`https://recodemate-mjrkgnh2vq-du.a.run.app/`)에서 `https://mate.recode.ai.kr`로 교체(30개 페이지 60곳 일괄 치환, `target="_blank" rel="noopener"` 패턴은 그대로 유지). `CLAUDE.md`(GNB 항목)에 반영.
- v0.40을 dev에 커밋·푸시 후 main에 fast-forward 병합·푸시(사용자가 "main 브랜치 병합도 해 줘"로 명시 요청) — studio 링크 커스텀 도메인 변경 배포. GitHub Pages 빌드 완료(`built`) 확인 후 `https://recode.ai.kr/`에서 신규 링크 반영·구 URL 잔존 없음을 curl로 확인, 이어서 CDP(`Emulation.setDeviceMetricsOverride`, 390px)로 실제 모바일 뷰포트에서도 모바일 메뉴의 studio 링크가 정상 동작함을 스크린샷으로 재확인.
- 사용자 지시 — "local 서버에서 자동 구동 시킬 수 있는 bash 스크립트 제작해 줘. 이름은 'start.sh'": 저장소 루트에 `start.sh` 신설 — 실행 위치와 무관하게 스크립트 자신의 경로 기준으로 저장소 루트로 이동해 `python3 -m http.server`를 구동(기본 포트 8765, 인자로 변경 가능), `lsof`로 포트 사용 여부를 먼저 확인해 이미 사용 중이면 안내 후 종료. 다른 디렉터리에서 실행·포트 충돌 시 동작 모두 확인. `CLAUDE.md`(개발 명령 섹션)에 반영.
- 사용자 지시 — "자동으로 크롬으로 실행되도록 해 줘": `start.sh`가 서버를 백그라운드로 띄운 뒤 실제 응답이 올 때까지 폴링, 준비되면 `open -a "Google Chrome"`으로 `index.html`을 자동으로 열도록 수정(Linux 대비 `google-chrome`/`xdg-open` 폴백, 스크립트 종료 시 `trap`으로 서버 프로세스도 함께 정리). 실제로 Chrome 새 탭이 열려 이미지·JS까지 정상 로드되는 것을 확인. `CLAUDE.md`(개발 명령 섹션)에 반영.
- 사용자 지시 — "'../business' 폴더 확인되지? 해당 프로젝트에서 favicon을 제작했어. 확인해서 현재 프로젝트에서도 반영시켜줘": 형제 저장소 `../business`(`recode-ai/business`)의 `public/favicon.ico`·`public/apple-touch-icon.png`(코드 브래킷 `<>` 아이콘, 브랜드 그린 원형 배경)를 확인 — 이 저장소엔 이전까지 favicon이 전혀 없었음. 두 파일을 `assets/image/favicon.ico`·`assets/image/apple-touch-icon.png`로 복사하고, GNB를 공유하는 30개 페이지 전부의 `<head>`에 `<link rel="icon">`/`<link rel="apple-touch-icon">`을 추가(캐시 무효화용 `?v=1` 쿼리 포함, business의 관례를 그대로 따름). 로컬 서버로 파일 서빙·페이지별 상대경로(루트 `assets/image/...` vs `src/` `../assets/image/...`) 정상 여부 확인. `CLAUDE.md`(저장소 관례 섹션)에 반영.
- (문서 점검 시 보완) 이 날의 커밋: v0.39(`52b6d76`, studio 링크 활성화) / v0.40(`004a782`, studio 커스텀 도메인 `mate.recode.ai.kr`) / v0.41(`d2d9b53`, `start.sh` 추가) / v0.42(`ad450cd`, favicon) — 전부 main 병합·배포 완료.

## 2026-08-26
- 사용자 지시 — "'../business' 프로젝트에서 HTML 파일 하나씩 + 공통부 분리 작업 확인하고, 현재 프로젝트에서도 적용 여부 검토해 줘": `../business`의 `docs/ARCHITECTURE.md` D9("프로젝트 상세는 항목당 정적 HTML 한 장, 공통부는 `assets/`로 분리")를 확인 — 빌드 도구 없이 정적 HTML N장을 만들되, 페이지마다 복제되는 Tailwind 설정과 커스텀 CSS만 `public/assets/js/tailwind-config.js`·`public/assets/css/base.css`로 빼서 `<script src>`/`<link>`로 참조하는 패턴(브랜드 바·푸터 마크업 자체는 여전히 페이지마다 복제 — 빌드 도구 없이는 불가피하다고 인정하고 20장 넘으면 SSG 재검토하기로 남겨둠). 이 저장소의 30개 페이지를 md5로 비교해 Tailwind config 블록(~40줄)과 GNB critical CSS 블록(~14줄)이 **전부 byte-identical**임을 확인 — 정확히 같은 문제 상황.
- 사용자 승인("진행하자") 후 적용: `assets/js/tailwind-config.js`·`assets/css/base.css` 신설(명명도 business와 동일하게 맞춤), Python 스크립트로 30개 파일 전부에서 두 블록을 exact-match 치환(치환 전 정확히 1회씩 매치하는지 assert로 검증) — `<script src="/assets/js/tailwind-config.js"></script>`·`<link rel="stylesheet" href="/assets/css/base.css" />`로 교체. 절대경로(`/assets/...`)를 택한 이유는 `CNAME`이 `recode.ai.kr` 루트라 `index.html`과 `src/*.html` 양쪽에서 상대경로 구분(`assets/...` vs `../assets/...`) 없이 동일하게 동작하기 때문(로컬 `python3 -m http.server`도 저장소 루트가 서버 루트라 동일).
- 검증: 치환 후 인라인 `tailwind.config`/`@view-transition` 잔존 0건, 두 신규 파일 참조 30/30 확인. 로컬 서버 기동 후 `curl`로 `index.html`·`assets/css/base.css`·`assets/js/tailwind-config.js`·`src/about.html` 전부 200 확인. claude-in-chrome으로 `index.html`·`src/algo-bst.html` 실제 렌더링(GNB 다크 헤더·CTA 알약 버튼·brand 컬러 카드) 정상, 콘솔 에러 0건 확인.
- `CLAUDE.md`(페이지 구조 도입부, GNB 항목의 "새 페이지 만들 때 복사할 것" 지시, `assets/` 목록)와 `docs/ARCHITECTURE.md`(§3 도입부)를 새 참조 방식에 맞춰 갱신. 겸사겸사 `CLAUDE.md`의 "`../business`는 별도 Next.js 프로젝트" 표기가 틀렸음을 확인(실제로는 이 저장소와 동일하게 빌드 도구 없는 정적 사이트, Firebase Hosting 배포)해 정정.
- 사용자 지시 — "GNB의 business 항목: '준비중' 배지 제거 → https://business-1e563.web.app 로 새 탭 링크(target=\"_blank\" rel=\"noopener\"), 데스크톱 nav와 모바일 메뉴 두 곳 모두, 9개 페이지에 GNB가 복제돼 있으므로 전부 반영": studio 활성화(2026-08-25) 때와 동일한 패턴으로 "준비중" `<span>` 배지 + `aria-disabled`/`onclick="event.preventDefault()"`를 제거하고 외부 활성 링크로 전환. GNB가 실제로는 9개가 아니라 **30개 페이지**(Algorithm/Web 코스·about.html 추가로 늘어남)에 복제돼 있음을 확인해 30개 전부에 반영(데스크톱+모바일 60곳, exact-match 치환 전 전부 동일 마크업임을 md5로 검증). 브라우저로 실제 클릭 시 `business-1e563.web.app`(`../business`의 Firebase Hosting 기본 도메인) 새 탭이 정상적으로 열림을 확인. `CLAUDE.md`(GNB 항목)·`docs/ARCHITECTURE.md`(서브도메인 상태 표·mermaid 다이어그램 — 겸사겸사 마찬가지로 누락돼 있던 studio 활성화 상태도 함께 정정)에 반영.
- 사용자 지시 — "랜딩 페이지 좌상단 로고 이미지 교체해 줘. 리소스 필요하나? ../business 폴더에서 탐색해 봐": `../business`의 `public/logo.png`(96×96, 투명 배경, 브랜드 그린 원형 + 흰 `<>` 마크)를 확인 — 2026-08-25 favicon/apple-touch-icon으로 이미 들여온 것과 같은 마크의 투명 배경 버전. `assets/image/logo.png`로 복사하고, 헤더 로고 자리의 `<span class="... rounded-lg bg-brand-500/15 ...">R/</span>` 텍스트 배지를 `<img src="assets/image/logo.png" alt="Recode Coding" class="h-8 w-8" />`(`src/` 페이지는 `../assets/image/logo.png`)로 교체 — GNB가 30개 페이지에 복제돼 있으므로 전부 반영(치환 전 30개 파일 전부 배지 마크업이 동일함을 확인). `assets/css/base.css`의 `header span.rounded-lg{...}`(옛 배지용 critical CSS)는 매칭 대상이 없어져 제거. 로컬 서버 + claude-in-chrome으로 `index.html`·`src/about.html` 양쪽 헤더에 로고 이미지가 정상 렌더링되고 콘솔 에러 없음을 확인 — 이제 브라우저 탭 아이콘과 헤더 로고가 같은 이미지를 쓴다. `CLAUDE.md`(GNB 항목)에 반영.
- 사용자 질문 — "recode.ai.kr 이라고 카톡을 보내면 카톡 상에서 자동으로 보이는 것들이 있지? 그걸 뭐라고 하지?": Open Graph(OG) 태그 기반 링크 미리보기(=링크 카드)임을 설명, 이 저장소엔 `og:*` 태그가 전혀 없어 카톡 공유 시 미리보기가 빈약할 것이라는 점을 확인해줌.
- 사용자 지시 — "응, 추가해줘": 30개 페이지 전부의 `<head>`에 `og:type`/`og:site_name`/`og:locale`/`og:url`/`og:title`/`og:description`/`og:image` 태그를 Python 스크립트로 일괄 삽입 — `og:title`/`og:description`은 새로 쓰지 않고 각 페이지의 기존 `<title>`/`<meta name="description">`을 그대로 재사용, `og:url`은 실제 배포 도메인(`https://recode.ai.kr/...`) 기준 절대경로로 하드코딩, `og:image`는 30개 페이지 공통으로 `assets/image/logo.png`(96×96) 재사용. 삽입 직후 생긴 중복 빈 줄을 정리하는 후처리 스크립트도 실행. 검증: `og:title` 개수 30/30, escaping 이상 없음, `html.parser`로 30개 파일 전부 파싱 오류 없음 확인. `CLAUDE.md`(저장소 관례 섹션)에 반영.
- 사용자 — v0.46 배포 후 실제 카카오톡에 `recode.ai.kr` 공유 테스트, "미리보기에 favicon만 등장하는데, 정상?": 큰 이미지 카드 없이 favicon만 뜨는 건 비정상 — 원인으로 카카오의 URL별 미리보기 캐시(OG 추가 이전 캐싱 가능성)와 `og:image`(96×96)가 카카오 최소 이미지 크기 기준을 못 채웠을 가능성 두 가지를 안내(robots.txt·응답 헤더는 크롤러 차단 요소 없음을 확인해 배제). 카카오 공식 "공유 디버거"(`developers.kakao.com/tool/debugger/sharing`)는 카카오 개발자 로그인이 필요해 직접 캐시를 초기화·재크롤링 확인하는 건 사용자 몫으로 안내, 근본 해결로 og:image를 800×400 이상 전용 이미지로 교체할 것을 제안.
- 사용자 — "이미지는 내가 준비해?" → "일단 너가 작업해서 수정해 봐": 브랜드 컬러(`assets/js/tailwind-config.js`의 brand 팔레트)와 기존 로고(`assets/image/logo.png`)를 활용해 Python Pillow로 `assets/image/og-image.png`(1200×630, OG 표준 권장 크기)를 신규 제작 — 히어로 배너와 동일한 다크 배경(`#0c0c0f`)+브랜드 그린 블러 블롭 2개, 로고를 132×132로 업스케일해 좌상단에 배치, mono 폰트(Menlo)로 "RECODE CODING" 라벨, 흰색 볼드 "Recode Coding" 타이틀 + 브랜드 그린 태그라인("AI시대, 신입 시니어를 훈련하다") + 하단 mono URL로 구성(한글은 `AppleSDGothicNeo.ttc` Bold/SemiBold 페이스로 렌더링). 30개 페이지의 `og:image`를 이 파일로 교체하고 `og:image:width`(1200)/`og:image:height`(630) 태그도 추가. 로컬 서버로 파일 서빙·HTML 파싱 이상 없음 확인, 사용자에게 생성된 이미지 파일 전달. `CLAUDE.md`(Open Graph 항목)에 반영.
- v0.47을 dev에 커밋·푸시 후 main에 fast-forward 병합·푸시 — 전용 OG 이미지 배포. GitHub Pages 빌드 완료(`built`) 확인 후 `https://recode.ai.kr/`에서 새 og:image·og:image:width/height·이미지 파일(200) 전부 정상 확인.
- 사용자 — 카카오톡에 원래 URL(`recode.ai.kr`, 쿼리 없음)로 재공유해도 여전히 새 이미지가 안 뜨는 것을 재보고, "그럼, 카카오쪽에서의 캐시는 어떻게 지우니?": `?v=2` 같은 더미 쿼리로 테스트해보니 새 이미지가 뜨는 것을 사용자가 직접 확인 — 순수 카카오 URL 캐시 문제로 확정. 카카오 공식 "공유 디버거"(`developers.kakao.com/tool/debugger/sharing`, 카카오 계정 로그인 후 URL 입력 → "초기화")로 캐시를 강제 초기화하는 방법을 안내(로그인 필요해 사용자가 직접 진행).
- 사용자 지시 — "GNB가 두줄로 보이는 모바일 단말들이 있던데.. 갤럭시 폴더 7 같은 단말기.. 일단 확인만 좀 해 줘": 배포된 `recode.ai.kr`을 CDP로 260~390px 폭에서 재현 시도했으나 헤더 바·모바일 드롭다운 메뉴 모두 wrap 없이 정상 — 좁은 폭 문제가 아님을 확인, 사용자에게 실제 기기 스크린샷을 요청.
- 사용자가 `capture.jpeg`(갤럭시 Z 폴드7 펼친 화면 스크린샷) 첨부, "스크린샷 분석": 분석 결과 이건 모바일 햄버거 메뉴가 아니라 **데스크톱 GNB(nav)가 그대로 렌더링**되며 "회사 소개"/"원장님의 낙서"/"LMS 준비중"/"1:1 상담 신청" 등 각 항목의 텍스트가 개별적으로 두 줄 줄바꿈된 상태임을 확인. 원인: 폴드7 펼친 화면은 `md`(768px) 브레이크포인트는 넘지만 데스크톱 nav 전체를 한 줄에 담기엔 폭이 부족한 "애매한 중간 폭"이라, flexbox가 플렉스 아이템(각 링크)을 min-content(=가장 긴 단어 폭)까지 줄이면서 텍스트 내부 줄바꿈이 발생. `lg`(1024px)로 브레이크포인트를 올리는 방향을 제안, 사용자 승인("그 방향으로 진행해줘") 후 적용.
- 적용: GNB 관련 4개 클래스(`hidden md:inline-flex`/`hidden md:flex`/`md:hidden`/`hidden md:hidden`)를 각각 `lg:`로 30개 페이지 전부 일괄 치환(치환 전 4개 패턴이 30개 파일 전부에 정확히 1회씩 존재함을 확인), `assets/css/base.css`의 깜빡임 방지 미디어쿼리도 `768px`→`1024px`로 수정. 로컬 서버+CDP로 390/767/800/900/950/1023/1024/1200px 전 구간 재검증 — 768~1023px 구간(폴드7 대역)은 햄버거 메뉴만 표시(줄바꿈 사라짐), 1024px 이상은 데스크톱 nav가 한 줄로 정상 표시됨을 확인. `html.parser`로 30개 파일 파싱 오류 없음 확인. `CLAUDE.md`(GNB 항목)에 반영. 사용자 요청으로 분석에 쓰인 `capture.jpeg`는 작업 완료 후 삭제.
- 사용자 지시 — "path를 dev/recode-ai/home 에서 recode-ai/home 으로 옮겼어. 확인하고, 예전 path의 memory도 삭제해 줘": 저장소 자체는 절대경로 하드코딩이 없어(HTML/JS/CSS 전부 상대경로 또는 `/assets/...` 절대참조, `start.sh`는 `BASH_SOURCE` 기준) 이동만으로 깨진 것이 없음을 확인 — git remote·브랜치·워킹트리 정상, 로컬 서버로 `index.html`·`base.css`·`tailwind-config.js`·`src/about.html`·`og-image.png` 전부 200 확인. 사용자 확인 후 옛 경로의 Claude Code 프로젝트 디렉터리(`~/.claude/projects/-Users-won-dev-recode-ai-home/`, 세션 트랜스크립트 포함 105MB)를 삭제 — 이 시점에 `home`만 옮겨져 있어 형제 저장소 `../business`가 일시적으로 존재하지 않는 상태였다.
- 사용자 지시 — "../business repo도 이동해서 옮겨놓았어. 확인하고, 관련 내용 업데이트 할 것들이 있으면 업데이트 해 줘": `business`도 `~/recode-ai/business`로 이동해 **`../business` 상대 참조가 다시 유효**함을 확인(`CLAUDE.md`·`docs/ARCHITECTURE.md`·`docs/DEVLOG.md`의 기존 `../business` 표기는 그대로 맞으므로 수정 불필요). `wonoj`만 `~/dev/recode-ai/wonoj`에 남아 있어 형제가 아니다. 남은 잔재는 **`~/.claude.json`이 경로를 키로 들고 있는 상태** 두 곳 — `projects["/Users/won/dev/recode-ai/home"]`(사문화된 신뢰 설정)과 `githubRepoPaths["re8code/home"]`의 옛 경로. Claude Code는 이 파일을 메모리에 들고 있다가 종료 시 되쓰므로 실행 중 편집하면 조용히 사라진다 — `~/cleanup-old-home-path.sh`(저장소 밖에 둠, claude 프로세스가 살아 있으면 중단·타임스탬프 백업·임시 파일 검증 후 `os.replace` 원자적 쓰기·멱등)를 만들어두고 실행은 Claude Code 종료 후로 미룸. 스크립트에 박힌 파이썬을 `.claude.json` **사본**으로 실제 실행해 검증(원본 mtime 불변, 최상위 키 62개 개수·순서 보존, 새 home 프로젝트 값 완전 동일, 그 외 최상위 값 무변경, 옛 경로 잔존 0건, 재실행 시 NOCHANGE).
- v0.49를 dev에 커밋·푸시 — 저장소 경로 이동(`~/dev/recode-ai/home` → `~/recode-ai/home`) 확인 및 기록. 사이트 산출물 변경은 없어 배포 영향 없음(문서만 변경).
- (문서 점검 시 보완) 이 날의 커밋: v0.43(`1bea3af`, 공통 자산 분리) / v0.44(`7e5ce1b`, business 링크) / v0.45(`2d22453`, 헤더 로고 이미지) / v0.46(`7385b9d`, OG 태그) / v0.47(`6835892`, 전용 OG 이미지) / v0.48(`d3b1b9e`, GNB 브레이크포인트 `lg` 상향) — v0.48까지 main 병합·배포 완료, **v0.49(`55001df`, 문서만)는 dev에만 있고 main 미병합**.

## 2026-08-28
- 사용자 지시 — "장비 이동해서 pull 했어. 전체 프로젝트 내용 숙지하고, 모든 문서들 점검 & 체크해 줘": 새 장비(`/Users/Wons/recode-ai/home`)에서 저장소 전체를 다시 파악하고 5개 문서(`CLAUDE.md`/`docs` 4종)를 코드 실물과 대조 점검했다.
  - 코드 대조 결과 이상 없음: 30개 페이지 전부가 `tailwind-config.js`·`base.css`·favicon·apple-touch-icon·OG 7종·`og-image.png`·헤더 로고 이미지·`mobile-menu.js`·GNB `lg:` 클래스·`main pt-16`을 갖고 있고(30/30), 인라인 `tailwind.config`·`md:` GNB 클래스 잔존 0건, 내부 링크(href/src) 깨짐 0건, `#lang`/`#algo`/`#ds`/`#web` 카드 24장 전부 `<a>`(Unity·Agent AI만 비클릭 `<div>` 8장), `src/algo-sort.html` 미참조(미사용) 확인. 로컬 서버로 주요 경로 200 확인 — 장비 이동으로 깨진 것 없음.
  - 문서 수정: `PRD.md`(`NEED.md`→`../tasks/Philosophy.md` 참조 정정, In Scope 표에 Algorithm·Web & WebApp·회사 소개·Unity/Agent AI 행 추가, 낙서장 상태 명시, §7 Tailwind config 공통화 결정 반영, §8 서브도메인 진행 상황), `DEV_PLAN.md`(같은 참조 정정, §1에 공통 자산 분리 결정 신설·페이지 수 30개로 현행화, Phase 0/2/2.6 상태 표기, Phase 3 3/4 완료로 갱신), `ARCHITECTURE.md`(최종 갱신일, mermaid에 algo/web/about 노드, index 행의 "자료구조 준비중" 등 낡은 서술 정정, about 행을 현재 구성(레이어 4종·CTA flow 배치·성장시킵니다 카피)으로 갱신, GNB 항목에 로고 이미지·`lg` 브레이크포인트·"준비중은 LMS만" 반영, assets·공통 head 요소 정리), `DEVLOG.md`(누락돼 있던 `## 2026-08-24` 섹션 헤딩과 v0.35·v0.39~v0.48 커밋 기록 보완).
  - 확인된 미해결 사항: ① `dev`가 `main`보다 1커밋 앞섬(v0.49, 문서만) — 병합은 사용자 지시 대기. ② `assets/image/`의 원본 PNG 5장(~42MB)이 어느 페이지에서도 참조되지 않은 채 저장소에 남아 있음(재크롭 소스로 보존 중, 이미 git 이력에 있어 지금 지워도 클론 용량은 안 줄어듦). ③ GNB "준비중"은 LMS 하나만 남음. ④ Unity·Agent AI 코스는 카드만 있고 상세 페이지 미착수.
- 사용자가 점검 도중 `CLAUDE.md`를 직접 수정 — 상단 영문 안내문 1줄을 걷어내고 규칙 4종을 신설: **제1원칙**(작업 전후로 5개 문서를 검토·갱신, `ARCHITECTURE.md` 상단에는 기술 스택을 기록) / **git commit 규칙**(`vX.XX: ` 접두사, minor 1씩 증가, 작업 완료 시마다 commit+push, `dev` 브랜치 기본) / **여러 장비 간 작업 연속성**(저장소 문서가 1순위 정보원, Claude 메모리는 보조) / **작업 보고서**(`report/YYYY-MM-DD-<slug>.md`에 체크리스트+요약).
  - 신규 규칙 반영: `docs/ARCHITECTURE.md`에 **§1 "기술 스택" 절 신설**(마크업/스타일/폰트/스크립트/UI 라이브러리/백엔드/호스팅/로컬 개발 8행 표 — Tailwind Play CDN, Pretendard Variable v1.3.9, Swiper 11, AOS 2.3.1, Firebase JS SDK 10.12.2 등 실제 참조 버전을 코드에서 확인해 기재)하고 기존 §1~§6을 §2~§7로 밀면서 내부 상호참조(§3/§4)도 함께 조정, §7 갱신 원칙에 "기술 스택은 §1에 유지" 항목 추가.
  - 이번 점검 작업의 보고서를 `report/2026-08-28-docs-audit.md`로 작성(새 규칙에 따른 첫 보고서). `report/`의 직전 파일이 2026-08-23이라 v0.30~v0.49 구간은 보고서가 비어 있는데, 상세 경위가 이 DEVLOG에 남아 있어 소급 작성은 하지 않고 이후 작업부터 관례를 유지하기로 함.
  - 이 장비의 Claude 개인 메모리는 비어 있음을 확인 — "저장소가 1순위 정보원" 규칙과 어긋나는 메모리 전용 정보는 없다.
- 사용자 지시("main 브랜치 병합도 해 줘") — v0.50을 `dev`에 커밋·푸시한 뒤 `main`에 fast-forward 병합·푸시(`d3b1b9e`→`4d921f0`, v0.49·v0.50 두 커밋이 함께 올라감). 둘 다 문서만 변경이라 사이트 산출물 변화는 없다. GitHub Pages 빌드 `built` 확인, `https://recode.ai.kr/`·`/src/about.html` 200 확인.

## 2026-08-29
- 장비 경로 이동 마무리 — `home`이 `~/dev/recode-ai/home` → `~/recode-ai/home`으로, 이어서 `business`도 `~/recode-ai/business`로 옮겨져 `../business` 상대 참조가 다시 유효함을 확인(저장소 문서의 `../business` 표기는 수정 불필요). 저장소 자체는 절대경로 하드코딩이 없어(상대경로·`/assets/...` 절대참조·`start.sh`의 `BASH_SOURCE`) 이동만으로 깨진 것 없음 — 로컬 서버로 주요 경로 200 확인. 경로를 키로 보관되던 Claude Code 로컬 상태(옛 세션 기록·`~/.claude.json`·입력 이력·파일 스냅샷)는 저장소 밖에서 정리. 상세: `report/2026-08-29-repo-path-move-cleanup.md`.
- 사용자가 `CLAUDE.md`를 직접 수정 — 규칙 2종 추가: **ADR**(`ARCHITECTURE.md` 마지막 항목에 Architecture Decision Record를 기록) / **버전 표기**(minor가 100을 넘으면 `vX.XXX` 양식으로 전환).
- **v0.52** — 위 ADR 규칙에 따라 `docs/ARCHITECTURE.md`에 **§8 ADR 절 신설**. 사용자 지시("ADR은 추가로 적용되는 사항이 있으면 기록하도록")에 따라 **과거 결정을 소급해 채우지 않고 자리와 기록 규약만** 정함 — 표기법은 형제 저장소 `../business`의 `### D<번호>.` + 맥락/결정/트레이드오프 형식을 그대로 따르고, 번호는 다음 결정부터 `D1`로 시작한다. §7 갱신 원칙에도 "새 아키텍처 결정은 §8에 추가한다" 한 줄 추가.
- **v0.53** — 사용자 지시("전체 문서 정합성 체크해 줘"): 5개 문서의 주장을 코드 실물과 기계적으로 전수 대조. 30개 페이지 공통 요소(공통 CSS/JS·favicon·OG 8종·로고·`mobile-menu.js`·`pt-16`) 30/30, GNB 브레이크포인트 `lg:` 30/30·`md:` 잔존 0, 내부 링크 705건 중 깨짐 0, 카드 연결(Language 5/정렬·탐색 8/자료구조 6/Web 5 = `<a>` 24장, Unity·Agent AI 8장 비클릭 `<div>`), CDN 라이브러리 버전 4종(Pretendard 1.3.9·Swiper 11·AOS 2.3.1·Firebase 10.12.2) 전부 §1 기술 스택 표와 일치, GNB 링크 상태가 §6 서브도메인 표와 일치(LMS만 준비중), 로컬 서버 11개 경로 200. **불일치 1건 수정** — §1 폰트 행이 "Pretendard·JetBrains Mono를 30개 페이지 전부 `<link>`로 로드"라고 적고 있었으나 실제로 웹폰트로 내려받는 것은 Pretendard뿐이고 JetBrains Mono는 `tailwind-config.js`의 `mono` 스택에 이름만 있어 미설치 기기에서는 시스템 모노스페이스로 폴백된다는 사실을 반영. 상세: `report/2026-08-29-docs-consistency-check.md`.
- **v0.54** — 사용자가 `CLAUDE.md`를 직접 수정해 규칙 추가: 문서 목록에 **`docs/CHANGE_DEVICE.md`(장비 간 작업 인수/인계 자동화)** 신설(문서 세트가 5개→6개), `ARCHITECTURE.md` 항목에 "ADR 작성" 명시, 제1원칙에 "장비 이동 시 `CHANGE_DEVICE.md` 프로토콜을 우선 진행" · "장비 이동 정리 후 반드시 전체 문서 정합성 확인" 2줄 추가.
  - 점검 결과 **`docs/CHANGE_DEVICE.md`가 0바이트 빈 파일**이라 규칙이 가리키는 프로토콜이 존재하지 않았고, `CLAUDE.md` 안의 문서 개수 표기 6곳이 5개/4개인 채로 남아 어긋나 있었음 — 개수 표기를 전부 6개(‧`docs/`는 5개)로 정정하고, 문서별 갱신 기준에 `CHANGE_DEVICE.md`·ADR 항목을 추가.
  - `docs/CHANGE_DEVICE.md` 신규 작성 — 2026-08-26 경로 이동을 실제로 수행하며 얻은 내용을 프로토콜로 정리(§0 핵심: 깨지는 건 저장소가 아니라 도구가 경로를 키로 들고 있는 상태 / §1 이동 전 / §2 저장소 무결성 / §3 경로 키 상태 6종 표·경로키 규칙 / §4 이관 자동화 스크립트 / §5 형제 저장소 / §6 실제로 당한 함정 6종 / §7 마무리 문서 정합성 확인). 스크립트는 가짜 `HOME`에 옛 경로 상태를 만들어 **실제 실행 검증** — 세션 디렉터리 이관, 신뢰 설정·allowedTools 승계, `githubRepoPaths` 정리, history `project` 이관, 사라진 저장소 기록 삭제, `display` 원문 보존, 고아 아티팩트 삭제, 무관 키 무변경, 재실행 시 `NOCHANGE` 전부 확인. 상세: `report/2026-08-29-change-device-protocol.md`.
- v0.52~v0.54를 `dev`에 커밋·푸시한 뒤 `main`에 fast-forward 병합·푸시(사용자 지시 "main 브랜치 병합도 해 줘", `7f03a76`→`6927a48`, 세 커밋이 함께 올라감). 전부 문서 변경이라 사이트 산출물 변화는 없음 — 배포 후 `https://recode.ai.kr/`의 `/`·`/index.html`·`/assets/css/base.css`·`/assets/js/tailwind-config.js`·`/src/about.html`·`/assets/image/og-image.png` 6개 경로 200, 배포본에 `hidden lg:flex`·`og-image.png` 반영 확인(`gh` CLI가 이 장비에 없어 Pages 빌드 상태 API 대신 실서비스 응답으로 확인).
- **v0.56** — 사용자 지시("`ARCHITECTURE.md` 기술 스택 상에서 계정이 필요한 항목과 비용 발생 개연성이 있는 항목들을 간단히 요약 정리"): `docs/ACCOUNT_COST.md` 신규 작성. 저장소의 외부 호스트를 전수 조사(참조 호스트 7종)하고 실서비스 응답 헤더·DNS로 실제 운영 주체를 확인 — 도메인 DNS는 닷홈(`ns1~3.dothome.co.kr`), 저장소는 public이라 Pages 무료, `oj`/`mate` 두 서브도메인은 `server: Google Frontend`(Cloud Run)로 **결제 계정 연결 필수**임을 확인. 필수 계정 3개(닷홈·GitHub·Google)와 과금 전환 조건(도메인 갱신만 확정 지출, Cloud Run이 개연성 1순위, Firebase는 Spark라 초과 시 과금이 아니라 차단, 저장소 비공개 전환 시 Pages 유료화)을 정리하고, Storage/Functions 미사용이라 **Blaze로 올릴 이유가 아직 없다**는 점도 명시. 요금제는 수시로 바뀌므로 **구체적 금액은 적지 않고 과금 전환 조건만** 유지하는 원칙을 문서에 못박음. `ARCHITECTURE.md` §1과 `CLAUDE.md` 저장소 관례에 상호참조 한 줄씩 추가(제1원칙 6개 문서에는 포함하지 않는 보조 참고 문서로 규정). 상세: `report/2026-08-29-account-cost-summary.md`.
- 사용자가 `docs/account-cost.md`를 **`docs/ACCOUNT_COST.md`로 개명**(다른 문서와 같은 대문자 규칙)하고 `CLAUDE.md` 제1원칙 목록에 직접 추가 — **문서 세트가 6개 → 7개**가 됨. 이에 맞춰 어긋난 표기를 정정: `CLAUDE.md`의 개수 표기(제1원칙 도입부 6→7, 장비 간 연속성 6→7, 작업 완료 후 점검 열거에 `ACCOUNT_COST.md` 추가·6→7, "6개 중"→"7개 중"), `docs/` 폴더 설명(5개→6개 주요 문서, "보조 참고 문서" 서술 제거), 문서별 갱신 기준에 `ACCOUNT_COST.md` 한 줄 추가, `ARCHITECTURE.md` §1과 `DEVLOG.md`·보고서의 파일명 참조.
- **v0.57** — 사용자 지시("language 들의 상세 페이지에서 첫번째 항목을 'Output' 관련 내용으로 채운다, console 출력 함수인 print 관련 함수들이다"): `src/lang-{cp,jv,py,js,dt}.html` 5개 페이지의 **1번 커리큘럼 카드를 출력(Output) 내용으로 교체**. 기존 1번은 "OT — {언어별 실행 모델}"(C 메모리 모델 / JVM 실행 모델 / 인터프리터 구조 / 이벤트 루프 / Flutter 렌더링 파이프라인)이었고, 이를 각 언어의 콘솔 출력 함수로 바꿨다 — C/C++ `printf` 서식 지정자와 `std::cout` 스트림 비교(`\n` vs `std::endl`의 버퍼 flush 차이, `stderr` 분리) / Java `println`·`print`·`printf` 차이와 `System.out`·`System.err` 분리, `String.format` / Python `print()`의 `sep`·`end`·`file`·`flush` 네 옵션과 f-string / JavaScript `console.log`·`error`·`warn`·`table`, 템플릿 리터럴, Node의 `process.stdout.write` / Dart `print()`·`stdout.write`(dart:io)와 Flutter의 `debugPrint()`(로그캣 잘림 방지). 카드 수 12개·번호 1~12·"전체 12건" 표기는 그대로 유지. 헤드리스 Chrome으로 5개 페이지 DOM을 덤프해 1번 카드의 제목/요약/상세 3단 구조와 아코디언 속성(`aria-expanded="false"`·`.curriculum-detail`)이 온전한지 확인하고, C/C++의 `&lt;&lt;` 이스케이프가 화면에 `<<`로 정상 렌더링되는 것까지 검증. 새 언어 페이지도 1번은 출력 함수로 채운다는 규칙을 `CLAUDE.md`에 기록. 상세: `report/2026-08-29-lang-output-first-item.md`.
- **v0.58** — 사용자 지시 3건 일괄 반영. ① **C/C++ 출력 항목에서 `std::cout` 제거** — "C의 경우 std::cout은 대상이 아님"에 따라 `lang-cp.html` 1번 카드를 C의 `printf` 중심으로 재작성(서식 지정자와 인자 타입 불일치, `puts`와의 차이, `\n`과 `fflush`, `fprintf`로 `stderr` 분리). C++ 문법은 기존대로 9~11번 카드에서 다룬다. ② **커리큘럼 제목의 `— 부제` 전면 제거** — `lang-*`·`web-*` 10개 페이지의 제목 26건에서 em 대시 뒤 부제를 제거("1. 출력(Output) — printf와 서식 지정자" → "1. 출력(Output)", "12. 미니 프로젝트 — REST API 서버 구축" → "12. 미니 프로젝트" 등). 부제가 담던 구체 내용은 바로 아래 요약 `<p>`가 이미 갖고 있어 정보 손실 없음. ③ **커리큘럼 카드 hover(포커스) 효과 제거** — `.curriculum-card`의 `transition`과 `:hover`(`translateY(-2px)`+브랜드 그린 테두리+그림자) 규칙을 커리큘럼 페이지 11개(`lang-*` 5, `web-*` 5, 미사용 `algo-sort.html`)에서 전부 삭제. 카드는 여전히 `cursor-pointer`이고 셰브런 회전·아코디언 펼침은 그대로. 검증 — 헤드리스 Chrome으로 10개 페이지 DOM 덤프해 카드 12개·부제 잔존 0·hover CSS 잔존 0을 전부 확인, `{`/`}` 개수 대조로 `<style>` 블록 무결성 확인, `lang-cp.html` 스크린샷 육안 확인. 세 가지 모두 관례로 `CLAUDE.md`에 기록. 상세: `report/2026-08-29-curriculum-card-cleanup.md`.
