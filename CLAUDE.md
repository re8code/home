# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 개요

"Recode Coding" 홈페이지 — 빌드 도구/패키지 매니저 없는 순수 정적 사이트(HTML + Tailwind CDN + Vanilla JS ES Module). `index.html`만 저장소 루트에 있어 GitHub Pages 루트 배포와 그대로 호환되고, 나머지 페이지는 모두 `src/`에 있다(2026-08-22, 아래 "페이지 구조" 참고).

## 개발 명령

- **로컬 서버 실행**: `python3 -m http.server 8765` (저장소 루트에서 실행 후 `http://localhost:8765/index.html` 접속, 그 외 페이지는 `http://localhost:8765/src/graffiti.html`처럼 `src/` 경로로 접속). Firestore(`assets/js/firebase-client.js`)를 사용하는 `src/graffiti*.html`은 `file://`로 직접 열면 ES module import가 동작하지 않으므로 반드시 로컬 서버를 통해 확인해야 한다.
- **빌드/린트/테스트**: 없음. `package.json` 자체가 존재하지 않는다. 변경 확인은 브라우저에서 직접 페이지를 열어 눈으로 검증한다(가능하면 claude-in-chrome으로 중간 점검).
- **진짜 좁은 모바일 폭(예: 390px) 검증이 필요할 때(2026-08-24 확립)**: claude-in-chrome의 `resize_window`도, 헤드리스 Chrome의 `--window-size`도 이 환경에서는 **폭 500px 밑으로 내려가지 않는 하드 플로어**가 있어(둘 다 시도해봤으나 요청한 값과 무관하게 500px로 고정됨 — `about.html` 배너 CTA 잘림 버그가 처음에 이 한계 때문에 500px 테스트에서는 안 잡히고 실제 아이폰(390~428pt)에서만 재현된 전례가 있음) 실제 좁은 기기 폭을 재현하려면 이 방법으로는 부족하다. 대신 Chrome을 `--remote-debugging-port`로 직접 띄우고 CDP(`Emulation.setDeviceMetricsOverride`)로 뷰포트를 강제 지정하면 500px 미만 폭도 정확히 재현된다: `google-chrome --headless=new --remote-debugging-port=9333 --user-data-dir=<임시경로> about:blank &` 로 띄운 뒤, Python(`pip install websockets`)으로 `/json/new`(PUT)에 붙어 WebSocket으로 `Emulation.setDeviceMetricsOverride({width:390,height:844,deviceScaleFactor:3,mobile:true})` → `Page.navigate` → `Runtime.evaluate`/`Page.captureScreenshot` 순으로 호출하면 실제 iPhone 뷰포트와 동일한 CSS 폭에서 오버플로우·줄바꿈을 정확히 측정할 수 있다. 작업이 끝나면 띄운 Chrome 프로세스(`pkill -f "remote-debugging-port=9333"`)와 임시 프로필 디렉터리를 정리할 것.

## 배포

- GitHub Pages, `main` 브랜치 `/ (root)` 소스로 배포(GitHub Pages branch 배포는 `/ (root)` 또는 `/docs`만 지원 — `/src`는 소스로 지정 불가). 커스텀 도메인은 저장소 루트의 `CNAME` 파일(`recode.ai.kr`)로 연결됨 — 이 파일을 삭제하거나 내용을 바꾸면 도메인 연결이 끊어지므로 주의.
- `index.html`만 루트에 남기고 나머지 페이지를 `src/`로 옮긴 것(2026-08-22)도 이 제약 때문 — 루트 배포 진입점(`index.html`)은 유지하면서 나머지 `.html`은 폴더로 정리했다.
- 자세한 절차/DNS 설정은 `report/2026-07-29-deployment-guide.md` 참고.

## 페이지 구조

`index.html`만 저장소 루트에 있고, 나머지 `.html` 페이지는 전부 `src/`에 있다(2026-08-22 정리). GitHub Pages branch 배포는 소스 폴더로 `/ (root)` 또는 `/docs`만 지원해 `/src`를 그대로 배포 소스로 지정할 수 없기 때문에, `index.html`을 루트에 남겨 배포 진입점을 유지하고 나머지 페이지만 `src/`로 옮겼다. 공유 컴포넌트나 템플릿 시스템은 없다 — Tailwind 설정(브랜드 색상 `brand.50~950`, Pretendard 폰트 등)이 `<script>tailwind.config = {...}</script>` 블록으로 **페이지마다 중복 정의**되어 있다. 톤/색상을 바꿀 때는 모든 페이지 상단의 config 블록을 동일하게 맞춰야 한다.

- `index.html` (루트) — 랜딩 페이지. Epic Games Store 레이아웃 참고 히어로(5개 훈련 코스 카테고리 자동 전환 배너 + 사이드 게이지 리스트) + Language/Algorithm/Web & WebApp/Unity/Agent AI 5개 코스 섹션 + 핵심 철학 + 1:1 상담 CTA로 구성. 원래 `src/mockup.html`이라는 별도 파일에서 라이트 톤 시안으로 반복 검토하다가, 2026-08-22 최종안을 이 파일로 옮기고 `mockup.html`은 삭제했다(그 전 다크 톤 버전의 문제의식/훈련 프로세스/대상 섹션은 이 교체로 사라짐 — 상세: `report/2026-08-22-index-adopt-mockup.md`). Swiper(캐러셀)와 AOS(스크롤 애니메이션)를 CDN으로 추가 로드하는 유일한 페이지. `src/` 페이지로의 링크는 `src/graffiti.html`처럼 `src/` 접두사를 붙인다.
  - 히어로/섹션 상단 라벨("01 · LANGUAGE" 등)의 번호 접두사는 2026-08-22 제거 — 히어로 eyebrow(`#hero-eyebrow`)·5개 섹션 헤더 `<p>`·히어로 슬라이드 JS 배열(`SLIDES[].eyebrow`)까지 6+5곳 전부 "LANGUAGE"/"ALGORITHM"/"WEB & WEBAPP"/"UNITY"/"AGENT AI"처럼 텍스트만 남겼다. 단, 사이드 게이지 리스트(`#hero-side-list`)의 컬러 사각 아이콘 안 숫자(`01`~`05`, 이름과 분리된 아이콘 배지)는 디자인 요소로 그대로 유지 — 이 두 가지는 별개 취급.
  - **히어로 배너 콘텐츠(`#hero-content`) 레이아웃 및 랜덤 카드 노출**(2026-08-23): `#hero-content`는 `justify-end`(전체 하단 정렬)가 아니라 자연스러운 상단 흐름(eyebrow → title → desc → CTA 순서, 각 `mt-4`/`mt-4`)이며, CTA만 `mt-auto pt-10`으로 배너 하단 쪽에 배치한다 — 이렇게 해야 desc가 1줄이든 2~3줄이든 title의 시작 y좌표가 5개 슬라이드 전부 동일하게 고정되고(우측 `#hero-side-list` 첫 항목과도 높이가 맞음), CTA 버튼만 하단 여백을 두고 아래쪽에 놓인다. 히어로 타이틀·설명 자체는 더 이상 `SLIDES[]`의 고정 카피가 아니라, 해당 히어로 버튼(게이지)이 포커싱될 때마다 그 섹션의 실제 `.course-card`(Language 5·Algorithm 14·Web & WebApp 5·Unity 4·Agent AI 4 = 32개) 중 하나를 `pickRandomCard()`로 랜덤 선택해 카드의 `h3`/설명을 그대로 노출한다(eyebrow·CTA 텍스트·href·배경 블롭 색상은 여전히 `SLIDES[]` 고정값). 카드 그리드에 보이는 1줄 요약(`<p>`)은 그대로 두고, 히어로 전용 3줄 분량 설명을 각 `.course-card` 엘리먼트에 `data-hero-desc` 속성으로 별도 작성해뒀다 — JS는 `card.dataset.heroDesc`를 우선 사용하고 없으면 `<p>` 텍스트로 폴백한다. 새 코스 카드를 추가할 때는 짧은 그리드용 `<p>` 설명과는 별도로, 히어로 배너 폭(576px, text-base leading-relaxed 기준 대략 100~145자)에서 정확히 3줄로 줄바꿈되는 `data-hero-desc`도 함께 작성해야 한다 — 글자 수만으로는 줄바꿈 지점을 정확히 예측하기 어려워(단어 경계에 따라 편차가 큼), 오프스크린 `<p>` 프로브 엘리먼트의 `getBoundingClientRect().height / line-height`로 실제 렌더링 줄 수를 측정하며 맞춰야 한다(`getClientRects().length`는 블록 엘리먼트 자체에는 항상 1을 반환하므로 줄 수 측정에 쓸 수 없다).
  - **Algorithm 섹션(`#algo`)** 2026-08-22 개편: 기존 4개 카드(자료구조 기초/알고리즘 문제 해결/코딩테스트 대비/시스템 설계 기초, Swiper 캐러셀 + 입문/중급/심화 뱃지)를 Language 섹션과 동일한 정적 그리드 형식(`grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-4`, 좌우 이동 없이 5개 카드 전체 노출)으로 교체하고, 정렬(Sorting) 5개 알고리즘(버블/선택/삽입/병합/퀵) 카드로 내용을 바꿨다. 정렬 5종 상세 페이지가 모두 완성된 뒤 5개 카드 전부 `<a>`로 전환되어 Language 카드와 동일하게 실제 페이지로 연결됨(아래 정렬 페이지 항목 참고).
  - **Algorithm 섹션 다시 Swiper 캐러셀로 전환(2026-08-23)**: 이진 탐색 카드가 추가되며 6개(같은 날 안에 BFS/DFS까지 더해져 실제로 8개까지 늘어남)로 늘어나 정적 그리드로는 한 화면에 다 담을 수 없게 됨 — "한 번에 5개 카드만 보여야 한다"는 요청에 따라 Web & WebApp/Unity/Agent AI와 같은 `.course-swiper` 구조(`swiper-slide`로 카드를 감싸고 섹션 헤더 우측에 이전/다음 화살표 + `swiper-pagination`)로 되돌렸다. 다만 다른 3개 섹션은 카드가 4개 고정이라 `slidesPerView: 'auto'` + 카드 자체 너비(`!w-[240px] sm:!w-[280px]`)로 자연스럽게 개수가 차는 방식인 반면, Algorithm은 카드 수가 계속 늘어날 예정이라 **`slidesPerView`를 화면 폭별로 2/3/5(640px 이상 3, 1024px 이상 5)로 고정**해 정확히 5개만 보이도록 별도 분기 처리함 — JS 초기화 루프(`document.querySelectorAll('.course-swiper').forEach(...)`)에서 `el.id === 'algo-swiper'`인지로 구성을 분기(`spaceBetween`도 16px로 그리드 시절 `gap-4`와 동일하게 유지, 카드에는 고정 너비 클래스를 주지 않음 — `slidesPerView` 숫자 지정 시 Swiper가 폭을 자동 계산하므로). 처음엔 `slidesPerGroup`을 지정하지 않아(기본값 1) 화살표 클릭 시 카드가 한 장씩만 넘어가는 버그가 있었는데, 같은 날 안에 `slidesPerView`와 동일한 값(2/3/5)으로 `slidesPerGroup`도 지정해 화면에 보이는 묶음 단위로 넘어가도록 수정됨.
  - **Language·Web & WebApp도 Swiper로 통일 + 카드 5개 모두 노출 시 nav 비활성화(2026-08-24, `report/task & bug/bug-report-01.md` 버그 2건)**: Language(`#lang`)와 Web & WebApp(`#web`)이 각자 `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-4` 정적 그리드였던 탓에, 모바일 폭(2열)에서는 스와이프 없이 카드가 여러 줄로 늘어서기만 할 뿐 Algorithm/자료구조처럼 화면 폭별로 정확히 N개만 보여주는 캐러셀 UX가 빠져 있었다는 지적 — 두 섹션 모두 Algorithm의 "고정 개수 Swiper" 패턴(`swiper-slide`로 각 카드를 감싸고, 섹션 헤더 우측에 `swiper-nav-group`, 하단에 `swiper-pagination`)으로 전환하고 `FIXED_COUNT_SWIPER_IDS`에 `lang-swiper`/`web-swiper`를 추가해 화면 폭별 2/3/5 슬라이드가 동일하게 적용되도록 함(`slidesPerGroup`도 동일 값). 두 번째 버그(카드 5개가 한 화면에 다 보이면 nav를 비활성화)는 Swiper 자체 옵션 `watchOverflow: true`를 전체 `.course-swiper` 공통 초기화 옵션에 추가하는 것으로 해결 — 슬라이드 수가 `slidesPerView` 이하가 되면 Swiper가 자동으로 nextEl/prevEl에 `swiper-button-disabled` 클래스를 붙이므로, `.nav-arrow.swiper-button-disabled { opacity:.35; cursor:not-allowed; pointer-events:none; }` CSS만 추가하면 됨(커스텀 버튼이라 Swiper 기본 CSS의 `.swiper-button-next/prev.swiper-button-disabled` 규칙이 적용되지 않아 별도 규칙이 필요했음). `watchOverflow`는 전역 옵션이라 Unity/Agent AI(`slidesPerView:'auto'`, 카드 4개 고정)에도 함께 적용되어, 화면이 충분히 넓어 4장이 모두 보일 때는 그쪽 nav도 동일하게 비활성화된다 — 의도된 부수 효과. 헤드리스 Chrome 스크린샷(1440px에서 Language·Web & WebApp 5장 전부 노출 시 nav 회색으로 비활성화 확인, 500px—Chrome headless가 허용하는 최소 창 폭—에서 2장만 보이고 nav 활성화·pagination 3점 확인)으로 검증(claude-in-chrome 확장 연결 안 됨).
  - **Algorithm 섹션을 "정렬·탐색"/"자료구조" 두 서브그룹으로 분리(2026-08-23)**: 자료구조 6종(스택/큐/연결 리스트/해시 테이블/힙/이진 탐색 트리) 추가가 결정되면서 카드가 14장(8+6)까지 늘어나게 되자, 한 캐러셀에 배지로 구분하는 대신 섹션 내부를 두 서브그룹으로 나누기로 결정(히어로의 5개 카테고리 체계 자체는 그대로 유지 — Algorithm 섹션 내부 구성만 재편). `#algo` 섹션 안에 `.swiper-block` 컨테이너 2개(정렬·탐색 `#algo-swiper` 8장 / 자료구조 `#ds-swiper` 6장)를 세로로 배치하고 각각 자기 소제목(`<h3>`)·nav 화살표·pagination을 갖는다. 두 서브그룹이 같은 섹션 안에 있어 nav 화살표 클래스(`.swiper-nav-prev`/`-next`)가 중복되므로, JS 초기화 루프에서 `el.closest('section')` 대신 **`el.closest('.swiper-block') || el.closest('section')`**로 스코프를 좁혀 각 캐러셀이 자기 nav만 찾도록 함(다른 섹션은 `.swiper-block`이 없어 기존처럼 `section` 폴백, 영향 없음). 카드 수 고정 캐러셀 목록(`FIXED_COUNT_SWIPER_IDS`)도 `algo-swiper` 단일 값에서 `['algo-swiper', 'ds-swiper']` 배열로 일반화해 자료구조 캐러셀도 동일하게 화면 폭별 2/3/5 고정 방식을 쓴다. 자료구조 6개 카드는 상세 페이지가 아직 없어 `<a>`가 아닌 `<div>`(비클릭)로 제목 옆에 "준비중" 배지만 붙여뒀다(OJ/LMS 등 서브도메인 미오픈 시 쓰던 배지 관례를 카드 단위로 재사용) — 전용 색상(보라 계열 `--from:#ddd6fe;--to:#4338ca`, 정렬·탐색의 teal과 구분)과 구조를 표현한 아이콘 SVG만 미리 준비됨. 어떤 자료구조부터 실제로 만들지는 미정.
  - **`data-aos`는 `swiper-slide`가 아니라 `course-card` 요소 자체에 붙일 것(2026-08-23 수정)**: AOS의 `[data-aos^=fade].aos-animate{transform:...}` 규칙은 속성 셀렉터 2개+클래스 1개(0,3,0)라 `.course-card:hover{transform:translateY(-6px)}`(0,2,0)보다 명시도가 높아, `data-aos`가 걸린 요소 자신에게는 hover의 `transform`만 무효화되고 `border-color`/`box-shadow`는 그대로 적용된다(속성별 캐스케이딩이라 규칙째로 밀리지 않음). Language 섹션(`#lang`)은 원래부터 `data-aos="fade-up"`을 `<a class="course-card">` 자체에 붙여왔기 때문에 hover 시 살짝 들뜨는 느낌 없이 테두리·그림자만 바뀌는데, Algorithm/자료구조 두 섹션은 `data-aos`를 부모 `swiper-slide` div에 붙였던 탓에 `course-card`의 hover `transform`이 그대로 살아 카드가 위로 6px 튀는 효과가 났다 — 사용자가 "포커스 효과가 이상하다, Language처럼 맞춰달라"고 지적해 발견, `#algo-swiper`·`#ds-swiper` 14장 전부 `data-aos`를 `swiper-slide`에서 `course-card`(또는 준비중 `<div>`)로 옮겨 Language와 동일하게 통일했다. 새 카드를 추가할 때(정렬·탐색/자료구조뿐 아니라 Web·Unity·Agent AI의 `!w-[240px]` 계열 슬라이드도 동일 패턴이므로) `data-aos`는 항상 `course-card` 요소 자체에 붙일 것 — `swiper-slide`에 붙이면 이 hover 불일치가 재발한다.
  - **카드 cover-art 아이콘**: 처음엔 3글자 모노스페이스 약어(Bub/Sel/Ins/Mrg/Qck)였으나, 2026-08-22 각 알고리즘을 상징하는 인라인 SVG 일러스트로 교체 — `viewBox="0 0 400 300"`(cover-art의 `aspect-[4/3]`와 정확히 일치)에 막대그래프 형태로 그리고 `class="absolute inset-0 h-full w-full"`로 cover-art 영역 전체를 채운다(부모 `cover-art relative` 컨테이너 기준 절대 위치, 기존 flex 중앙정렬 클래스는 남아 있지만 절대 위치 SVG에는 영향 없음). 알고리즘별 그림: 버블=인접한 두 막대가 rose(#fb7185)로 강조되고 위에 이중 화살표(swap 아이콘), 선택=최솟값 막대(sky #38bdf8 solid)에서 맨 앞 점선 테두리 목표 자리로 향하는 곡선 점선 화살표, 삽입=정렬된 막대 행 위에 떠 있는 sky 막대가 점선 테두리 빈 자리로 내려꽂히는 화살표, 병합=위쪽 작은 두 그룹(각 3개 막대, white/40)이 곡선으로 수렴해 아래쪽 완전히 합쳐진 브랜드그린(`#aef1cd`) 막대 행으로 이어짐, 퀵=violet(#a78bfa) pivot 막대를 사이에 두고 두 점선 세로선으로 좌우 분할 구간을 표시. 시각화 상세 페이지에서 쓰는 것과 같은 의미 색상(rose=교환, sky=선택/삽입 대상, violet=pivot, brand green=완료/병합결과)을 그대로 재사용해 랜딩 카드와 상세 페이지 사이 시각 언어를 통일했다.
- `src/graffiti.html` / `src/graffiti-detail.html` / `src/graffiti-new.html` — "원장님의 낙서" 게시판 (목록 / 상세·수정·삭제 / 작성). Firestore CRUD를 `assets/js/firebase-client.js`에서 가져와 사용하며, `<script type="module">` 인라인 블록에서 DOM과 연결한다. 헤더는 `index.html`과 동일한 GNB(아래 참고)를 쓰지만, 본문(카드 리스트·푸터 등)의 디자인 톤 조정은 아직 미착수(`docs/DEV_PLAN.md` Phase 2).
- `src/lang-cp.html` — Language 코스 중 C/C++ 상세 페이지(`index.html`의 C/C++ 카드에서 연결). 커리큘럼 목록 UI는 korea-pass.kr 공지사항 목록 페이지(`/notice/noticeList.do`)를 벤치마크(브레드크럼(홈/C/C++, "훈련 코스" 중간 항목은 2026-08-22 제거)+타이틀, "더보기" 버튼 — 검색창은 2026-08-22 제거). 상단 "LANGUAGE" 라벨은 번호(`01 ·`) 없이 텍스트만 표기(2026-08-22). `index.html`과 동일한 라이트 테마·헤더/푸터를 재사용.
  - **커리큘럼 카드 형식** (2026-08-22, `lang-cp.html`에서 먼저 적용한 뒤 같은 날 `lang-jv/py/js/dt.html` 4개에도 동일 적용 — 5개 페이지 전부 동일 형식): 뱃지(기초/실습/프로젝트)+N주차 표시를 없애고 제목에 `1.`~`12.` 번호를 붙이는 방식으로 교체. 각 카드는 항상 보이는 brief 설명(`text-sm text-slate-500`, non-bold, 제목과 같은 위치)과, 클릭 시에만 펼쳐지는 detail 설명 2단 구성 — CSS Grid의 `grid-template-rows: 0fr → 1fr` 트랜지션(`.curriculum-detail`, `<style>` 블록)으로 높이 애니메이션을 구현해 별도 JS 높이 계산 없이 부드럽게 확장/축소된다. 카드 자체가 `role="button" tabindex="0" aria-expanded`를 갖는 클릭/키보드(Enter·Space) 토글 대상이며, 파일 하단 인라인 `<script>`가 `data-expanded` 속성을 토글해 CSS가 반응하는 구조(셰브런 아이콘도 같은 속성으로 회전). 카드가 포커스를 잃으면(`blur` 이벤트 — 다른 카드 클릭, 빈 영역 클릭, Tab으로 다음 요소 이동 등) 열려 있던 상태와 무관하게 자동으로 `data-expanded="false"`로 되돌아가 원래 크기로 접힌다 — 한 번에 하나의 카드만 펼쳐진 상태를 유지하기 위한 동작. `data-category`(basic/practice/project)는 정보성 메타데이터로 카드에 남아 있지만 더 이상 뱃지로 렌더링되지 않음. 새 언어 페이지를 추가할 때는 이 5개 파일 중 하나를 템플릿으로 카드 구조·`<style>`의 `.curriculum-detail`/`.curriculum-chevron` 블록·파일 하단 토글 스크립트를 함께 복사할 것.
  - **언어 바로가기 탭** (2026-08-22, 이전엔 기초/실습/프로젝트 카테고리 필터였음): C/C++/Java/Python/JavaScript/Dart 5개 pill 탭이 각각 `lang-cp.html`/`lang-jv.html`/`lang-py.html`/`lang-js.html`/`lang-dt.html`로 이동하는 페이지 전환 링크로 동작(현재 페이지는 `aria-current="page"` + `is-active` 스타일). 5개 파일 모두 존재하며(2026-08-22, C/C++·Java·Python·JavaScript·Dart 전부 제작 완료) 서로의 탭 `href`가 정확히 일치한다. 새 언어 페이지를 추가로 만들 때는 이 파일명 규칙을 따르고, **5개 페이지 전부**의 언어 바로가기 탭에 새 항목을 반영해야 한다(한 파일만 만들고 나머지 4개의 탭을 안 챙기면 링크가 어긋난다).
  - "더보기" 페이지네이션은 2026-08-22 제거 — 12개 커리큘럼 카드를 처음부터 전부 노출한다(관련 JS도 제거, 정적 마크업만 남음).
- `src/algo-sort.html` — 2026-08-22 초안 제작 후 같은 날 방향 수정으로 **현재 어디서도 링크되지 않는 미사용 파일**(파일 자체는 삭제하지 않고 남겨둠). 처음엔 "정렬 5개를 Language 때와 같은 방식으로 구성"이라는 지시를 lang-cp.html과 동일한 12주형 커리큘럼 카드 상세 페이지로 오해해 만들었으나, 실제 의도는 랜딩 페이지(`index.html`) Algorithm 섹션의 카드를 Language 섹션과 같은 정적 그리드 형식으로 구성하라는 것이었음이 확인됨. 재사용 여부(시각화 페이지로 개조 vs 삭제)는 아직 미정.
- `src/algo-bubble-sort.html` / `src/algo-selection-sort.html` / `src/algo-insertion-sort.html` / `src/algo-merge-sort.html` / `src/algo-quick-sort.html` — Algorithm 코스 정렬 5종 전부의 시각화 상세 페이지(2026-08-22, visualgo.net 스타일). `index.html` Algorithm 섹션 5개 카드(버블/선택/삽입/병합/퀵 정렬) 전부 이 페이지들로 연결 완료 — Algorithm 코스는 이제 Language처럼 카드 전부가 실제 페이지를 가리킨다.
  - **시각화 구조**: 배열을 막대그래프(높이=값)로 렌더링하고, 정렬 과정을 미리 전부 계산해 "step" 배열(각 단계의 배열 스냅샷·비교/포인터 대상 인덱스·정렬 진행 상태·의사코드 라인 번호·설명 문구·누적 비교/교환(또는 기록) 횟수)로 기록한 뒤, 재생/일시정지/이전·다음 단계/처음·끝으로 이동 버튼이 이 step 배열의 인덱스만 앞뒤로 옮기며 매번 다시 그리는 구조(visualgo.net과 동일한 "사전 계산 후 재생" 패턴) — 알고리즘 로직과 재생 컨트롤이 완전히 분리되어 있어 5개 파일의 컨트롤·프리셋·속도조절 JS(`buildSteps`/`render`/`play`/`goTo` 등)는 구조가 동일하고, `buildSteps`의 알고리즘 로직과 색상 매핑만 각 알고리즘에 맞게 다르다.
  - **정렬 진행 상태 표현이 알고리즘마다 다름** — 이게 5개 파일에서 가장 핵심적인 차이:
    - 버블 정렬: `sortedFrom`(뒤에서부터 정렬 완료, 매 순회 축소) + 조기 종료(교환 0회 순회 시 즉시 중단).
    - 선택 정렬: `sortedUntil`(앞에서부터 정렬 완료) + 최솟값 후보(`minIdx`, sky-400) 추적, 조기 종료 없음.
    - 삽입 정렬: `sortedUntil`(선택 정렬과 동일하게 앞에서부터) + "삽입 대상"(sky-400) 표시, while 조건이 매번 즉시 실패하는 방식으로 자연스러운 O(n) 최선.
    - 병합 정렬: 단일 경계값이 아니라 재귀적 `[lo,hi)` "병합 구간"(indigo-200 배경)을 표시하고, 최상위(전체 범위) 병합이 끝나야만 전체가 정렬 완료(green) 처리됨 — `sortedUntil`은 최상위 병합 전까지 0.
    - 퀵 정렬: 조각난(연속하지 않는) `fixed` 인덱스 배열 — pivot이 자리를 확정할 때마다 그 위치 하나만 개별적으로 green 처리되어 누적됨(선형적으로 자라는 경계가 아님). pivot 자체는 violet-400으로 별도 표시.
  - **색상 코딩 5종 요약**: 버블(대기/비교/교환/정렬완료 4색), 선택·삽입(4색+최솟값후보 또는 삽입대상 sky-400=5색), 병합(대기/병합구간 indigo-200/비교/기록 rose-400/정렬완료=5색), 퀵(대기/pivot violet-400/비교/교환/자리확정=5색). 각 페이지 하단에 해당 범례 표시.
  - **의사코드 패널**: 버블 4줄, 삽입 5줄, 선택 6줄, 퀵(Lomuto partition) 8줄, 병합(merge 단계만) 8줄 — 전부 어두운 코드 블록에 현재 실행 줄을 `.code-line.is-active`(브랜드 그린 배경)로 하이라이트.
  - **배열 프리셋**: 무작위 · 정렬됨 · 역순 · 거의 정렬됨 4가지 + "새로 섞기" 버튼, 5개 파일 공통 기준 배열 `BASE_VALUES = [22,30,38,46,54,62,70,78,86,94]`을 셔플/역순/2회 인접교환으로 변형. 각 알고리즘의 "정렬됨"·"역순" 프리셋 결과가 서로 다른 것 자체가 핵심 학습 포인트:
    - 버블·삽입: "정렬됨" = 최선(O(n), 비교 9회·교환 0회) — 조기 종료/while 즉시 실패로 실제 도달.
    - 선택: "정렬됨"이어도 비교는 여전히 45회(n=10 고정) — 교환만 0회. "선택 정렬은 최선의 경우도 O(n²)"임을 수치로 증명.
    - 병합: 프리셋과 거의 무관하게 항상 비슷한 비교·기록 횟수 — O(n log n)이 배열 상태에 좌우되지 않음을 증명.
    - 퀵: 마지막 원소 pivot 고정 방식이라 "정렬됨"·"역순" **둘 다** 최악(O(n²), 비교 45회)이 되도록 프리셋 라벨 자체를 "정렬됨 (최악)"/"역순 (최악)"으로 명시 — pivot 선택 전략의 중요성을 직접 체감하게 하는 의도적 설계.
  - **속도 조절**: 느리게(1000ms)/보통(500ms)/빠르게(180ms) 3단 pill 버튼, 재생 중 속도를 바꾸면 즉시 그 속도로 재시작.
  - 브레드크럼은 5개 파일 전부 `홈 / {알고리즘명}` 2단(Language 상세 페이지의 `홈 / C/C++`와 동일한 패턴 — 처음 `algo-bubble-sort.html`은 `algo-sort.html`을 따라 3단으로 만들었다가 사용자 지시로 2단 정정, 이후 4개는 처음부터 2단으로 제작). 페이지 최대 너비는 시각화 공간 확보를 위해 다른 상세 페이지(`max-w-4xl`)보다 넓은 `max-w-5xl`을 사용 — 이 5개 페이지만의 의도적 예외.
  - `lang-cp.html`류의 아코디언 커리큘럼 카드 CSS(`.curriculum-detail`/`.curriculum-chevron`)는 이 페이지들에 없음(내용 성격이 달라 불필요) — 새 알고리즘 시각화 페이지(그래프탐색·이진탐색 등)를 추가할 때는 이 5개 파일 중 정렬 진행 상태 표현이 가장 비슷한 것을 템플릿으로 삼을 것.
- `src/algo-binary-search.html` — Algorithm 코스 파일럿 리스트업(`report/2026-08-22-algorithm-visualization-listup.md`)의 탐색 파트 중 이진 탐색 시각화 상세 페이지(2026-08-23). 정렬 5종 페이지와 같은 막대그래프 엔진·step 배열·재생 컨트롤 구조를 그대로 재사용하되 핵심 차이는 다음과 같다.
  - **배열이 아니라 목표값이 프리셋 대상**: 이진 탐색은 정렬된 배열을 전제로 하므로 배열(`BASE_VALUES`, 정렬 페이지와 동일한 `[22,30,38,46,54,62,70,78,86,94]`)은 항상 고정, 대신 "무엇을 찾는가"를 바꿔가며 복잡도를 체감시킨다 — 프리셋 4종: 중앙값 적중(target=54, 첫 mid에서 바로 발견 → 비교 1회로 최선 O(1) 증명) / 일반(target=86) / 존재하지 않음(target=50, 최악 O(log n)=비교 4회 증명) / 첫 번째 값(target=22, 경계 사례). 프리셋 외에 직접 숫자를 입력해 즉석에서 탐색해보는 입력창+버튼도 추가(정렬 5종에는 없는 이 페이지만의 인터랙션).
  - **정렬 진행 상태(`sortedFrom`/`sortedUntil`/`range`/`fixed`) 대신 탐색 범위 `[lo, hi]`와 `mid`**: 매 비교마다 범위 밖(제외됨, slate-100+opacity)/범위 안 대기(slate-300)/mid 비교 중(amber-400)/발견(brand-500) 4색으로 표시 — 교환·기록 개념이 없어 정렬 5종보다 색상 종류가 적다.
  - **L/M/R 포인터 행**: 막대 아래 인덱스 행 밑에 별도 `#pointers` 행을 추가해 현재 `lo`/`mid`/`hi`에 해당하는 칸에 L/M/R 문자를 표시(정렬 5종에는 없던 요소) — visualgo.net의 포인터 표기를 참고해 강화.
  - 의사코드 7줄(`lo,hi 초기화 / while / mid 계산 / if 발견 / else if 왼쪽 버림 / else 오른쪽 버림 / return -1`), cover-art는 상세 페이지 헤더에서만 인디고 그라디언트(`--from:#a5b4fc;--to:#312e81`)+"Bin" 텍스트로 정렬 5종의 teal 계열과 구분(카테고리 라벨도 "SORTING" 대신 "SEARCHING"). 단 **랜딩 카드(`index.html`)의 cover-art는 Algorithm 섹션 통일성을 위해 정렬 5종과 동일한 teal 그라디언트를 유지**하고 아이콘만 다르게(막대그래프 + 좌우 점선 괄호로 탐색 범위 표시 + 중앙 mid 막대 amber 강조 + 위에서 내려꽂는 화살표)했다 — 상세 페이지 헤더 색과 랜딩 카드 색이 다른 것은 의도된 차이.
  - 2026-08-23 `index.html` Algorithm 섹션 6번째 카드로 `<a href="src/algo-binary-search.html">` 연결(당시 마지막) — 아래 BFS/DFS 추가로 지금은 6번째, 마지막은 DFS. 아래 "Algorithm 섹션 다시 Swiper 캐러셀로 전환" 항목 참고.
- `src/algo-bfs.html` / `src/algo-dfs.html` — Algorithm 코스 파일럿 리스트업의 "그래프 탐색" 파트, 너비/깊이 우선 탐색 시각화 상세 페이지(2026-08-23). 정렬·이진 탐색과 같은 "step 배열 사전 계산 → 재생 컨트롤이 인덱스만 이동" 패턴을 재사용하지만, 시각화 대상이 막대그래프가 아니라 **노드-엣지 그래프**라는 점에서 완전히 새로운 렌더링 엔진이다.
  - **공유 그래프**: 두 파일 모두 동일한 9개 노드(A~I)·12개 무방향 간선을 `ADJ`(인접 리스트)와 `POS`(SVG `viewBox="0 0 800 460"` 좌표) 상수로 하드코딩해 공유한다(파일은 독립적이라 각자 동일한 값을 중복 정의 — 프로젝트 관례상 페이지 간 공유 JS 파일을 두지 않음). 같은 그래프를 BFS/DFS 양쪽에서 그대로 써야 "같은 문제, 다른 순서"라는 핵심 학습 포인트가 성립하므로, 그래프를 바꾸려면 두 파일의 `ADJ`/`POS`/`EDGES` 파생 로직을 함께 수정해야 한다.
  - **BFS**: 큐(FIFO)를 `visited-at-enqueue` 방식으로 구현(중복 큐잉 방지) — 노드 상태 4종(`unvisited`/`queued`/`current`/`done`) + 발견 간선 강조(rose). A에서 시작 시 방문 순서 A→B→C→D→E→F→G→H→I(레벨별로 정확히 층을 이루며 퍼짐)로, 클로드 인 크롬으로 최종 확인.
  - **DFS**: 스택(LIFO)을 `visited-at-pop` 방식으로 구현(이웃을 알파벳 역순으로 push해 popping 순서가 재귀 DFS와 정확히 일치하도록 설계) — 이미 방문한 노드가 스택에 중복으로 남아있다가 나중에 pop될 때 건너뛰는 것을 노드 테두리 점선(rose dashed ring, `skip` 상태)으로 별도 표시. A에서 시작 시 방문 순서 A→B→E→I→G→C→F→D→H로, **BFS와 완전히 다른 순서**임을 같은 그래프로 대비시킨다. 스텝 수도 BFS(28)보다 훨씬 많음(60, 중복 push/skip 스텝 포함).
  - **방문 순서 배지**: 각 노드 우상단에 작은 원형 배지로 "몇 번째로 방문했는지" 숫자를 표시(`current`/`done` 상태가 되는 순간부터 노출) — visualgo류 순서 표기를 그래프에도 반영.
  - **큐/스택 패널**: 막대그래프 페이지의 L/M/R 포인터 행과 같은 역할 — 현재 큐(FRONT가 왼쪽)/스택(TOP이 오른쪽) 내용물을 pill 리스트로 보여주고, 다음에 처리될 항목에 링 하이라이트.
  - **시작 노드 프리셋**: 배열 프리셋(정렬)·목표값 프리셋(이진 탐색)에 대응하는 "시작 노드" 프리셋 4종(A 기본/E/G/I) + `<select>`로 나머지 노드(B/C/D/F/H) 중 선택해 시작하는 커스텀 입력 — 같은 그래프에서 시작점만 바꿔도 층 구조·방문 순서가 달라지는 것을 비교하게 함.
  - 의사코드 BFS 5줄/DFS 6줄. cover-art는 상세 페이지 헤더·랜딩 카드 모두 blue 계열(`--from:#93c5fd;--to:#1e3a8a`)로 정렬(teal)·이진탐색(랜딩만 teal 유지, 헤더는 indigo)과 구분되는 새 "GRAPH" 카테고리 색. 랜딩 카드 아이콘은 작은 트리형 그래프 SVG로 BFS는 층별 색상(초록 완료/앰버 처리중/스카이 대기), DFS는 한 갈래를 끝까지 파고든 rose 경로 + 나머지 미방문(회색)을 대비시켜 두 알고리즘의 성격 차이를 아이콘만으로도 드러낸다.
  - `index.html` Algorithm 섹션에 7·8번째 카드로 연결 완료(2026-08-23) — "마지막 카드 하이라이트" 관례가 이진 탐색에서 DFS로 이동. Algorithm 코스 파일럿 8종(정렬 5 + 탐색 3) 전부 완료.
- `src/algo-stack.html` — 자료구조 6종(스택/큐/연결 리스트/해시 테이블/힙/이진 탐색 트리) 중 첫 번째로 제작된 상세 시각화 페이지(2026-08-23). 정렬·탐색 페이지와 같은 "step 배열 사전 계산 → 재생 컨트롤이 인덱스만 이동" 골격을 재사용하지만, 정렬(배열 값 비교)·탐색(범위 좁히기)·그래프(노드-엣지)와 달리 **연산 시퀀스(push/pop 목록)를 스크립트로 미리 작성해 시뮬레이션**하는 방식이라는 점이 다르다 — 자료구조 카테고리 최초 페이지이므로 향후 큐/연결 리스트/해시 테이블/힙/이진 탐색 트리를 만들 때 이 페이지가 1차 템플릿이 된다.
  - **프리셋 대신 "동작 시나리오" 4종**: 기본 Push/Pop(마지막에 언더플로우 시연 포함) / 함수 호출 스택(중첩 함수 호출·반환을 push/pop으로 표현) / 괄호 검사(`{[()]}` 문자열을 스캔하며 여는 괄호는 push, 닫는 괄호는 pop해 짝을 확인) / 실행 취소(Undo, 타이핑 스냅샷을 push하고 Ctrl+Z를 pop으로 표현, 끝까지 되돌리면 언더플로우) — 각 시나리오는 `{op:'push', value, label}` / `{op:'pop', label}` 객체 배열로 정의되고, 공통 `buildSteps()` 하나가 모든 시나리오와 커스텀 조작을 동일하게 처리한다(연산 종류만 다를 뿐 엔진은 하나).
  - **직접 조작(커스텀) 모드**: 값 입력창 + Push/Pop 버튼으로 프리셋과 무관하게 자유롭게 스택을 조작할 수 있다 — 다른 페이지의 "직접 값 입력"(이진 탐색)·"이 노드에서 시작"(BFS/DFS)이 단발성 재계산이었던 것과 달리, 스택은 클릭할 때마다 현재 커스텀 연산 목록에 한 건씩 누적되며 즉시 마지막 단계로 이동한다(`state.mode`가 `'preset'`↔`'custom'`을 오가며, 커스텀 진입 시 프리셋 버튼의 `is-active`를 전부 해제 — BFS/DFS의 커스텀 시작 노드 클릭 시 프리셋 버튼 해제 관례와 동일).
  - **렌더링**: 막대그래프·SVG 그래프 대신 세로로 쌓이는 박스 리스트(`flex flex-col-reverse`로 배열 앞=바닥, 뒤=꼭대기가 자연스럽게 아래→위로 쌓이도록 구현) — 매 스텝마다 전체를 다시 그리는 단순한 방식(개수가 push/pop마다 바뀌어 막대그래프처럼 스켈레톤을 고정해두는 최적화가 애초에 불가능함). 색상 4종: 스택 내부 값(흰색)/최상단 TOP(브랜드그린 링)/방금 push된 값(sky)/언더플로우 시도(스택 영역 전체에 rose 링 표시, 박스는 그대로 — pop할 대상 자체가 없으므로).
  - 의사코드 6줄(push 2줄 + pop 4줄), cover-art는 랜딩 카드와 동일한 보라 계열(`--from:#ddd6fe;--to:#4338ca`)을 상세 페이지 헤더에도 그대로 사용(이진 탐색·BFS/DFS처럼 헤더만 다른 색을 쓰지 않음 — 자료구조 카테고리는 랜딩·상세 색을 통일하기로 함), 카테고리 라벨은 "DATA STRUCTURE".
  - `index.html`의 `#ds-swiper` 스택 카드를 비클릭 `<div>`+"준비중" 배지에서 `<a href="src/algo-stack.html">`로 전환(배지·`opacity-80` 제거) — 큐/연결 리스트/해시 테이블/힙/이진 탐색 트리 5장은 아직 미제작이라 그대로 둠.
- `src/algo-queue.html` — 자료구조 6종 중 두 번째, 스택 페이지를 그대로 템플릿 삼아 제작(2026-08-23). 엔진(공통 `buildSteps()`가 시나리오·커스텀 조작을 동일하게 처리, 값 입력창+버튼 2개로 자유 조작, `state.mode` `'preset'`↔`'custom'` 전환)은 스택과 완전히 동일하고, FIFO/LIFO 차이만 반영했다.
  - **동작 시나리오 4종**: 기본 Enqueue/Dequeue(마지막에 언더플로우 시연) / 프린터 작업열(먼저 접수된 인쇄 작업부터 처리) / 대기열·줄서기(먼저 줄 선 사람부터 응대) / BFS 탐색열(A→B,C 발견→B→D 발견→C→D 순서로, "가까운 노드부터 레벨 순서로 처리"라는 FIFO의 실제 쓰임을 보여줌) — 스택의 "함수 호출/괄호 검사/Undo"에 대응하는, FIFO에 자연스러운 실사용 시나리오로 새로 구성.
  - **큐만의 렌더링 차이**: 스택은 세로 박스 리스트(`flex flex-col-reverse`)였지만 큐는 **가로 박스 리스트**(`flex items-center gap-2 overflow-x-auto`)로 변경 — FIFO는 "왼쪽(FRONT)에서 빠지고 오른쪽(REAR)에 쌓인다"는 방향성이 스택의 "위/아래" 은유보다 좌우 은유가 더 직관적이기 때문. 배열의 `queue[0]`이 FRONT(다음에 나갈 값, 브랜드그린 링), 마지막 원소가 REAR(방금 들어온 값)이며, `enqueue`는 `array.push`, `dequeue`는 `array.shift`로 구현(스택의 `push`/`pop`과 반대쪽 끝을 사용한다는 점이 핵심 차이 — 설명 섹션에 명시).
  - 의사코드 6줄(enqueue 2줄 + dequeue 4줄, `array.push`/`array.shift` 등 스택과 동일한 줄 수·구조를 유지해 두 자료구조를 나란히 비교하기 쉽게 함), cover-art·카테고리 라벨("DATA STRUCTURE")은 스택과 동일한 보라 계열로 통일.
  - `index.html`의 `#ds-swiper` 큐 카드를 `<a href="src/algo-queue.html">`로 전환 — 연결 리스트/해시 테이블/힙/이진 탐색 트리 4장은 아직 미제작이라 그대로 둠.
- `src/algo-linked-list.html` — 자료구조 6종 중 세 번째(2026-08-23). 스택/큐와 달리 값이 인덱스로 바로 접근되지 않는다는 점을 보여주기 위해 **순회(traversal) 스텝을 명시적으로 기록**하는 `buildSteps()`를 새로 작성 — 내부 상태는 실제 노드/포인터 객체 대신 값 배열(`list[]`)로 유지하되, `insertBack`/`insertAfter`/`deleteValue`처럼 대상을 찾아야 하는 연산은 배열을 처음부터 훑으며 매 노드마다 "지나감(traverse)" 스텝을 하나씩 push해 O(n) 탐색 비용을 시각적으로 드러낸다(`insertFront`만 순회 없이 O(1)).
  - 동작 시나리오 4종: 기본 삽입/삭제(마지막에 존재하지 않는 값 삭제 시도로 오류 시연) / 재생목록 관리 / 즐겨찾기 목록 관리 / 검색 후 삽입·삭제(순회 스텝이 가장 많이 발생하도록 설계, 마지막에 존재하지 않는 값을 끝까지 순회하며 실패하는 예시 포함). 직접 조작 모드는 "뒤에 삽입"/"값 삭제" 두 버튼 + 값 입력창.
  - 렌더링은 큐와 같은 가로 박스 리스트를 재사용하되 각 박스 사이에 화살표(`→`)와 리스트 끝에 `NULL` 박스를 추가해 포인터 체인을 시각화. 순회 중인 노드는 호박색(amber), 방금 삽입된 노드는 sky로 구분(큐/스택은 대기·TOP/FRONT·삽입 3색이었던 것과 달리 "탐색 중" 상태가 추가되어 색상 의미가 하나 더 필요했음).
  - 의사코드 6줄(`insertFront`/`insertBack`/`insertAfter`/순회/`deleteValue`/오류), cover-art·카테고리 라벨은 스택·큐와 동일한 보라 계열 "DATA STRUCTURE"로 통일.
- `src/algo-hash-table.html` — 자료구조 6종 중 네 번째(2026-08-23). `hash(key) = key % 7`(TABLE_SIZE=7)이라는 가장 단순한 해시 함수를 사용해 버킷 배열(`buckets[7]`, 각 버킷은 체이닝용 배열)을 시뮬레이션하는 새 엔진.
  - 동작 시나리오 4종: 기본 삽입/검색/삭제(키 3과 10이 3%7=10%7=3으로 의도적으로 충돌) / 학번→사물함 배정(15와 22가 15%7=22%7=1로 충돌 — "다른 학번인데 같은 사물함"이라는 체감형 비유) / 전화번호부 검색(11·23·35가 서로 다른 버킷에 들어가 충돌 없이 O(1) 검색을 시연) / 최악의 해시 함수(7·14·21이 전부 7의 배수라 모두 버킷 0에 몰리고, 존재하지 않는 키 28을 검색하면 버킷 0의 3개 항목을 전부 순회한 뒤 실패 — 나쁜 해시 함수가 O(n)으로 퇴화하는 것을 직접 증명). 커스텀 조작은 키(숫자)+값 입력 후 삽입/검색 버튼.
  - 렌더링은 버킷 개수(7)만큼 가로 행을 세로로 나열하고, 각 행 안에서 충돌된 항목들을 연결 리스트 페이지와 같은 화살표 체인으로 표시 — 해시 계산이 끝난 버킷 행 전체를 호박색으로 강조해 "이 버킷으로 왔다"는 것을 먼저 보여준 뒤, 그 안에서 개별 항목을 탐색/삽입 상태로 구분(sky=방금 삽입/브랜드그린=검색 성공/호박색=탐색 중).
  - 의사코드 6줄(해시 계산/삽입/검색-순회/발견/실패/삭제), 나머지 스타일은 스택·큐·연결 리스트와 동일.
- `src/algo-heap.html` — 자료구조 6종 중 다섯 번째(2026-08-23), Algorithm 코스에서 지금까지와 달리 **완전히 새로운 SVG 트리 렌더링 엔진**을 최초로 도입(BFS/DFS의 고정 그래프와 달리 힙은 크기가 계속 바뀌므로 좌표를 매번 인덱스로부터 계산). 최소 힙(min-heap)으로 구현 — `insert`는 배열 끝에 추가 후 부모와 비교하며 교환하는 sift-up, `extractMin`은 루트를 마지막 값으로 대체한 뒤 더 작은 자식과 비교하며 내려가는 sift-down.
  - **좌표 계산**: `heapPos(i, n)`이 인덱스 `i`의 레벨(`floor(log2(i+1))`)과 그 레벨 안에서의 위치를 계산해 x/y를 산출 — BFS/DFS처럼 하드코딩된 `POS` 상수 없이 매 렌더마다 동적으로 계산되므로 노드가 늘거나 줄어도(삽입/삭제) 항상 올바른 완전 이진트리 모양을 유지한다.
  - **트리 + 배열 동시 표시**: SVG 트리 바로 아래에 같은 힙을 배열 형태(인덱스 라벨 포함)로도 나란히 렌더링 — "배열 하나로 트리 전체를 표현한다"는 힙의 핵심 특징을 두 표현을 대조시켜 체감하게 함.
  - 동작 시나리오 4종: 기본 삽입 / 마감일 스케줄러(우선순위=마감까지 남은 일수, extractMin을 반복하면 항상 가장 급한 일부터 나옴을 시연) / 힙 정렬 체감(8개 값을 모두 삽입한 뒤 전부 extractMin — 오름차순으로 하나씩 나오는 것이 힙 정렬의 원리임을 직접 증명) / 최솟값이 떠오르는 순간(큰 값들을 먼저 넣은 뒤 아주 작은 값을 마지막에 넣어 sift-up으로 단숨에 루트까지 올라가는 것을 시연). 커스텀 조작은 Insert/ExtractMin 두 버튼.
  - 의사코드 5줄(insert/sift-up/extractMin/교체/sift-down), cover-art·카테고리는 다른 자료구조 페이지와 동일.
- `src/algo-bst.html` — 자료구조 6종 중 마지막(2026-08-23). 힙과 마찬가지로 새 SVG 트리 엔진을 쓰지만, 힙은 배열 인덱스로 좌표를 계산하는 반면 BST는 **실제 노드 객체(`{value, left, right}`)를 유지하며 매 스텝마다 트리 전체를 깊은 복사**해 히스토리를 남기고, 좌표는 `layoutTree()`가 **중위 순회(in-order) 순서**로 x좌표를 매겨 계산한다(중위 순회 결과가 항상 오름차순이라는 BST 성질을 그대로 레이아웃에 활용 — 왼쪽 서브트리가 항상 왼쪽에, 오른쪽이 항상 오른쪽에 그려짐이 보장됨).
  - **삽입/검색만 구현, 삭제는 의도적으로 범위 제외**: BST 삭제는 자식 수(0/1/2개)에 따라 처리 방식이 갈리는 복잡한 규칙이 필요해, 스택/큐/연결 리스트/해시 테이블/힙까지의 "2개 핵심 연산" 스코프에 맞춰 insert+search로 제한했다 — "설명" 섹션에 이 트레이드오프를 명시.
  - 동작 시나리오 4종: 기본 삽입(50/30/70/20/40/60/80 — 균형 잡힌 트리 모양 관찰) / 정렬된 순서 삽입(10~50을 오름차순으로 넣으면 트리가 오른쪽으로만 길어지는 편향 트리가 되어 사실상 연결 리스트와 같아짐, 높이 5) / 균형 잡힌 삽입(같은 값 5개를 30·20·40·10·50 순서로 넣으면 높이 3의 균형 트리가 됨 — "같은 값도 삽입 순서에 따라 모양이 달라진다"는 대비) / 검색 시연(존재하는 값 60은 2번 비교 후 발견, 존재하지 않는 값 65는 3번 비교 후 null에 도달해 실패). 커스텀 조작은 Insert/Search 두 버튼.
  - 의사코드 6줄(insert 3줄 + search 3줄), cover-art·카테고리는 다른 자료구조 페이지와 동일. **Algorithm 코스 자료구조 파일럿 6종(스택/큐/연결 리스트/해시 테이블/힙/이진 탐색 트리) 전부 완료** — `index.html`의 `#ds-swiper` 6개 카드 모두 실제 페이지로 연결되고, "마지막 카드 하이라이트" 관례가 이진 탐색 트리 카드로 적용됨(다른 완성된 서브그룹들과 동일하게, 6종이 모두 완성된 시점에 처음 적용).
- **Web & WebApp 섹션(`#web`) 카드 4장 → 5장 재구성(2026-08-23)**: 대화로 코스 흐름을 프론트/백엔드/DevOps/CI-CD 4단계로 정리한 뒤, 기존 "Full-stack 프로젝트" 캡스톤 카드를 유지하기로 해 5장으로 확정 — Frontend(React) → Backend(Node/Spring) → DevOps(Docker) → CI/CD(GitHub Actions) → Full-stack 통합 프로젝트(캡스톤, 마지막 카드 하이라이트). 기존에 한 장("배포/인프라 기초")에 뭉쳐 있던 DevOps·CI/CD 설명을 두 장으로 분리.
  - **1차 시도(실제 로고 이미지) → 2차 수정(커버아트 전체를 채우는 커스텀 SVG)**: 처음엔 Unsplash류 사진 스톡 대신 Language 섹션처럼 React/Node.js/Docker/GitHub Actions **실제 로고**(devicon, MIT 라이선스, `*-original.svg`를 jsdelivr CDN에서 다운로드해 `assets/image/react.svg`/`nodejs.svg`/`docker.svg`/`github-actions.svg`로 저장 — Java/Python/Dart 워드마크와 동일 출처, `report/2026-08-22-mockup-language-layer-02.md` 참고)를 `h-20 w-20` 크기로 커버아트 중앙에 배치했으나, 사용자가 "카드 상단 영역을 가득 채우는 이미지"를 요청 — 로고 아이콘은 여백이 많아 그대로 확대하면 어색해지므로, Algorithm/자료구조 섹션에서 이미 검증된 패턴(`class="absolute inset-0 h-full w-full"`로 커버아트 전체를 채우는 커스텀 인라인 SVG)으로 전환. 다운로드한 로고 SVG 4개는 `assets/image/`에 남겨뒀지만 현재 `index.html`에서는 참조하지 않음(추후 상세 페이지 헤더 등에서 재사용 가능).
  - **커버아트 색상을 JS 대표 색상(노란색)으로 통일**: 기존 `--from:#7dd3fc;--to:#075985`(하늘색) → `--from:#fde047;--to:#854d0e`(Tailwind yellow-300~yellow-800)로 5개 카드 전부 변경(사용자 지시).
  - **카드별 커스텀 아이콘**: Frontend=브라우저 창 안에 React 원자 심볼(cyan `#61dafb`) + UI 블록, Backend=서버 랙(Node 그린 `#68a063` 강조)↔DB 원통 사이 양방향 화살표, DevOps=컨테이너 3개(Docker 블루 `#2496ed`가 가운데 강조) + 위쪽 톱니바퀴, CI/CD=4단계 파이프라인 노드(GitHub Actions 블루 `#2088ff`가 진행 중 단계), Full-stack 통합 프로젝트=계층 3개가 화살표로 위→아래 이어지는 기존 아이콘(색상만 새 배경에 맞춰 갱신).
  - **Swiper 캐러셀 제거 → Language와 동일한 정적 그리드**: 카드가 5장으로 확정되면서 Web & WebApp/Unity/Agent AI가 공유하던 `.course-swiper`(`slidesPerView:'auto'` + 카드 고정폭) 구조 대신, `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-4 mt-8` 정적 그리드로 전환(Language 섹션과 완전히 동일한 마크업) — 섹션 헤더의 `swiper-nav-group`(이전/다음 화살표)도 제거. Unity/Agent AI 두 섹션은 여전히 4카드 Swiper 구조를 유지하므로, `.course-swiper` 순회 JS는 그대로 두고 이 섹션만 클래스를 빼는 방식으로 변경. **2026-08-24 다시 Swiper로 전환** — 모바일에서 스와이프 네비게이션이 빠진 문제가 버그로 제보되어, Language 섹션과 함께 Algorithm의 "고정 개수 Swiper" 패턴으로 되돌아감(위 Algorithm 섹션 항목의 "Language·Web & WebApp도 Swiper로 통일" 참고) — 이 정적 그리드 설명은 그 이전 히스토리로 남겨둠.
  - 5개 카드 모두 아직 상세 페이지가 없어 비클릭 `<div>`(href 없음, "준비중" 배지도 없음 — Unity/Agent AI 섹션과 동일하게 이 섹션 자체가 통째로 미착수 상태이므로 개별 배지 불필요)로 유지. 상세 페이지 제작 시 Language 페이지(`lang-cp.html`)류의 12개 커리큘럼 아코디언 카드 형식을 템플릿으로 삼을 예정. 상세: `report/2026-08-23-web-webapp-cards.md`.
- `src/web-frontend.html` / `src/web-backend.html` / `src/web-devops.html` / `src/web-cicd.html` / `src/web-fullstack.html` — Web & WebApp 코스 5개 카드의 상세 페이지(2026-08-23, 카드 구성 직후 같은 날 제작). Algorithm 코스가 시각화 엔진(step 배열 재생) 중심인 것과 달리, Web & WebApp은 다루는 범위가 넓은 주제라 **`lang-cp.html`류의 12개 커리큘럼 아코디언 카드 형식**을 그대로 템플릿으로 채택(사용자와 "Algorithm처럼 시각화하기는 어려울 듯" 논의 후 결정) — 브레드크럼+타이틀, 뱃지 없이 번호가 매겨진 카드 제목, 클릭 시 펼쳐지는 `curriculum-detail`(CSS Grid `0fr→1fr` 트랜지션), 카드가 포커스를 잃으면 자동으로 닫히는 스크립트까지 `lang-cp.html`과 완전히 동일한 구조를 재사용.
  - **Web & WebApp 코스 바로가기 탭**: `lang-*.html`의 "언어 바로가기 탭"과 동일한 패턴으로, 5개 파일 모두 Frontend/Backend/DevOps/CI-CD/Full-stack 통합 프로젝트로 이동하는 pill 탭을 상단에 공유(`aria-current="page"`로 현재 페이지 표시). 새 Web & WebApp 페이지를 추가하거나 이름을 바꿀 때는 **5개 파일 전부**의 탭을 함께 갱신해야 한다(언어 페이지와 동일한 주의사항).
  - **12개 커리큘럼 항목 구성**: 각 페이지 기초(1~5, `data-category="basic"`)/실습(6~10, `practice`, sky 테두리)/프로젝트(11~12, `project`, violet 테두리+링) 3단 구성은 `lang-cp.html`과 동일하되, 항목 배분은 코스 성격에 맞춰 조정 — Frontend/Backend/DevOps/CI-CD는 "개념 학습 10~11개 + 마무리 미니 프로젝트 1~2개" 구조인 반면, Full-stack 통합 프로젝트만 예외적으로 12개 항목 전부가 **기획→설계→구현→배포→회고**로 이어지는 하나의 프로젝트 진행 단계(새로운 개념을 가르치는 게 아니라 앞의 4개 코스에서 배운 것을 순서대로 엮어 실행하는 캡스톤이므로) — 각 항목의 detail 설명에서 "Backend 코스에서 다룬 ~을 이번엔 실제로", "DevOps 코스에서 익힌 ~을 적용" 식으로 다른 4개 페이지를 명시적으로 되짚는 문장을 넣어 통합 코스라는 성격을 드러냄.
  - **CI/CD 페이지 전용 보너스: 파이프라인 시각화 위젯**: 사용자와 "Algorithm 방식은 어렵겠지만 CI/CD는 파이프라인 자체가 순차적 상태라 예외적으로 가능할 것 같다"고 논의한 뒤, 커리큘럼 목록 위에 작은 Push→Build→Test→Deploy→Live 5단계 파이프라인 재생 위젯을 추가(Algorithm 코스의 "step 배열 사전 계산 → 재생 컨트롤이 인덱스만 이동" 패턴을 아주 단순화해 재사용, 힙/BST처럼 새 엔진을 통째로 만들 필요 없이 원형 노드+연결 화살표만으로 구성). 시나리오 2종: 성공 흐름(5단계 전부 순서대로 완료) / 테스트 실패 흐름(Test 단계에서 실패해 Deploy·Live에 도달하지 못하고 파이프라인이 중단됨, 실패 시 롤백된다는 설명 포함) — 재생/일시정지/이전/다음/처음/끝 컨트롤은 다른 시각화 페이지와 동일한 패턴이지만 속도 조절 버튼은 생략(간단한 5스텝 내외라 불필요하다고 판단, 700ms 고정).
  - 커버아트는 랜딩 카드와 동일한 노란색 계열(`--from:#fde047;--to:#854d0e`)을 상세 페이지 헤더에도 그대로 사용(자료구조 페이지들이 랜딩·상세 색을 통일했던 것과 같은 방식). 헤더 아이콘은 텍스트 배지(FE/BE/Ops/CI/CD/FS, algo 페이지들의 "Bub"/"Stk" 관례와 동일)로 랜딩의 풀-블리드 SVG 일러스트를 재사용하지 않음(헤더 아이콘 슬롯이 `h-16 w-16` 정사각형이라 4:3 비율 일러스트가 맞지 않아서).
  - **"마지막 카드 하이라이트" 색상 예외(2026-08-23)**: 다른 모든 섹션(Language/Algorithm/자료구조/Unity/Agent AI)은 마지막 카드에 브랜드 그린(`border-brand-400/60 ring-1 ring-brand-400/40`)을 공통으로 쓰지만, Web & WebApp 카드는 노란 톤이라 초록 테두리가 유독 튀어 보인다는 사용자 지적으로 Full-stack 통합 프로젝트 카드만 `border-yellow-500/60 ring-1 ring-yellow-500/40`로 교체 — 이 섹션만의 예외이며 다른 섹션은 브랜드 그린을 그대로 유지. 새 섹션을 카드 배경색이 브랜드 그린과 대비되는 톤(노랑·빨강 계열 등)으로 만들 때는 이 예외를 참고해 하이라이트 색을 섹션 톤에 맞출지 먼저 검토할 것.
- `src/about.html` — "회사 소개" 페이지(2026-08-23, `report/task & bug/task-01.md`의 요청으로 제작). liveklass.com/service/consulting 페이지를 참고하되, 사용자 지시로 **배너 + 소개 + CTA** 3개 요소만 남긴 단순 구성으로 축소했다(참고 페이지에 있던 고객 성공사례·후기·컨설턴트 프로필 카드 등은 이번 범위에서 제외). "소개" 본문 섹션(좌측 정렬 "Recode Coding을 소개합니다" + 2문단)은 사용자가 2026-08-24 "단계별로 레이어 구성해 나갈 예정"이라며 제거 지시 — 현재 페이지는 배너 → 중앙 정렬 타이틀/서브 텍스트(아래 항목) → Footer로만 구성되며, 이후 요청에 따라 새 레이어(섹션)를 하나씩 추가해나갈 예정이므로 다음 작업 시 이 구조를 임의로 되돌리거나 섣불리 완성형으로 채우지 말 것.
  - **상단 배너**: 처음엔 `index.html`의 `#hero-banner`와 동일한 다크 브랜드 그라디언트+블롭 배경으로 placeholder를 채워뒀으나, 페이지 제작 직후 사용자가 저장소에 실사진 `assets/image/bg-consult.png`(3168×1344, 8.9MB PNG — 숲 전망 통유리 코워킹 공간에서 노트북 작업하는 모습, 원본은 `assets/image/`에 그대로 보존)를 추가해, `sips`로 2400×1018 JPEG(quality 72, `assets/image/about-banner.jpg`, ~550KB)로 리사이즈·압축한 뒤 실제 배경 이미지로 교체 완료(2026-08-23). 배너는 `<img class="absolute inset-0 h-full w-full object-cover">` + 텍스트 가독성을 위한 `#about-banner-scene`의 `bg-gradient-to-r from-slate-950/85 via-slate-950/45 to-slate-950/10` 다크 스크림(좌측이 진하고 우측으로 갈수록 옅어짐 — 텍스트가 좌측에 있어 좌측만 어둡게 유지하고 사진 우측은 최대한 그대로 보이도록) 2단 구성. 배너 비율은 `aspect-[7/3]`(=1400:600, 원본 사진 비율 2.36:1과 거의 일치해 크롭이 미미함)로 고정 — 다른 사진으로 교체할 때도 이 비율에 가까운 소스를 준비할 것(권장 크기는 `report/task & bug/task-01.md`에 기록).
  - **카피**: "신입이 아니라, 시니어로 성장시킵니다" — `index.html`의 이미 확립된 브랜드 태그라인("신입 시니어를 훈련하다", `#philosophy` 섹션의 "신입 시니어로 성장")을 그대로 계승하되, 사용자 지시로 **"시니어"만 별도로 크게/브랜드 그린으로 강조**(제목의 "시니어"는 `text-4xl sm:text-6xl` + `text-brand-300`으로 나머지 텍스트보다 더 크게, 본문 소개 문단에서도 "시니어"가 등장하는 두 곳 모두 `<strong class="text-brand-700">`으로 강조). 초안은 "연차가 아니라 구조를 보는 시야가 시니어를 만든다"는 추상적 표현이었으나, 사용자가 "신입인데 시니어로 키운다는 것은 곧 실무에 준하는 경험과 경력을 훈련시킨다는 의미"라고 취지를 명확히 해 2026-08-24 문구를 "실무에 준하는 경험과 경력을 훈련시켜 ... 시니어를 만든다" 쪽으로 정정했고, 이어서 "키운다"보다 "성장시킨다"가 더 어울린다는 피드백으로 제목의 "시니어를 키웁니다" → "시니어로 성장시킵니다"로 한 번 더 정정(같은 날) — 이후 이 페이지나 다른 곳에서 "신입 시니어" 관련 카피를 새로 쓸 때는 ① 추상적 "시야/사고"보다 **실무 경험·경력 훈련**이라는 구체적 의미, ② "키운다"보다 **"성장시킨다"** 표현을 우선한다.
  - **CTA 버튼 위치**: 처음엔 `absolute right-6 bottom-14 sm:right-10 sm:bottom-20`로 배너 우측 하단에 겹쳐 배치 — 사용자가 "CTA는 우측 하단 워터마크를 덮도록 배치"라고 지시했고, 실제로 `about-banner.jpg` 우측 하단(사진 기준 우측에서 약 7~9%, 하단에서 약 17~22% 지점)에 반투명 스파클 워터마크가 있어 그 좌표에 맞춰 위치를 잡았다(단순한 모서리 밀착이 아니라 워터마크 중심에 맞춘 오프셋 — Python `PIL`로 크롭해 워터마크 위치를 직접 확인 후 산출). 버튼 문구는 2026-08-24 사용자 지시로 "1:1 상담 신청"에서 **"대학생 컨설팅 신청"**으로 변경(이 배너 CTA만의 예외 — 같은 페이지의 GNB CTA·Footer 링크는 여전히 "1:1 상담 신청" 그대로, 연결되는 Google Form 링크(href)는 동일하게 유지).
    - **모바일 겹침 버그 수정(2026-08-24)**: 이 `absolute` 좌표 방식은 배너가 `aspect-[7/3]`라 화면 폭이 좁아지면 배너 높이도 함께 줄어드는데, 반면 타이틀은 세로 중앙 정렬(`justify-center`)된 고정 폰트 크기(`text-3xl`/`text-4xl`) 2줄이라 좁은 화면에서 배너 높이 대비 텍스트가 차지하는 비율이 커지면서 CTA와 물리적으로 겹치는 버그가 있었다(사용자가 아이폰 스크린샷으로 제보, "대학생 컨설팅 신청" 버튼이 타이틀 첫 줄과 겹쳐 보임). `#about-banner-scene` 안을 `flex flex-col`(텍스트 `<div class="max-w-2xl">` + CTA) 하나로 합치고 CTA에 `mt-auto self-end`를 줘서 항상 텍스트 아래 여백에 배치되도록 구조를 바꿨다 — 좌표 기반 겹침 배치 대신 flow 기반 배치라 화면 폭에 관계없이 구조적으로 겹칠 수 없다(index.html 히어로 배너의 `mt-auto pt-10` CTA 패턴과 동일한 해법). 다른 사진으로 교체해 워터마크 위치가 달라지면, 이제는 `right`/`bottom` 좌표가 아니라 `#about-banner-scene`의 `p-8 sm:p-14` 패딩 값으로 CTA와 배너 가장자리 사이 여백을 조정한다.
    - **후속 버그 — CTA가 배너 하단에서 잘려 보임(2026-08-24, 사용자가 두 번째 아이폰 스크린샷으로 재제보)**: `mt-auto self-end`로 겹침은 해결됐지만, 배너 자체가 여전히 `aspect-[7/3]`(모바일 폭 기준 높이 ~163px)라 텍스트(2줄, ABOUT US 라벨 포함 약 114px)+CTA(약 45px)+상하 패딩(`p-8`=32px×2)을 다 담기엔 배너가 너무 얕아, CTA가 배너 바닥 아래로 밀려나 `overflow-hidden`에 잘려 절반만 보이는 문제가 있었다(직전 수정이 겹침은 없앴지만 공간 자체가 부족하다는 근본 원인은 남겨둔 것). `aspect-[7/3]`를 `aspect-[4/3] sm:aspect-[7/3]`로 바꿔 640px 미만(모바일)에서만 배너를 더 세로로 길게(같은 폭 기준 높이가 약 1.75배) 만들어 텍스트+CTA가 겹침·잘림 없이 전부 들어갈 여유 공간을 확보 — `sm:` 이상(태블릿·데스크톱)은 기존 `aspect-[7/3]` 그대로 유지되어 이미 검증된 데스크톱 렌더링에는 영향 없다.
    - **진짜 390px 폭 검증으로 추가 발견된 버그(2026-08-24, 위 "진짜 좁은 모바일 폭 검증" 방법 최초 적용 사례)**: 그동안 검증에 쓰던 500px가 실제 아이폰 폭(390~428pt)보다 넓어 가려져 있던 문제 두 건을 CDP 뷰포트 에뮬레이션으로 처음 발견 — ① 배너 타이틀 `h1`("신입이 아니라, 시니어로 성장시킵니다.")이 390px 폭에서 의도한 2줄을 넘어 3번째 줄로 흘러넘치면서 "성장시킵니"+"다."로 단어 중간이 끊김, ② 배너 다음 서브 텍스트 `p`("Recode Coding이 대학생의 눈높이에 맞춰 1:1로 컨설팅합니다.")도 마찬가지로 "컨설"+"팅합니다"로 끊김 — 둘 다 위 h2 사례와 같은 CJK 기본 줄바꿈 규칙 문제. h1의 "시니어로 성장시킵니다." 한 줄만 필요 폭이 컨테이너보다 워낙 커서(2단계 폰트 크기 혼재라 h2처럼 폰트 축소로 한 줄에 다 넣기는 무리) 두 곳 모두 `break-keep`만 추가해 줄바꿈은 허용하되 항상 단어 경계에서 끊기도록 수정(h1은 결과적으로 3줄이 되지만 "신입이 아니라," / "시니어로" / "성장시킵니다."처럼 깨끗하게 끊김). CTA는 이미 `aspect-[4/3]`로 확보한 여유 공간 덕분에 h1이 3줄이 되어도 겹침·잘림 없음을 CDP로 재확인.
  - 다른 `src/*.html`과 동일하게 GNB/Footer를 통째로 재사용하되, GNB의 "회사 소개" 링크가 이 페이지 제작을 계기로 전체 30개 페이지에서 활성화됨(아래 GNB 항목 참고) — `about.html`은 자기 자신을 가리키는 링크도 다른 페이지와 동일하게 평범한 활성 링크로 두고 별도의 "현재 페이지" 강조 스타일은 적용하지 않음(그래피티 페이지 등 기존 관례와 동일).
  - **배너 다음 레이어(중앙 정렬 타이틀+서브 텍스트, 2026-08-24)**: 배너 섹션 바로 다음에 `text-center` 전용 섹션을 추가 — "AI 시대, 진짜 대학생활을 위한 코딩 컨설팅"(`h2`, `text-2xl sm:text-4xl font-bold`) + 서브 텍스트("전공 수업만으로는 채워지지 않는 실무 감각을...", `max-w-xl mx-auto`로 줄 길이 제한) 2단 구성. 배너 CTA 문구가 "대학생 컨설팅 신청"으로 바뀐 것과 같은 맥락에서, 이 페이지의 타깃이 (신입 채용 대상 일반론보다) **대학생 대상 코딩 컨설팅**임을 명시적으로 드러내는 섹션 — 이후 이 페이지에 카피를 더 추가할 때 이 톤(대학생 눈높이)을 참고할 것.
    - **모바일에서 단어 중간이 끊겨 보이는 버그(2026-08-24)**: 이 h2에 `max-w`/`whitespace-nowrap` 제약이 없어 좁은 화면에서 자동 줄바꿈이 일어나는데, 기본 CJK 줄바꿈 규칙은 공백뿐 아니라 한글 음절 사이 아무 지점에서나 끊을 수 있어 "컨설팅"이 "컨설"+"팅"으로 쪼개져 2번째 줄에 "팅" 한 글자만 남는 부자연스러운 줄바꿈이 있었다(사용자가 "타이틀이 2줄로 삐져나온다"고 재제보). 1차로 `break-keep`(`word-break: keep-all`)만 추가해 단어 경계에서만 끊기도록 했으나, 실제 아이폰(뷰포트 폭 428pt)에서는 여전히 2줄로 wrap되는 것 자체가 문제로 재지적됨 — 사용자가 "이미지+텍스트 레이어 4종"(아래 항목, `whitespace-nowrap` + 반응형 폰트 축소로 항상 1줄 유지)과 동일한 방식을 요구해, `text-2xl sm:text-4xl` → **`text-xl sm:text-4xl` + `whitespace-nowrap`**로 교체(break-keep은 제거, nowrap이 줄바꿈 자체를 막으므로 불필요). 격리된 테스트 페이지로 텍스트의 실제 필요 폭을 측정(`text-xl`=20px 기준 자연 폭 320px, `text-2xl`=24px 기준 384px)해, 모바일 컨테이너 폭(안내 배너와 동일한 `px-6` 패딩 기준 iPhone 428pt→380px, 390pt→342px, 375pt→327px)이 text-xl의 320px보다 넉넉히 넓다는 걸 확인 후 결정 — text-2xl(384px 필요)은 이 폭들에서 전부 부족했던 것이 근본 원인이었다. 사이트에서 폭이 좁을 때 wrap될 수 있는 긴 문장형 타이틀은, 카드 제목처럼 짧은 타이틀과 동일하게 우선 `whitespace-nowrap` + 반응형 폰트 축소로 항상 1줄을 유지하는 쪽을 기본으로 하고, 문장이 너무 길어 폰트를 과도하게 줄여야 하는 경우에만 `break-keep`(줄바꿈은 허용하되 단어 경계 유지)을 대안으로 검토한다.
  - **이미지+텍스트 좌우 배치 레이어 4종(2026-08-24)**: 사용자가 "다음 레이어들은 수평으로 2개 영역(이미지 한쪽, 메인/서브 텍스트 한쪽)으로 4개 정도 구성할 예정, liveklass 참고 페이지처럼"이라고 요청 — liveklass.com/service/consulting의 "데이터 기반 분석"(이미지 좌/텍스트 우) → "회원 관리 CRM"(텍스트 좌/이미지 우) 교대 배치 패턴을 그대로 참고해, `grid lg:grid-cols-2 gap-10 lg:gap-16 items-center` 섹션 4개를 홀수 행(이미지 좌·텍스트 우)/짝수 행(텍스트 좌·이미지 우, `order-2 lg:order-1`/`order-1 lg:order-2`로 모바일은 이미지 먼저·데스크톱만 좌우 반전)으로 교대 배치했다. 처음엔 사용자 지시로 `cover-art` 그라디언트 블록 + "(임시)" 표기 가짜 카피로 레이아웃만 먼저 잡았다가(`.cover-art` CSS 규칙을 이 4개 섹션을 위해 about.html에 복원), 같은 날 이어진 지시로 4개 전부 실제 콘텐츠로 교체 완료:
    1. **이미지 소싱·워터마크 제거 공통 워크플로우**: 사용자가 Gemini로 생성한 원본 사진(`assets/image/consult-0{1..4}.png`, 각 2400×1792)을 저장소에 추가하면, Gemini가 이미지 안에 삽입하는 반투명 스파클 워터마크 위치를 먼저 확인(코너를 잘라 `Read`로 육안 확인하거나, 워터마크가 안 보이면 사분면별로 크롭해 탐색)한 뒤, Python `PIL`로 그 워터마크가 포함된 가장자리를 크롭해 4:3 비율(`aspect-[4/3]` 카드 규격과 동일)로 정확히 맞추고 1300×975로 리사이즈해 `assets/image/about-consult-0{1..4}.jpg`(quality 85)로 저장하는 방식을 4장 모두 동일하게 적용했다 — 워터마크가 이미지마다 다른 위치(우하단/좌하단/우측 인물 옷/배경 소품 등)에 나타나므로, 매번 먼저 위치를 확인한 뒤 크롭 방향(상/하/좌/우 중 어느 쪽을 얼마나 잘라낼지)을 결정하고, 주요 피사체(사람·모니터 등)는 최대한 보존하면서 4:3 비율을 정확히 맞추는 게 핵심 — 배너 이미지(`about-banner.jpg`) 때는 CTA로 워터마크를 "덮는" 방식을 썼지만, 이 4개 레이어는 CTA가 없는 순수 이미지 카드라 크롭으로 워터마크 자체를 제거하는 방식을 사용했다(사용자가 "이미지 만들면 크롭해서 적용해줘"로 이 방식을 명시적으로 확정).
    2. **레이어 1** (`about-consult-01.jpg`, 이미지 좌·텍스트 우): 주제는 사용자 지정 "AI-Agent와 함께하는 거시적 사고의 확장 연습" → 이후 "AI Agent 시대, 지식의 습득을 넘어 거시적 통찰로"로 타이틀 교체(서브 텍스트도 새 타이틀에 맞춰 재작성: 지식 습득의 가치 하락 → 질문·판단력이 경쟁력 → Recode Coding의 훈련이 그 능력을 기른다는 3단 논리로 설득력 구성).
    2-1. **타이틀 스타일 확정(공통 패턴)**: 사용자가 "배지 같은 텍스트(녹색)는 제거, 타이틀은 한 줄로 구성" 지시 → 이후 4개 레이어 전부 이 스타일로 통일: 카테고리 배지(`<p class="text-xs font-mono...">`) 없이 `h3`가 곧바로 시작하고, `h3`는 `text-xl sm:text-2xl lg:text-3xl font-bold text-slate-900 tracking-tight whitespace-nowrap`로 반응형 폰트 크기를 낮춰가며 강제로 한 줄 유지(브라우저에서 `scrollWidth > clientWidth`로 각 타이틀·서브 텍스트 오버플로우 없음을 확인하는 절차를 4개 레이어 전부에 적용). 섹션 상단 여백도 이 때 `py-14` → `pt-24 pb-14`로 늘려 바로 위 중앙 정렬 섹션과 간격을 더 뒀다(레이어 1에만 적용, 2~4번째는 `py-14` 유지).
    3. **레이어 2** (`about-consult-02.jpg`, 텍스트 좌·이미지 우): 주제 "지식 위에서 훈련을 통해서 비로소 지혜가 쌓인다" — 암기가 아니라 도제식(사수 옆에서 함께 고쳐나가는) 훈련이라는 맥락으로 서브 텍스트 구성.
    4. **레이어 3** (`about-consult-03.jpg`, 이미지 좌·텍스트 우): 처음 타이틀 "실력은 혼자 자라지 않는다, 사수 곁에서 자란다"였다가 사용자 지시로 **"左멘토 右AI-Agent"**(한자 표기)로 교체 — 레이어 1의 AI-Agent 주제와 레이어 2~3의 사수/멘토링 주제를 하나로 묶어 "속도는 AI-Agent가, 판단은 사수가" 역할 분담 구조로 서브 텍스트를 재구성, 사이트 전체에서 유일하게 한자(左/右)를 타이틀에 쓰는 페이지.
    5. **레이어 4** (`about-consult-04.jpg`, 텍스트 좌·이미지 우): 주제 "실전은 경험하는 곳이 아니라 증명하는 무대" — 이후 사용자가 직접 파일을 수정해 "실전은 경험이 아니라 증명하는 무대"로 축약(2026-08-24, `about.html`에 직접 손댄 유일한 사례 — 다음 작업 시 이 문구가 이미 사용자의 최종 확정본임을 전제할 것). 서브 텍스트는 "많이 아는 것보다 직접 만들고 방어해 본 경험이 진짜 실력"이라는 논리로, 페이지 전체 4개 레이어의 마무리(포트폴리오/발표) 역할.
    - 원본 사진 `assets/image/consult-0{1..4}.png`(각 8~9MB)는 크롭 소스로 저장소에 그대로 보존, 실제 페이지에서는 크롭·압축된 `about-consult-0{1..4}.jpg`(각 1300×975, 300KB 내외)만 참조한다.
- **GNB(전역 내비게이션)**: 2026-08-22부터 헤더(`<header>...</header>` 전체, 로고+nav+CTA+모바일 메뉴)는 30개 페이지(`index.html`, `src/graffiti*.html` 3개, `src/lang-*.html` 5개, `src/algo-sort.html`, `src/algo-{bubble,selection,insertion,merge,quick}-sort.html` 5개, `src/algo-binary-search.html`, `src/algo-bfs.html`, `src/algo-dfs.html`, `src/algo-stack.html`, `src/algo-queue.html`, `src/algo-linked-list.html`, `src/algo-hash-table.html`, `src/algo-heap.html`, `src/algo-bst.html`, `src/web-frontend.html`, `src/web-backend.html`, `src/web-devops.html`, `src/web-cicd.html`, `src/web-fullstack.html`, `src/about.html`) 전부 **완전히 동일한 마크업**을 쓴다 — 페이지별 커스텀 nav 링크(예: 예전 `graffiti*.html`의 "철학"/"훈련 코스", `lang-cp.html`의 "훈련 코스")는 제거하고, `index.html`의 GNB(회사 소개/원장님의 낙서/wonoj/LMS·business 준비중·studio/1:1 상담 신청)로 통일했다. 새 페이지를 추가할 때는 이 헤더 블록을 그대로 복사하고 로고·"원장님의 낙서" 링크의 상대경로만 위치에 맞게 조정한다(`src/` 페이지는 로고 `../index.html`, "원장님의 낙서" `graffiti.html`; 루트 `index.html`은 로고 `#`, "원장님의 낙서" `src/graffiti.html`).
  - "회사 소개"는 2026-08-22 추가 당시엔 LMS/business/studio와 동일한 "준비중" 뱃지 + `aria-disabled="true"` 비활성 링크였으나, `src/about.html`(아래 항목 참고) 제작과 함께 **2026-08-23 활성화** — 30개 페이지 전부에서 "준비중" 뱃지를 제거하고 `href`를 `about.html`(`index.html`에서는 `src/about.html`)로 연결했다. 배치(로고 옆 `<div class="flex items-center gap-6">`, `hidden md:inline-flex`로 데스크톱 전용, 모바일은 메뉴 nav 첫 항목)는 이전과 동일 — 이 위치가 `<nav>` 밖이라 critical CSS의 `header nav a{color:#94a3b8}` 회색 규칙이 적용되지 않는다. 활성화 1차 작업 때 이 점을 놓치고 `text-white`(항상 흰색, hover 조건 없음)로 지정해, 사이트 어느 페이지를 열어도 "회사 소개"만 클릭 전부터 흰색으로 튀어 마치 항상 포커스/활성 상태처럼 보이는 버그가 있었다(사용자가 "메뉴가 처음부터 focus 되어 있네?"로 지적, 같은 날 수정) — 다른 nav 링크(원장님의 낙서 등)와 동일하게 평소엔 회색, hover 시에만 흰색이 되도록 데스크톱 `text-slate-400 hover:text-white transition-colors`, 모바일 `hover:text-white transition-colors`(모바일은 부모 `<nav class="... text-slate-300">`에서 회색을 상속받으므로 별도 색상 클래스 불필요)로 정정했다. 새 준비중 메뉴를 추가할 때 데스크톱(로고 옆 또는 우측 nav, 어느 그룹에 넣을지 판단)과 모바일 메뉴 nav 둘 다 챙길 것 — 그리고 `<nav>` 밖에 배치하는 항목은 회색 기본색을 명시적으로 지정해야 함을 유의(이번 버그의 원인).
  - "studio"는 2026-08-25 `https://recodemate-mjrkgnh2vq-du.a.run.app/`로 활성화 — 30개 페이지 전부에서 "준비중" 뱃지(`<span>`)와 `aria-disabled`/`onclick="event.preventDefault()"`를 제거하고 `target="_blank" rel="noopener"` 외부 링크로 전환(다른 외부 링크인 "wonoj"와 동일한 패턴 — `href="#"` 내부 링크가 아니므로 "회사 소개" 활성화 때와 달리 상대경로 조정은 불필요). 같은 날 커스텀 도메인 연결이 준비되면서 링크를 `https://mate.recode.ai.kr`로 변경(30개 페이지 60곳 일괄 치환, `target="_blank" rel="noopener"` 패턴은 동일 유지). LMS/business 두 항목은 여전히 "준비중" 상태로 유지.
  - 헤더 포지셔닝은 `fixed top-0 inset-x-0`(2026-08-22 `sticky`에서 전환 — 스크롤을 당길 때 오버스크롤 바운스로 흔들리지 않도록 완전히 뷰포트에 고정). `fixed`는 문서 흐름에서 빠지므로 모든 페이지의 `<main>`에 `pt-16`(헤더 높이 `h-16`=64px과 동일)을 반드시 함께 줘야 본문이 헤더에 가려지지 않는다 — 새 페이지를 만들 때 이 둘은 항상 짝으로 다닌다.
  - **페이지 이동/새로고침 시 GNB가 순간적으로 흔들리는 문제**(2026-08-22 발견·수정, 1차 대응으로 재현되어 2차로 강화): Tailwind를 CDN 런타임 스크립트(`<script src="https://cdn.tailwindcss.com">`)로 쓰기 때문에, 그 스크립트가 로드·실행되기 전까지는 `header`의 Tailwind 유틸리티 클래스가 전혀 적용되지 않는다. 1차 대응(헤더 `position:fixed`+`height`+`main` 여백만 순수 CSS로 고정)은 헤더 박스 자체가 늘어졌다 접히는 큰 흔들림은 없앴지만, 안의 nav 링크(파란 밑줄 기본 브라우저 스타일)·CTA 버튼(모양 없는 텍스트 → 초록 알약 버튼)·로고 배지·모바일 메뉴 버튼이 Tailwind 로드 전후로 색상·모양이 바뀌는 잔여 깜빡임이 남아 있었음(테스트 방법: Tailwind 스크립트를 `setTimeout`으로 인위적으로 수 초 지연시키는 스크래치 사본을 만들어 로드 전/중/후를 각각 스크린샷 비교 — 이 저장소엔 남기지 않음, 필요시 같은 방식으로 재현 가능).
  - 2차 대응(critical CSS 확장): Tailwind `<script>` 앞의 `<style>` 블록에 헤더 안 요소들의 **최종 렌더링과 동일한 값**을 순수 CSS로 추가 — `header a{color:#fff;text-decoration:none}`(밑줄 제거), `header nav a{color:#94a3b8}`(비활성 링크 회색), `header nav + a{...초록 알약 버튼 스타일...}`(CTA), `header span.rounded-lg{...로고 배지 스타일...}`, `@media (min-width:768px){header #mobile-menu-btn{display:none}}`(데스크톱 폭에서 모바일 버튼 숨김, Tailwind의 `md:hidden`과 동일 breakpoint). 값을 Tailwind가 나중에 생성할 유틸리티와 동일하게 맞췄기 때문에 Tailwind 로드 후에도 재적용만 될 뿐 시각적으로 아무 변화가 없다(멱등) — 지연 테스트로 로드 전/후 스크린샷이 완전히 동일함을 확인.
  - **모바일 메뉴가 절대 열리지 않던 버그**(2026-08-24 발견·수정, 사용자가 "모바일에서 GNB가 안 보이고 메뉴 아이콘을 눌러도 아무것도 안 뜬다"고 제보): 2차 대응 당시 `header #mobile-menu { display: none; }`를 ID 선택자로만 작성해뒀는데, 이 규칙은 `mobile-menu.js`가 `menu.classList.toggle('hidden', ...)`로 `hidden` 클래스를 껐다 켜는 것과 무관하게 항상 우선 적용돼 — 즉 메뉴 버튼을 눌러 `hidden` 클래스가 제거돼도 `#mobile-menu`는 계속 `display:none`으로 고정되어 모바일 메뉴 자체가 영구히 열리지 않는 버그였다(30개 페이지 전부 동일 코드가 중복 정의되어 있어 사이트 전역에 영향). "회사 소개" 링크는 모바일 화면에서 이 `#mobile-menu` 안(nav 첫 항목)에만 존재하므로 메뉴가 안 열리면 함께 보이지 않는다. `header #mobile-menu.hidden { display: none; }`로 수정해 `hidden` 클래스가 있을 때만 숨기도록 정정 — JS가 클래스를 제거하면 이 규칙이 더 이상 매칭되지 않아 정상적으로 펼쳐지고, 데스크톱 폭에서는 Tailwind의 `md:hidden` 유틸리티가 그대로 항상 숨김 처리하므로 기존 동작에 영향 없다. 30개 페이지 전부 일괄 반영, v0.33.
  - 새 페이지를 만들 때 이 critical CSS 블록 전체(`header`/`header > div:first-child`/`header nav`/`header #mobile-menu.hidden`/`header a`/`header nav a`/`header nav + a`/`header span.rounded-lg`/미디어쿼리/`main`)를 헤더 마크업과 함께 그대로 복사할 것 — 하나라도 빠지면 그 요소만 다시 깜빡이거나(스타일 누락), `#mobile-menu`처럼 `.hidden` 조건 없이 ID만으로 작성하면 JS 토글이 영구히 무효화되는 버그가 재발한다.
  - 2차 대응 후에도 사용자가 흔들림을 재보고해, `PerformanceObserver({type:'layout-shift'})`로 실제 새로고침을 측정 — **레이아웃 시프트 0건**으로 확인됨(콘텐츠가 움직이는 종류의 흔들림은 더 이상 없음). 그래도 남아있다면 그건 시프트가 아니라 정적 다중 페이지 사이트가 새로고침/이동마다 이전 문서를 통째로 버리고 새로 그리는 **MPA 전환 자체의 끊김**일 가능성이 큼 — 이건 헤더 CSS로 고칠 수 없어서, CSS Cross-Document View Transitions(`@view-transition { navigation: auto; }`, critical `<style>` 블록 맨 앞)를 추가해 같은 오리진 페이지 전환을 브라우저가 자동 크로스페이드하도록 함(Chrome/Edge 126+, 빌드 도구 불필요, 미지원 브라우저는 그냥 무시 — progressive enhancement). 새 페이지를 만들 때 이 한 줄도 critical CSS와 함께 복사할 것. **사용자가 하드 리프레시로 재확인해 최종적으로 흔들림 해결 확인됨(2026-08-22)** — 상세 진단 과정은 `docs/DEVLOG.md` 참고.
- 경로 규칙: `src/` 안의 페이지끼리는 같은 디렉터리이므로 파일명만으로 링크(`href="graffiti.html"` 등)하고, 루트(`index.html`)나 `assets/`를 가리킬 때는 반드시 `../`를 붙인다(`href="../index.html"`, `src="../assets/js/mobile-menu.js"`). `assets/js/*.js` 파일끼리의 상호 import(`from './firebase-config.js'` 등)는 위치가 바뀌지 않았으므로 그대로 둔다.
- 모든 페이지 하단에서 `assets/js/mobile-menu.js`(루트에서 상대경로, `src/` 페이지는 `../assets/js/mobile-menu.js`)를 `defer`로 로드해 모바일 내비게이션 토글(`#mobile-menu-btn` / `#mobile-menu`)을 처리한다.
- `assets/image/` — 외부에서 가져온 로고·아이콘 등 이미지 리소스(예: `index.html` Language 카드의 언어별 로고 PNG, Web & WebApp 카드의 React/Node.js/Docker/GitHub Actions SVG 로고). 각 파일의 출처·라이선스는 도입 시점의 `report/*.md`에 기록되어 있으니, 재배포 범위를 확인하려면 해당 보고서를 참고한다.

## Firebase 연동 (`assets/js/`)

"원장님의 낙서" 게시판 전용. 파일별 역할:

- `firebase-config.js` — 프로젝트 설정값, 컬렉션 이름(`graffiti_posts`), 관리자 이메일(`ADMIN_EMAIL`), 그리고 `IS_PLACEHOLDER_CONFIG` 플래그(`projectId`가 `TEMP_`로 시작하면 실제 Firebase 프로젝트 없이 로컬 개발 모드로 동작). 다른 모든 Firebase 관련 파일이 이 플래그를 참조해 각자 폴백 로직을 중복 구현하지 않도록 함.
- `firebase-app.js` — `getFirebaseApp()` 싱글턴으로 Firestore/Auth가 동일 App 인스턴스를 공유.
- `firebase-client.js` — Firestore CRUD(`fetchPosts`/`createPost`/`updatePost`/`deletePost`). `IS_PLACEHOLDER_CONFIG`일 때는 즉시 `null`/`false`를 반환하고, 실제 연결 시에도 6초 타임아웃(`REQUEST_TIMEOUT_MS`)을 넘기면 실패로 간주해 연결을 `terminate` 후 리셋한다(무한 재시도 방지).
- `firebase-auth.js` — 원장 계정(`ADMIN_EMAIL`) 로그인/로그아웃/로그인 상태 확인.
- `admin-auth.js` — "수정"/"삭제"/"글 작성" 진입 시 뜨는 비밀번호 모달(`requestAdminPassword`)과 삭제 확인 모달(`confirmAction`). `IS_PLACEHOLDER_CONFIG`이면 로컬 하드코딩 비밀번호(`DEV_FALLBACK_PASSWORD`)로, 실제 config가 설정되면 Firebase Authentication으로 자동 전환된다 — 두 경로를 분기하는 지점이 이 파일이다.
- Firestore 보안 규칙은 `firestore.rules`에 있으며, 반드시 `ADMIN_EMAIL`과 규칙 파일 내 이메일이 일치해야 한다. 규칙 파일은 코드에서 자동 배포되지 않고 Firebase 콘솔에 수동으로 붙여넣어 게시해야 함.

## 저장소 관례

- `docs/` — `PRD.md`/`DEV_PLAN.md`/`DEVLOG.md`/`ARCHITECTURE.md` 4개 주요 문서가 위치한 폴더(2026-08-22 정리). `CLAUDE.md`만 Claude Code가 저장소 루트에서 자동으로 읽는 파일이라 루트에 남아 있다 — 4개 문서를 가리킬 때는 `docs/PRD.md`처럼 `docs/` 접두사를 붙인다.
- `tasks/*.md` — 작업 지시/요구사항을 기록. `report/YYYY-MM-DD-*.md` — 해당 작업을 완료한 뒤 진행 과정을 날짜별로 정리한 보고서. 새 작업을 마쳤을 때 이 관례를 따라 보고서를 남기는 흐름이 이미 자리잡혀 있다.
- **작업 완료 후 문서 점검(중요 규칙)**: 사용자가 지시한 작업이 끝날 때마다, 별도 요청 없이도 매번 `docs/PRD.md`, `docs/DEV_PLAN.md`, `CLAUDE.md`, `docs/DEVLOG.md`, `docs/ARCHITECTURE.md` 5개 문서를 검토해 업데이트가 필요하면 반영한다.
  - `docs/DEVLOG.md`는 날짜별 작업 일지 — 무슨 작업을 했는지 간단히 기록(장황한 서술 대신 요약).
  - `docs/PRD.md`/`docs/DEV_PLAN.md`는 제품 범위·요구사항이나 기술 계획이 바뀐 경우에만 갱신.
  - `CLAUDE.md`는 개발 명령, 페이지 구조, Firebase 연동, 저장소 관례 등 이 문서가 다루는 내용이 바뀐 경우에만 갱신.
  - `docs/ARCHITECTURE.md`는 서비스 아키텍처(배포 구조, 프론트엔드/백엔드 구성, 서브도메인 연동 상태 등)에 실질적 변화가 있을 때만 갱신 — 작업 과정 자체는 이 문서가 아니라 `report/*.md`에 남긴다.
  - 5개 중 바꿀 내용이 없으면 억지로 손대지 않고 넘어간다.
